---
title: "Contents"
sidebar_label: "Contents"
sidebar_position: 0
slug: /
---

:::info 🔒 Active work — who is writing what

Several Claude sessions work in this repo at once. **Check here before you start,
and add your own row when you claim something.**

| Area                                       | Claimed by         | Since      | State                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------------------------------ | ------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **React** — `docs/react/`, **PATTERNS · CHUNK A** | session `02b2af2d` (prior: **A** `bfcb390b`, **B** `05921047`) | 2026-08-17 | ✅ **CHUNK A COMPLETE — 6 topics, 13 files, 2,987 lines, 0 over the 300-line cap, 2,706 internal links across all of `docs/react/` resolved, 0 broken.** New cross-cutting section `docs/react/pages/patterns/` — a **selection layer**, not a new phase. The hub (210) indexes **21 already-written pattern pages** by the problem they solve; **nothing was re-implemented.** Six pages fill gaps each **verified absent by grep** before writing: ✅ **01 · Headless components** — 🔴 **re-tiered to Master and rewritten as a 6-chunk topic (1,531 lines)** after the user challenged the depth: the first version argued for APG keyboard contracts and demonstrated `aria-expanded`. Now quotes the **listbox spec in full** (fetched, ~30 requirements), then builds it — roving `tabindex`, arrow keys, `Home`/`End`, type-ahead with its timing rule, prop getters — then four delivery shapes and the production limits · ✅ **02 · The state reducer pattern** (229 — the `(state, action, changes)` third argument, and action types becoming public API) · ✅ **03 · Polymorphic components** (218 — `<button>` vs `<a>` semantics; the capitalization rule; changing `as` remounts the subtree) · ✅ **04 · Prop getters** (236 — spread clobbering, the `...rest` position as default-vs-invariant, and `mergeRefs` dropping React 19 ref-callback cleanups) · ✅ **05 · Provider composition** (230 — the `reduceRight` fold is cosmetic; the real fix is fewer providers *at the root*, and provider **count is not the metric**) · ✅ **06 · Container and presentational** (234 — the author's 2019 retraction, **fetched from the source**, and why RSC brings the split back as an enforced boundary). ⚠️ **Build NOT run** — rule 12's registry row was never claimed; pages were **link-checked against the filesystem**. ⚠️ `docs/frontend-architecture/…/01-composition-patterns.md` overlaps — cross-linked, never edited. **Chunk B (local site search) is still unclaimed** and touches only `docusaurus.config.js` + `package.json`. Split rules and both prompts: `devbible/project_react_patterns_and_search_split.md`; progress: `devbible/progress_react_patterns_chunk_a.md`. |
| **PostgreSQL** — all of `docs/postgresql/` | session `052a10c2` | 2026-08-13 | ✅ **COMPLETE — RELEASED, free to pick up.** Phase 13 finished 18/18 (every stamp gone), plus new phase-2 topic 17 (money) and phase-3 topic 20 (multi-tenancy). **298 pages, 298 carrying `> Verified:`, 0 files over 300 lines, 0 broken links.** Topics 07–18 are documentation-validated under the no-new-sandboxes rule and say so inline. Remaining PG work is *review only* — the rubric pass and `/code-review ultra`.                                                                                       |
| **JavaScript · chunk A** — phases **5, 11** (built-in library + data transfer) | session `3d9f98b8` | 2026-08-15 | 🔴 **Chunk A of a FOUR-way split (2026-08-15) — this replaces the old lane A/B split, which is closed.** ✅ **CHUNK A IS FINISHED — phase 11 is complete at every tier, 21/21** (written this session: 13–21; written 2026-08-15: ✅ **08 · Aborting and timing out** 565 lines, ✅ **09 · Cookies** 497, ✅ **10 · Web storage** 520, ✅ **11 · Uploading files** 684 (4 files), ✅ **12 · `Blob`/`File`/object URLs** 487, ✅ **13 · WebSocket** 1,320 lines across **5 chunks + index**, ✅ **14 · Same-origin and `postMessage`** 525, ✅ **15 · CSP** 521 — **phase 11's Understand tier is COMPLETE**, ✅ **16 · IndexedDB** 288, ✅ **17 · Service workers** 263, ✅ **18 · Server-sent events** 230, ✅ **19 · Streams** 241, ✅ **20 · `sendBeacon` and keepalive** 190, ✅ **21 · `XMLHttpRequest`** 193). ✅ **Phase 5 is COMPLETE at every tier — 26/26** (Master 8/8 · Understand 14/14 · Know 4/4), finished 2026-08-15. Written 2026-08-15: ✅ **18 · `Object` statics** 1,045 lines, ✅ **19 · `Date`** 1,070, ✅ **20 · `Intl`** 918, ✅ **21 · `structuredClone`** 207, ✅ **22 · Array-likes and iterables** 569, ✅ **23 · `WeakMap`/`WeakSet`** 485, ✅ **24 · `Temporal`** 547, ✅ **25 · Typed arrays/`ArrayBuffer`/`DataView`** 534, ✅ **26 · Text encoding** 478 — **9 topics / 30 files / 5853 lines this session** and phase 11 (14: topics 08–21, aborting → `XMLHttpRequest`). They pair on purpose: `structuredClone`, typed arrays and `TextEncoder` are what phase 11's `postMessage`, `Blob` and streams pages lean on. Tier-locked to **Understand and Know** — Master is closed at 99/99 and is not reopened for depth. The 2026-08-14 scope cut stands (21 dropped, 34 parked). Work on `main`, stage explicit paths, **never `git add -A`**. Resume file: `devbible/progress_javascript_split_4way.md`. See the [JavaScript claim notice](javascript/pages/README.md) |
| **JavaScript · chunk B** — phases **6, 17** (iteration + machine coding) | session `233dede7` (took over from `7c6611b4`) | 2026-08-15 | 🔴 **Chunk B of a FOUR-way split (2026-08-15).** ✅ **PHASE 6 COMPLETE — 13 of 13 topics at every tier, 40 files, 0 over the 300-line cap.** Ten topics written 2026-08-15: **04** the iteration protocols (585 lines) · **05** generators (604) · **06** async iterators (541) · **07** paginating an API with an async generator (553) · **08** early exit inside iteration (483) · **09** two-way generators (503) · **10** `yield*` delegation (461) · **11** iterator helpers (421) · **12** a collection class (465) · **13** driving an iterator by hand (445) — every one a `NN-topic/` directory of 3 files. 🚧 **Phase 17 · Machine coding under way — ✅ 05 · An `EventEmitter`** (3 files, 500 lines) ✅ **06 · Deep clone** (3 files, 440 lines) ✅ **07 · A concurrency-limited task queue** (3 files, 465 lines) ✅ **08 · Retry with backoff** (3 files, 449 lines) ✅ **09 · An LRU cache in O(1)** (3 files, 462 lines) ✅ **10 · A Promise from scratch** (3 files, 480 lines) ✅ **11 · `memoize`** (3 files, 435 lines) ✅ **12 · Deep equality** (3 files, 411 lines) ✅ **13 · `curry`, `pipe`, `compose`** (3 files, 424 lines) ✅ **14 · `promisify` and a callback↔promise bridge** (3 files, 605 lines) and ✅ **15 · A rate limiter** (3 files, 598 lines — the token bucket · windows, and the server) — **the Understand tier of phase 17 is COMPLETE, 05–15**; ✅ **16 · `new`, `Object.create` and `instanceof` by hand** (3 files, 480 lines); ✅ **17 · A tiny pub/sub and a reactive `signal`** (3 files, 580 lines) and ✅ **18 · A virtual-DOM diff in outline** (3 files, 462 lines). 🏁 **PHASE 17 IS COMPLETE — 18/18 at every tier (Master 4/4 · Understand 11/11 · Know 3/3), and with phase 6 already closed, CHUNK B IS FINISHED — 0 topics left.** Tier-locked to **Understand and Know**; Master is closed at 99/99. Documentation-validated against MDN, named per page — **no sandbox, no timings, no console blocks**. Work on `main`, stage explicit paths, **never `git add -A`**. Resume file: `devbible/progress_javascript_split_4way.md`. See the [JavaScript claim notice](javascript/pages/README.md) |
| **JavaScript · chunk C** — phases **7, 8** (async + modules, errors, tooling) | ✅ session `f7bca7a9` (took over from `f6dffd4a`) | 2026-08-15 | ✅ **DONE — chunk C of a FOUR-way split (2026-08-15) — this replaces the old lane A/B split, which is closed.** ✅ **PHASE 7 IS COMPLETE AT EVERY TIER — 22/22, 69 files, 11,962 lines, 0 over the 300-line cap.** Eleven topics written in this session: Understand **12–19** (Timers · Creating promises · Cancellation · Timeouts, retries, backoff and jitter · Concurrency limiting · Race conditions in a UI · `queueMicrotask` · Event loop: browser vs Node — 26 files / 4,397 lines) and Know **20–22** (`Promise.withResolvers` · Thenables · Async work and backpressure — 3 flat files / 644 lines). ✅ **CHUNK C IS COMPLETE — phase 7 22/22 AND phase 8 18/18, finished 2026-08-15.** the Understand tier of phase 8 is ✅ complete (05–14). ✅ **8/05 · Dynamic `import()`** (3 files, 493 lines), ✅ **8/06 · Circular imports** (3, 474) ✅ **8/07 · `throw`, `try`/`catch`/`finally`** (3, 510) ✅ **8/08 · Custom error classes** (3, 468) ✅ **8/09 · Failing well** (3, 463) ✅ **8/10 · Global error handling** (3, 521) ✅ **8/11 · The memory model** (3, 494) ✅ **8/12 · Finding a leak** (3, 428) and ✅ **8/13 · Bundlers and the build** (4 files, 639 lines — what a bundler does · tree shaking and what defeats it · analysing and shrinking) and ✅ **8/14 · Testing JavaScript** (4 files, 667 lines — the shape of a test · faking time, network and modules · what is worth testing) and ✅ **8/15 · CommonJS in a modern world** (3 files, 457 lines — the CommonJS model · interop both ways) ✅ **8/16 · `AggregateError`** (1 flat file, 197 lines) ✅ **8/17 · Mark-and-sweep and generational GC** (1 flat file, 183 lines) and ✅ **8/18 · Linting and formatting** (1 flat file, 195 lines — **which closes phase 8 and the chunk**). 🔴 **Cadence tightened to PER FILE** on the user's warning that usage is above 90%. **Nothing queued — chunk C's lock covers phases 7 and 8 only, and both are done.** Documentation-validated throughout — MDN, the HTML Standard, ECMAScript, RFC 9110, the Node docs and the AWS Builders' Library, each named in the page's `> Verified:` line; **no sandbox, no console blocks**. Tier-locked to **Understand and Know** — Master is closed at 99/99 and is not reopened for depth. The 2026-08-14 scope cut stands (21 dropped, 34 parked). Work on `main`, stage explicit paths, **never `git add -A`**. Resume file: `devbible/progress_javascript_chunk_c.md` (cursor also in `devbible/progress_javascript_split_4way.md`). See the [JavaScript claim notice](javascript/pages/README.md) |
| **JavaScript · chunk D** — phases **12, 18** (browser platform + storefront) · 🏁 **phase 18 SPLIT, all three landed: ✅ 11 written here; ✅ 12 by session `78e4bc26`; ✅ 15 by session `0e830881`** | session `dbaa68e7` | 2026-08-15 | 🔴 **Chunk D of a FOUR-way split (2026-08-15) — this replaces the old lane A/B split, which is closed.** 🏁 **PHASE 18 IS COMPLETE — 10/10 in scope, and with it CHUNK D and JavaScript itself.** ✅ **18 · 15 · Review uploads** — 4 files + index / 1,118 lines, 0 over the cap, written 2026-08-15 by session `0e830881` (the photo list · uploading and submitting · resizing before upload · a file you cannot trust); ⚠️ closed **link-checked against the filesystem, not built**, under the new no-unclaimed-build rule. ✅ **18 · 12 · Long lists without freezing** — 4 files / 782 lines, 0 over the cap, written 2026-08-15 by session `78e4bc26` (why a long list freezes · a windowing list from scratch · variable heights and what windowing breaks). ✅ **PHASE 12 IS COMPLETE — 21/21 at every tier** (Master 2 · Understand 11 · Know 7 · When Needed 1; written 2026-08-15: ✅ **15 · Cross-tab coordination** 4 files / 771 lines, ✅ **16 · Clipboard, Web Share and File System Access** 4 files / 660 lines, ✅ **17 · Permissions, Geolocation and Notifications** 4 files / 588 lines, ✅ **18 · Media from JavaScript** 4 files / 587 lines, ✅ **19 · Page Visibility, Wake Lock and Battery** 3 files / 374 lines, ✅ **20 · Internationalisation in the browser** 3 files / 411 lines, ✅ **21 · `SharedArrayBuffer` and `Atomics`** 3 files / 395 lines) and phase 18 (**only 11, 12 and 15** — infinite scroll, long lists, review uploads; the rest of phase 18 is dropped). The three storefront topics are exactly the observer/worker/File work phase 12 covers. Tier-locked to **Understand and Know** — Master is closed at 99/99 and is not reopened for depth. The 2026-08-14 scope cut stands (21 dropped, 34 parked). Work on `main`, stage explicit paths, **never `git add -A`**. Resume file: `devbible/progress_javascript_split_4way.md`. See the [JavaScript claim notice](javascript/pages/README.md) |
| **TypeScript · Part A** — 🔴 **narrowed 2026-08-17 to phase 5 only** (`docs/typescript/pages/phase-5-type-level/`); phase 6 split off as **Part C** | session `bbd2d39d`, 2026-08-17 (took over from `3af83cbb`) | 2026-08-17 | 🔴 **Active — Part A reopened 2026-08-15** on *"Pick typescript a"*: phases **2–6**, the type system. Part B (phases 7–12) is **live again under another session** — `phase-7-*` began appearing mid-session on 2026-08-15 — and is **not touched here**. **Phases 0, 1 and 2 are COMPLETE**; ✅ **PHASE 3 · GENERICS COMPLETE — 14/14, 22 files, 0 over the 300-line cap.** Three topics written here: **12 · `const` type parameters** (289 lines) · **13 · When *not* to write a generic** (293) · **14 · Variance** (281), plus all 12 stale forward references in the phase repointed to real links. 🚧 **Phase 4 · Classes, objects and declaration merging OPEN — 1/14.** ✅ **01 · Module augmentation** (Master, 4 files / 677 lines: what merging and augmentation are · augmenting a package · why it did not load) — the `Express.Request` chain and the `req.user` extension point are **read from the installed `@types/express-serve-static-core` 5.1.3**, and all six augmentation error codes from the compiler's diagnostic table. ✅ **02 · Access modifiers** (3 files / 453 lines — soft private and hard private · visibility rules and choosing): the first draft ran to **327 lines and was SPLIT on a concept boundary, not trimmed**. ✅ **03 · Parameter properties** (272 lines) — the one class feature in common use that **emits code**, so Node's strip-only mode rejects it and `erasableSyntaxOnly` flags it as `TS1294`; links to Phase 0 · 04 for the captured runtime error rather than reproducing it. ✅ **04 · `implements` vs `extends`** (276 lines) — the handbook's emphatic *"doesn't change the type of the class or its methods **at all**"*, the implicit-`any` parameter it does not fix, and the two mirror-image diagnostics (`TS2720`/`TS2689`) that each suggest the other keyword. ✅ **05 · Interface declaration merging** (3 files / 438 lines — drafted flat at **308 and split**) — the handbook's Namespace/Type/Value declaration table as the key to every merge and every disallowed one, and the accidents: a duplicated interface **combines silently** where a duplicated `type` is `TS2300`, `TS2717`'s word *"subsequent"* as the tell, and how a non-module `.d.ts` merges across package boundaries. ✅ **06 · Global augmentation** (263 lines) — `globalThis`, and the rule nobody expects: **`var` creates a global property, `let`/`const` do not**, so `declare global { var x }` is the only correct spelling and `no-var` is wrong in exactly that position. ✅ **07 · Branded / nominal types** (264 lines) — labelled honestly as a **community pattern, not a language feature**, with only its primitives doc-validated; the real value is that the unsafe `as` appears **once**, in a constructor that validates. ✅ **08 · `readonly` and `!:`** (264 lines) — one is a guarantee the compiler enforces, the other is a **waiver that asserts nothing**; carries the four honest alternatives to `!` and the throwing-getter pattern that fails loudly instead of three frames away. ✅ **09 · Typing getters and setters** (Know, 186 lines) — divergent read/write types (TS 4.3) and the single rule that governs them, rememberable as the test `x.p = x.p`. ✅ **10 · `this` types** (Know, 228 lines) — polymorphic `this` (inferred, and silently discarded by annotating `: Box`), `this` parameters, `this is T` guards, and the handbook's honest arrow-vs-method trade. ✅ **11 · Abstract classes** (Know, 234 lines) — why `typeof Base` is not "a constructor for a Base", and how a construct signature moves the error from inside your function to the call site. ✅ **12 · Static members and the static side** (Know, 242 lines) — statics live on the constructor, which is why `TS2302` exists and why `TS2699` collides with `Function.name`; static blocks reach `#private` and cannot `await`. ✅ **13 · Decorators** (Know, 241 lines) — **two incompatible systems in circulation**, told apart by the parameter list, and why NestJS/TypeORM cannot move (no parameter decorators, no `emitDecoratorMetadata` in the standard proposal). Next: topic 14 · Mixins, which closes the phase. ⚠️ **Fixed on takeover:** `src/data/progress.js` had phase 3 at `pages: 14` with no `pagesPlanned`, so the site showed a phase that was really 11/14 as COMPLETE. **Part A is unaffected by the 2026-08-15 Part B cut — phases 2–6 all stay in scope.** Part A stands at **59 of its own topics; 45 remain** (phase 4 ×7, phase 5 ×16, phase 6 ×16). ⚠️ **Closed link-checked against the filesystem, NOT built** — the phase-close build was claimed in the registry and run, and it **failed before compiling a page** on Docker chunk C's invalid `docs/docker/pages/phase-9-mern-pern-stack/08-mongodb-in-a-container/_category_.json` (literal `\n` escapes instead of newlines, committed at `d2fc0e23`), which breaks `sidebars.js` and blocks the build for **every** session. Left for its owner per the one-language rule. Documentation-validated against the TypeScript handbook, the 4.7/5.0 release notes and typescript-eslint, with the compiler's own diagnostic table read rather than recalled; **no sandbox and no fabricated console output**. 🚧 **Phase 5 · Type-level programming at 8/16** — topics 01–07 written 2026-08-17 (22 files, 4,628 lines), and **08 · Knowing when to stop** written the same day as **11 chunks / 2,795 lines**, 0 over the 300-line cap. It is the phase's spine rather than an opinion page: every earlier topic forward-links to *"a clever type that produces an unreadable error message is a net loss"*, so it carries the argument in full — the **cost asymmetry** (written once at maximum context, failed against many times at minimum), the mechanical reason a computed type reports badly (**the checker reports on the type you produced, not the program that produced it** — there is no diagnostic for "the third branch did not match"), **one typo through three designs** where the unconstrained conditional gives *no error at all* and two overloads give `TS2769` + `TS2772`/`TS2770` per candidate, the **seven stopping tests**, and the **six cases that earn it**. Rule 1 was exercised four times: drafts at **384, 346, 331 and 313 lines were each SPLIT on a concept boundary and both halves then GREW** (384→511, 346→510, 331→521, 313→444) — nothing trimmed. Documentation is quoted verbatim from the handbook's *Do's and Don'ts* (*Use Union Types*, *Use Optional Parameters*, the pass-through `fn` example) and the wiki's *Performance* page (*Naming Complex Types*, *Using Type Annotations*, *Preferring Interfaces Over Intersections*, *Preferring Base Types Over Unions*); fourteen diagnostics were read from the 5.9.3 message table and confirmed in the 7.0.2 binary, including 🔴 **`TS2859` *"Excessive complexity comparing types"* — a diagnostic distinct from `TS2321` *"Excessive stack depth"* that had never appeared in this corpus**, which makes the walls five rather than four. **19 stale forward references across phase 5 were repointed** to real links now their targets exist; 0 broken links in `phase-5-*`. ✅ **09 · Type-level performance** followed as **4 chunks / 1,063 lines** — and it is the topic where reading the compiler beat recalling it. 🔴 **Every limit was read out of 5.9.3's checker source:** `TS2589`'s guard is `instantiationDepth === 100 || instantiationCount >= 5e6`, so it is **two failures with one message** and the usual "add a depth cap" is a coin flip; `TS2321` and `TS2859` come from the **same** `overflow` flag with the message chosen by `relationCount <= 0`; and `TS2590` has **two** sites — a cross product refused at ≥100,000 and a subtype reduction that *extrapolates* past a million after 100,000 pairwise comparisons. 🔴 **The headline find is `relationCount = (16e6 − relation.size) >> 3`** — the comparison budget is sixteen million **minus the relation cache**, divided by eight, so **it shrinks as a project fills that cache**, which is the real explanation for *"it compiles in the playground and fails in the repo"*. 🔴 **Second find: `isNonGenericTopLevelType` quits at a function body**, so a `type` alias declared inside a function is not eligible for instantiation's cheapest early-out — hoisting aliases to module top level is a one-line change with a mechanical justification. Constants are labelled **5.9.3's and explicitly not claimed for the 7.0.2 Go port**, and the seven slow shapes are ranked **by which budget they consume rather than by seconds**, because no sandbox covers this phase and a time ranking would have been invented. Attribution tooling is named and handed to **phase 12**. ✅ **10 · Deriving one function's type from another** — 3 chunks / 752 lines, the applied half of the extractor family (topic 03 chunk 04 keeps the mechanisms and is linked, not restated). Its one-sentence rule is 🔴 **infer the parameter tuple, do not extract it**: `<A extends unknown[], R>` resolves per call site, so it survives overloads and keeps genericity where `Parameters<typeof f>` **sees only the last overload** and instantiates type parameters away — and the return-position half of that collapse is the dangerous one, because it type-checks and is untrue. Also settled: **`(...a: never[]) => unknown` is a bound and `(...a: any[]) => unknown` is a signature you can call** (contravariance makes `never` accept every function and gives you nothing to pass — the same decision phase 4's mixins made for construct signatures); `Promise<Awaited<R>>` never `Promise<R>`, or the wrapper is wrong for exactly the async half of its callers; `Parameters<F>` **drops `this`**, so a naive method wrapper loses the receiver; and optional markers plus 4.0 labelled names survive a **spread** and are destroyed by rebuilding the list from indexed access. Four shapes given with the signature that holds up — and shape 4, the adapter that re-types a parameter, is the one the stopping tests tell you to replace with two hand-written signatures. ✅ **11 · Recursive types** — **5 chunks / 1,417 lines**, spread **201 · 270 · 282 · 291 · 295**, planned as three and split to five when chunk 02's draft measured **349 lines**: it was cut on a concept boundary and the material that came out earned two chunks of its own rather than being compressed back in. 🔴 **The topic's find is that there are THREE ceilings, not one, and only two of them are about recursion:** 100 for nested recursion and 1,000 for tail recursion (both `TS2589`, from `getConditionalType`'s `tailCount === 1e3` loop), and 🔴 **10,000 tuple elements — `TS2799` in type position, `TS2800` in expression position — read from `createNormalizedTupleType`'s `elements.length + expandedTypes.length >= 1e4` guard, checked AT THE SPREAD**, so a doubling accumulator (`[...Acc, ...Acc]`) dies in about fourteen steps with 986 tail-call iterations unspent and no depth cap can help. The accumulator conversion is taught from **the compiler team's own `GetChars` / `GetCharsHelper` example**, quoted verbatim from the 4.5 release notes along with the sentence that defines the condition — *"As long as one branch of a conditional type is simply another conditional type…"* — and 📌 **the notes never give a number**, so the 1,000 is labelled as a source read rather than documentation. Other settled points: the three seeds are the **identity elements** of the operation (`never`, `""`, `[]`) and a wrong seed produces a wrong type with no error; **a recursion whose input never shrinks terminates on a COUNTER, not on the input**; `infer H extends string` (4.8) is **load-bearing rather than sugar**, because the pre-4.8 hand-constrained form wraps the recursive call and loses tail position; **the conversion is order-safe for unions and order-changing for tuples and strings** — the release-notes example is safe because a union has no order, which makes accidental reversal the pattern's commonest silent bug; **only the recursing branch must be bare**, so the base branch is the right home for cosmetics, the final shape change and the last element a `Split` would otherwise drop; **a conditional in the argument list keeps tail position** but is instantiated per step; 🔴 **the public-alias split is a correctness issue, not style** — a defaulted type parameter is public, so seeding the accumulator is a legal call that yields a wrong type rather than a diagnostic; and a depth cap's real content is **what it returns AT the cap**, where `never` is the trap because it is assignable in enough positions that the failure surfaces far from the cause. Closes with the four circularity diagnostics a cap cannot help — `TS2456`, `TS2615`, and 🔴 **`TS2313` / `TS2716` ("circular constraint" / "circular default"), neither previously in this corpus** — with `TS2716` flagged as the one this very construction causes. ✅ **12 · `DeepPartial` / `DeepReadonly`** — **5 chunks / 1,225 lines**, spread **207 · 230 · 231 · 241 · 257**. 🔴 **Its headline is an inversion read straight out of the checker: the `T extends object` guard that appears in nearly every published deep helper is protecting against something that cannot happen.** `instantiateMappedType`'s inner `instantiateConstituent` applies the template only to constituents flagged `AnyOrUnknown` / `InstantiableNonPrimitive` / `Object` / `Intersection` and returns everything else through a bare `return t` — so a homomorphic mapped type **never maps a primitive**, while the guard costs a conditional's union distribution, a worse error shape and a changed `any` case. The guard *is* needed, for the cases nobody writes it for: functions and class instances, which are object-flagged. 🔴 **Second find, also structural: a mapped type cannot carry a call signature at all** — `resolveMappedTypeMembers` opens by setting the call- and construct-signature lists to `emptyArray` and only ever repopulates members and index infos, so a method that passes through a mapping comes out as `{}` and the symptom (*"This expression is not callable"*) appears in a file that never mentions the helper. 🔴 **Third: `DeepReadonly` should NOT have an array branch and `DeepPartial` MUST** — the homomorphic path has handled arrays *and tuples* correctly since 3.1, and an array guard matches tuples and flattens them (length, positions and 4.0 labels gone), but `DeepPartial` cannot use that path because `instantiateMappedTypeTemplate` applies `getOptionalType` to the array's **numeric** element, turning `string[]` into `(string | undefined)[]`. **The two helpers are not the same type with a different modifier: `readonly` composes with arrays and `?` does not.** The five guards are given in the order they are forced into (`any` first, because it satisfies both branches of every conditional below it), with the three limits no guard removes — the class list is hand-maintained and finite, private state is nominal so even a correct mapping is not assignable back, and depth is untouched. Chunk 04 is the design half: **`DeepReadonly` is a restriction and `DeepPartial` is a widening**, so the latter asserts that every subset of your structure at every depth is meaningful — false for most domains — and it belongs in exactly three places (patch body, fixture builder, config merge), all of them **partial in, complete out**; a `DeepPartial<T>` that is returned or stored has become a domain type nobody designed, paid for as `??` defaults invented at call sites in other files. Chunk 05 ranks the cost **by which budget it spends rather than by seconds** (no sandbox covers this phase): the fan-out, a per-node cost for every guard, and a per-use/per-file/per-keystroke cache that is cleared when the mapper pops — then six fixes cheapest-first, and the honest note that what a published deep-helper library actually sells you is **the guard list, maintained**, not the mapped type. Resume point: `devbible/progress_typescript_build.md`. |
| **TypeScript · Part C** — ✅ **COMPLETE 2026-08-17 · phase 6 topics **01–06** (`docs/typescript/pages/phase-6-modules-build/`) | session `f4392a13`, 2026-08-17 (took over from `5ff47a9c`) | 2026-08-17 | ✅ **DONE — 6 of 6. Session `f4392a13` took the lane over from `5ff47a9c` and finished it.** ✅ **04 · `lib`, `target` and the ambient environment** written 2026-08-17 — **9 files / 2,112 lines**, spread **100–298**, 0 over the 300-line cap: two different jobs · what a lib file actually is · the default `lib` and the `.full` files · every value `lib` accepts · the ambient environment is not the language · `types`, `typeRoots` and the four sources of a global · the diagnostics · choosing. 🔴 **The headline, read from `getDefaultLibFileName` rather than recalled: every `target` maps to a `lib.esNNNN.FULL.d.ts`, and the `.full` files reference `dom` + `dom.iterable` + `dom.asynciterable` + `webworker.importscripts` + `scripthost`** — so a project with no `lib` set **has the DOM at every target, including a Node service**, and writing `"lib": ["es2022"]` selects the non-`.full` file and *removes* it. That trap catches people doing the right thing, and it is the same shape as `types` replacing the default `@types` inclusion. ⚠️ **A banked note was wrong and is corrected on the page:** `targetToLibMap` (target → default lib, always `.full`) and `libMap` (the `lib` option's own values, never `.full`) are **two different maps**; conflating them hid the whole story. 🔴 **Second find — the syllabus's `structuredClone` question has a three-way answer.** Across all **100** shipped `lib.*.d.ts` files, `console`, `setTimeout`, `fetch`, `URL` and `structuredClone` are declared **only** in `lib.dom.d.ts` and `lib.webworker.d.ts`, and `process` in **none** — so they are host APIs, not JavaScript, and no ES lib will ever supply them. **`@types/node` 26.2.0 keeps them in `web-globals/` (16 files) and actively detects the DOM lib**, guarding declarations with `typeof globalThis extends { onmessage: any } ? {} : …` — repeated **14 times in `fetch.d.ts` alone**. Where the guard is absent (`timers.d.ts`) the two `setTimeout` declarations merge into an overload set instead, which is the mechanical cause of *"Type 'Timeout' is not assignable to type 'number'"*. 🔴 **Third find — the compiler's advice is a hardcoded switch, not a heuristic.** `getCannotFindNameDiagnosticForName` is a literal `switch` over **27 identifier names**; everything else gets a bare `TS2304`. So `document` tells you to add `dom` and `structuredClone`, `fetch`, `setTimeout`, `URL` and `AbortController` — **identical fix** — say nothing, which means the easiest problems produce the most helpful errors and the confusing ones produce the least. TS2583's version comes from `getScriptTargetFeatures`, a **48-entry** hand-written table, and all 18 of the switch's ES names were checked present in it. **`containerSeemsToBeEmptyDomElement` is the one real heuristic** — `lib` set without `lib.dom.d.ts` plus an *empty* type named `Element`/`Node`/`EventTarget`/`HTML*Element` yields **TS2812**. Also settled: `libEntries` lists `esnext.array`, `esnext.string` and `esnext.promise` **twice** (`new Map` takes the later; the help list keeps both); `dom` and `webworker` are alternatives, each declaring `self` once with a different type; `lib` values are lowercased before lookup; and `libReplacement`'s lookup dates from **4.5**, its flag from **5.8** (default `true`), with **6.0 flipping the default to `false`**. ✅ **05 · `isolatedModules`** followed — **6 files / 1,288 lines**, spread **95–284**, 0 over the cap: the one-file compiler · every rule it enforces · `const enum` under the flag · and `verbatimModuleSyntax` · adopting it. 🔴 **The topic's spine came from grepping every `getIsolatedModules()` site in the 5.9.3 checker rather than from a rule list:** all **ten** diagnostics — TS1205, TS1448, TS1289–1292, TS1269, TS2748, TS18055, TS18056, TS1280, TS1272, TS2865/TS2866 — are one idea, *"would a transpiler have to open another file to emit this line?"*. Two names in the compiler state it better than any doc: **TS18055's phrase *"syntactically recognizable"*** is the flag's whole standard, and **the checker's variable for TS2865's condition is literally `appearsValueyToTranspiler`**. 🔴 **Three implication chains, all computed rather than written:** `isolatedModules` is computed as **`isolatedModules || verbatimModuleSyntax`**, so a config naming neither can still enforce every rule; **`preserveConstEnums` is computed as `preserveConstEnums || isolatedModules`**, so turning the flag on *emits the enum object* and removes the only reason to write `const enum`; and **`ts.transpileModule` forces the flag on** via `transpileOptionValue: true`, with `optionsRedundantWithVerbatimModuleSyntax` containing exactly one name. The **ambient** `const enum` (TS2748) cannot be rescued by `preserveConstEnums` at all — a `.d.ts` emits nothing to preserve — which makes it the one rule with a behavioural rather than two-token cost. **`erasableSyntaxOnly` goes further**, TS1294 on every non-ambient enum, `import =`, namespace and parameter property, giving a strict containment order to adopt in. 🔴 **`generateTSConfig` — the function behind `tsc --init` — writes `isolatedModules`, `verbatimModuleSyntax`, `moduleDetection: force`, `noUncheckedSideEffectImports`, `strict` and `skipLibCheck` live under a header called *Recommended Options*, with `module: nodenext`, `target: esnext` and `"types": []` above** — which independently endorses topic 04 chunk 06's `types` argument. ✅ **06 · File extensions — `.ts`/`.mts`/`.cts`/`.d.ts`** — **5 files / 950 lines**: the extension table · how the compiler picks a file · the extension you type in an import · choosing. 🔴 **Three finds, all read from 5.9.3's own arrays:** `allSupportedExtensions` is **nested and both levels are load-bearing** — the outer array is the three format families, the inner arrays are resolution priority (`.ts .tsx .d.ts .js .jsx`), so a source file always beats its own build output and **a `.d.ts` beating a `.js` is the entire `@types` mechanism in one line**; 🔴 **`extensionsNotSupportingExtensionlessResolution` means `import "./util"` can NEVER find `util.mts`** — excluded, not discouraged, which is why a `.ts` → `.mts` rename breaks imports that never named an extension, and it is consistent with Node requiring explicit ESM extensions; and **`TS5096` differs between the corpus's two compilers** — 5.9.3 requires `noEmit` or `emitDeclarationOnly` for `allowImportingTsExtensions`, **7.0.2 also accepts `rewriteRelativeImportExtensions`**, the same reasoning either way (the `.ts` string must never reach a runtime unchanged). Also: `getOutputExtension` shows **`.tsx` emits `.jsx` only under `"jsx": "preserve"`**, `getDeclarationEmitExtensionForPath` carries a `.d.json.ts` row the source itself calls a *"drive-by redefinition"*, `allowJs` swaps the candidate list entirely (a different failure from `TS7016`), and **`<T,>`'s trailing comma is a parser hint**, not a style convention. 🔴🔴 **LANE C IS COMPLETE — 6 of 6 topics, 49 files, 10,040 lines, 0 over the 300-line cap, spread 77–298.** Boundaries held throughout: format detection stayed topic 01's, `verbatimModuleSyntax`'s elision stayed topic 02's, and `allowImportingTsExtensions`/`rewriteRelativeImportExtensions` in depth stayed phase 7's. **2,780 links across `docs/typescript` resolve, 0 broken.**  ✅ **01 · `module` and `moduleResolution`** (12 files, 2,904 lines) · ✅ **02 · `import type` and `verbatimModuleSyntax`** (7 files, 1,540) · ✅ **03 · Path aliases `paths`** (6 files, 1,246). **25 files, 5,690 lines, 147 links resolving, 0 broken, 0 over the 300-line cap**, working tree empty. Next is **04 · `lib`, `target` and the ambient environment**, whose research is already banked — *do not re-derive*. ✅ **Arrived first and scaffolded `phase-6-modules-build/README.md` with the full 16-row table** (C rows 01–06 linked as they land, D rows 07–16 plain text for lane `8dcc0095`). 🔴 **Rule 1 evidence:** topic 01 **split three times mid-write** (at 311, 324 and 348 lines) going 7 → 8 → 10 → 11 chunks — **nothing trimmed, both halves grew each time**; lane-C spread is **77–293**, not a band under the cap. 🔴 **Finds:** the `_computedOptions` chain means every `module` value outside `commonjs`/the Node family/`preserve` implies **`classic`**, so `"module": "esnext"` alone silently picks a resolver that never searches `node_modules`; **`TS6280` vs `TS6278` are a diagnosis pair** (your config vs the library's `package.json`, and neither message says which); **`TS5095` widens in 7.0.2** to permit `bundler` with `commonjs`; **`"imports"` needs an explicit `rootDir`** or the build compiles against the previous build's output. ⚠️ **Deliberate evidence gap:** Node's error codes are named on the failure-catalogue page but their message text is **not quoted** — the errors page truncated on fetch and a reconstruction is not evidence. Resume: `devbible/progress_typescript_part_c.md`. 🔴 **Re-scoped the same day it was created.** Part C first took the whole of phase 6 (16 topics); weighting the remaining work by tier — Master ≈1,100 lines, Understand ≈600, Know ≈350, measured from work already written — showed phase 6 projecting to **~10,350 lines, 43% of everything left in TypeScript, in one unclaimed lane**, because its three Master rows sit at the front. On the user's instruction (*"go with option 1, split phase 6"*) it was cut after topic 06: **C ≈5,100 lines, D ≈5,250** — a delta of 150 — on a real concept boundary. **C owns the module system and how the compiler sees files:** 01 `module`/`moduleResolution` (Master) · 02 `import type` / `verbatimModuleSyntax` (Master) · 03 Path aliases `paths` (Master) · 04 `lib`, `target` and the ambient environment · 05 `isolatedModules` · 06 File extensions. **Nothing is written and the directory does not exist** — whoever of C or D arrives first scaffolds `README.md` with the **full 16-row table**, after which each lane edits only its own rows. ⚠️ **C and D share that README and the `phase-6-modules-build` row in `progress.js`** — this is devbible's only intra-phase split, so re-read both immediately before every edit. Topics 01–02 own the *general* rule for `module: nodenext` and `verbatimModuleSyntax`; `phase-7-server/01-tsconfig-for-a-node-service/` owns the applied server case — link it, do not restate it. Start at **01 · `module` and `moduleResolution`**. Cursor: `devbible/progress_typescript_split_4way.md`. |
| **TypeScript · Part D** — phase 6 topics **07–16 only** (`docs/typescript/pages/phase-6-modules-build/`) | session `e28ddf99`, 2026-08-17 (took over from `8dcc0095`) | 2026-08-17 | 🔴 **Created 2026-08-17** by the same split that re-scoped Part C. **D owns declarations, packaging and the build:** 07 Authoring `.d.ts` · 08 Typing an untyped dependency · 09 `esModuleInterop` · 10 `skipLibCheck` · 11 Publishing a typed package · 12 Sharing types across a monorepo · 13 Project references and `tsc -b` (Know) · 14 Incremental builds (Know) · 15 `isolatedDeclarations` · 16 Typing non-code imports (Know). **10 topics; 8 written, 2 left.** ⚠️ **The ~5,250-line projection is already wrong** — topics 07–10 have come to **7,703 lines / 34 files** against a projection for the whole lane of 5,250, because every one of them had far more in it than the per-tier average predicted. **Track this lane by topic count, not lines.** ⚠️ **Shares the phase directory, its `README.md` and the `phase-6-modules-build` row in `progress.js` with Part C** — edit rows 07–16 only, never C's 01–06, and re-read before every edit. 🔴 **Three inherited debts, none to be re-derived:** `isolatedDeclarations` (topic 15) already has `TS9021` and `TS9022` quoted in `phase-4-classes-declarations/14-mixins/05-the-cost-in-the-build.md`, which **forward-links to phase 6 — repoint it when topic 15 lands**; `skipLibCheck` (topic 10) has its correctness trade in phase 7 and its *performance* framing in phase 12 (Part B), and `phase-10-strictness/08-suppression-directives/03-the-suppression-tiers.md` already settles that **it is not a suppression mechanism**; `esModuleInterop` (topic 09) is argued in `phase-7-server/01-tsconfig-for-a-node-service/02-the-module-format.md`. ✅ **07 · Authoring `.d.ts` files WRITTEN** (2026-08-17) — **13 chunks / 2,918 lines**, 0 over the 300-line cap, spread 171–284. Drafted flat and **split five times on concept boundaries, never trimmed**: what a `.d.ts` is (the five ambient-context errors as the definition of the file format) · the declaration forms · the three declaration spaces · generated-vs-hand-written · module-or-global · the export forms · `declare module` · declaration-emit failures in two chunks · the handbook's Do's and Don'ts in two · `@internal` · triple-slash references. 🔴 **Three finds read from the compiler rather than recalled:** `declaration`'s own option record gives its default as **"false unless `composite` is set"**, and `declarationMap`/`declarationDir`/`emitDeclarationOnly` each raise **TS5069** requiring `declaration` **or** `composite`; **TS9005/TS9006 come from the JavaScript declaration path only** — `transformDeclarationsForJS` is their sole call site in the installed 5.9.3 build, so seeing them means `allowJs` + `declaration`; and **`stripInternal` detects `@internal` by a raw substring test** (`comment.includes("@internal")` over *all* leading comment ranges, not JSDoc parsing), so a line comment works and a comment merely *mentioning* the word silently strips the declaration. ✅ **08 · Typing an untyped dependency WRITTEN** (2026-08-17) — **6 chunks / 1,682 lines**, 0 over the cap, spread 102–298: reading the symptom · look for types first · the shim · growing and containing it · when the shipped types are wrong · the upstream fix. 🔴 **Three more compiler behaviours read rather than recalled:** **`TS7016` is suppressed when `allowJs` is on OR `noImplicitAny` is off** (`getAllowJSCompilerOption(options) || !getStrictOptionValue(options, "noImplicitAny")`), so enabling `allowJs` silences untyped-module errors and a project with none may simply have turned the question off; **the `@types` name for a scoped package is mangled** — leading `@` dropped, `/` → `__`, so `@babel/core` is `@types/babel__core`, and `--traceResolution` says so via **TS6182**; and **`compilerOptions.types` REPLACES the default inclusion**, which the checker reveals by picking **TS2591** over **TS2580** (*"…and then add 'node' to the types field"*) from a hardcoded name→suggestion table. Also draws the precise `skipLibCheck` line — it helps when *their* `.d.ts` fails to compile internally, and does nothing when their types are wrong *about* the API — and establishes that **a shim must be a SCRIPT file while an augmentation must be a MODULE file**, the asymmetry behind most `TS2664`. ✅ **09 · `esModuleInterop` and default imports WRITTEN** (2026-08-17) — **5 chunks / 1,251 lines**, 0 over the cap. 🔴 **Four more compiler behaviours read rather than recalled:** the two flags **differ by exactly one line in their option records** — `allowSyntheticDefaultImports` has `affectsSemanticDiagnostics`, `esModuleInterop` **also has `affectsEmit`** — so one is a *permission* (a promise something else does the interop, hence green-build-`undefined`) and the other is *behaviour*; **`esModuleInterop`'s COMPUTED default is `true`** for `module` = `node16`/`node18`/`node20`/`nodenext`/`preserve` despite its static description saying `false`, and **`allowSyntheticDefaultImports` computes `true` from `moduleResolution: bundler`** — so many "should we enable this?" debates are about a project that already has it; **`TS1259` names a different flag depending on `module`** (`moduleKind >= ES2015 ? "allowSyntheticDefaultImports" : "esModuleInterop"`), which is why the ecosystem's advice on this is permanently contradictory; and **`__importStar` builds a NEW object and copies own keys** unless `__esModule` is present — quoted verbatim from the compiler's helper table — so a CommonJS namespace import is *not* the package's `module.exports` and fails an identity check against it. Also that the flag **forbids as well as allows**: it makes `import * as x from 'cjs'; x()` a `TS2497`, which is why migrations feel contradictory. ✅ **10 · `skipLibCheck` WRITTEN** (2026-08-17) — **9 files / 2,055 lines**, 0 over the cap, spread 219–257, **0 broken links in the topic**. The flag everyone sets and nobody can describe, and reading the compiler turned it into a much larger topic than the one-line docs summary suggests. 🔴 **Seven behaviours read from the 5.9.3 build, none of them documented:** the gate is literally `options.skipLibCheck && sourceFile.isDeclarationFile` with **no path test**, so it is *not* "skip `node_modules`" — your `globals.d.ts`, your shims and **the `dist/*.d.ts` your own build emits** are skipped identically, which is the whole library-author trap; 🔴🔴 **the best find — `checkSourceFileWorker` early-returns BEFORE `checkGrammarSourceFile`, so the ambient-context rules that DEFINE the `.d.ts` file format (`TS1036`, `TS1038`, `TS1039`, `TS1046`, `TS1183` — the five topic 07 chunk 01 built its account on) are never computed** for a declaration file, meaning a `.d.ts` containing a statement or an initialiser is accepted in silence; **`getSyntacticDiagnosticsForFile` is UNGATED**, so the real boundary is parser-versus-checker rather than "syntax versus semantics" — `TS1046` reads like a syntax error and is still skipped; **`getDeclarationDiagnosticsForFile` returns early for declaration files**, so the flag has nothing to do with the `TS4053` emit family; **`skipDefaultLibCheck` tests `sourceFile.hasNoDefaultLib`, set by the `/// <reference no-default-lib="true"/>` pragma** — not "files shipped with TypeScript" as its own description says — and its CLI form is deprecated via `TS6160`; 🔴 **`tsc --init` emits `skipLibCheck: true` under a header literally called `Recommended_Options`, next to `strict`**, and a `jsconfig.json` implies it from its filename, which is the honest answer to "why does every project have this?"; and **`copyDeclarationFileDiagnostics` requires `!skipLibCheck === !oldSkipLibCheck`**, which is what `affectsBuildInfo` buys — flipping the flag invalidates exactly the declaration-file slice of `.tsbuildinfo`, so two configs sharing one `tsBuildInfoFile` thrash forever. 📌 **A documented-default conflict was found and resolved rather than picked:** the TSConfig reference says *"Default: `true` (as of TypeScript 5.4)"* while the option record says `false` and the predicate is falsy when the option is absent — **the compiler default is `false`; what changed is the config `--init` generates** — and the page says plainly that the 5.4 release notes were checked and do **not** mention the flag, so the attribution could not be confirmed. **No timing figure appears anywhere in the topic**, because none was measured; the performance framing stays with phase 12 and the correctness trade with phase 7, both linked rather than restated. ✅ **11 · Publishing a typed package WRITTEN** (2026-08-17) — **10 files / 2,380 lines**, 0 over the cap, **0 broken links**. Its one-sentence version is quoted verbatim from `arethetypeswrong`: 🔴 *"A golden rule of declaration files is that if they represent a module…they must represent **exactly** one JavaScript file"*, and *"They **especially** cannot represent JavaScript files of two different module formats."* Everything else in the topic is bookkeeping on that sentence — because a `.d.ts` does not merely describe an API, it **asserts a module format** (decided by its extension and the nearest `"type"`), so the canonical broken dual package is a single top-level `types` alongside `import`/`require`, and the fix is `types` nested **inside each branch**. 🔴 **Nine load-bearing findings, all from primary sources:** *"TypeScript always matches the `types` and `default` conditions"* and *"the `types` condition should be listed first"* — anything after `default` is unreachable, and the failure is **silent** because extension substitution often masks it; 🔴 *"The mere presence of an `exports` field prevents resolving any subpaths not explicitly listed"*, i.e. **adding `exports` is a breaking change**; **`TS6278` vs `TS6280` are a diagnosis pair and neither message says whose fault it is** — 6280 means the consumer is on `node10`, 6278 means the package's own map does not reach its types; **a matched `types` condition returns directly with no extension substitution**, so a path excluded by `files` is indistinguishable from a typo; 🔴 **`export default` describes `module.exports.default`** — `module.exports = x` must be declared `export = x`, and `esModuleInterop` hides the mismatch in exactly the configuration authors test in; **Node runs `cjs-module-lexer` rather than executing the module**, so `exports.a =` supports a named import and `module.exports = { a }` does not, with the never-running `0 && (module.exports = { a });` as the documented hint; **`typesVersions` matches on the compiler's `major.minor`** and 🔴 **`TS6209` — an unparseable range key — is skipped silently with nothing failing**, visible only under `--traceResolution`; the `types@{selector}` condition is argued as the better mechanism because it can version a dual package's two sides independently; and 🔴 **`publint`'s `NESTED_PACKAGE_JSON_FIELD_IGNORED`** catches the trap in the standard dual-build fix — a nested `dist/*/package.json` may carry **only** `"type"`, and `exports` there does nothing. 📌 **The topic's spine is that a green build of your own package proves almost nothing** — it never performs a consumer's import — and `arethetypeswrong`'s *"Consumers without `skipLibCheck` enabled will see a TypeScript error reported on declaration files from the package in their node_modules"* is **[topic 10]'s argument arriving from the consumer's side**. ⚠️ **Rule 1 was exercised:** chunk 07 drafted at **307 lines and was SPLIT on a concept boundary** into the problem catalogue (280) and the tooling (253) — **nothing trimmed, both halves grew**. ✅ **12 · Sharing types across a monorepo WRITTEN** (2026-08-17) — **7 files / 1,455 lines**, 0 over the cap, **0 broken links**. 🔴 **Its organising find is undocumented in prose and settles the whole topic:** the option `disableSourceOfProjectReferenceRedirect` is described as *"**Disable preferring source files instead of declaration files** when referencing composite projects"* — so with project references **TypeScript prefers SOURCE by default**, and a team running `tsc -b` in the belief that it checks against `.d.ts` is wrong unless they set that flag. 🔴 **And the check and the emit deliberately take different routes:** the check uses the source, while the emitter substitutes the referenced project's `outputDts`, so the artefact you emit still describes the published boundary — two halves that silently disagree the moment `dist` is stale. It is the same `isSourceOfProjectReferenceRedirect` that appears as a clause in **topic 10**'s skip predicate, which is why a referenced project's files are not checked by its consumer. **Five more behaviours read from the 5.9.3 build:** `composite`'s own description says *"**constraints**"* and the compiler enforces two by diagnostic — **`TS6304`** *"Composite projects may not disable declaration emit"* (commonly triggered by a `noEmit: true` inherited from an app-shaped base config) and **`TS6307`** *"File '{0}' is not listed within the file list of project '{1}'"*, which is what makes build order computable; 🔴 **`TS6305`'s ABSENCE is more dangerous than its presence** — a plain `tsc -p` resolving through `node_modules` to a stale `dist` reports **nothing at all** and checks happily against old declarations; **`rootDirs` is about module *resolution*, not output structure**, so it is the standard wrong turn for `TS6059`; **`TS6202`** is treated as a design finding rather than a config bug, and `import type` does not rescue a cycle because the reference graph is a **build-order** graph; and **two options exist purely to make the editor deliberately less informed than the build** — `disableSolutionSearching` (*"Opt a project out of multi-project reference checking when editing"*) and `disableReferencedProjectLoad` — so editor-versus-build divergence can be a **configured outcome** rather than a bug. 📌 **The fast diagnosis the topic gives away:** *consistent* divergence means different `tsconfig.json` files, *intermittent* means staleness — that one distinction saves most of the time this problem costs. Recommends project references throughout with the redirect left on, `declarationMap: true` everywhere (without it go-to-definition lands in generated files, which is what drives teams back to source imports), and one CI job that emits declarations and runs topic 11's checks — plus a six-step migration ordered so the repo stays buildable, with declarations **before** `composite` because otherwise two unrelated error classes arrive together. ⚠️ Deliberately does **not** restate lane C's topic 03 chunk 05 (the `paths`-to-`src` anti-pattern) or pre-empt topic 13, and **claims no build-time figure anywhere**, because none was measured. ✅ **13 · Project references and `tsc -b` WRITTEN** (2026-08-17) — **5 files / 963 lines**, 0 over the cap, **0 broken links**. 🔴🔴 **Its headline find is a default nobody expects, traced to source:** **`stopBuildOnErrors` defaults to `false`**, so **`tsc -b` keeps building downstream projects past an upstream failure** — and the four diagnostics that imply otherwise (**TS6362 · 6363 · 6382 · 6383**, *"Skipping build of project '{0}' because its dependency '{1}' has errors"*) **only appear when the flag is ON**, which is exactly why the behaviour reads as a bug. The flag is consumed in precisely two places — `getUpToDateStatus`, where it decides whether an upstream failure marks a project `UpstreamBlocked`, and `queueReferencingProjects` (`if (state.options.stopBuildOnErrors && buildResult & AnyErrors) return;`). ⚠️ **And the worse case combines with topic 12:** if the failed dependency's `dist` is stale but *valid*, the dependent compiles **cleanly** against the old declarations, so the log reads *"only the base package is broken"* when the dependent was never checked against the code you wrote. 🔴 **Second find — the optimisation that justifies references at all:** **`TS6354`** *"Project '{0}' is up to date with `.d.ts` files from its dependencies"* — a dependency rebuilt, its emitted declarations did not change, so the **entire downstream graph is skipped**; which turns stable declarations into a *build-time* argument for annotating a package's public surface, and is the one thing a task runner cannot do (they cache at whole-package granularity). **Five more read from the 5.9.3 build, `TS6354`/`TS6388` also confirmed in the 7.0.2 binary:** **`TS6381`** *"output for it was generated with version '{1}' that differs with current version '{2}'"* explains the full rebuild after every TypeScript upgrade; **`TS6379`** *"Composite projects may not disable incremental compilation"* — so composite ⇒ incremental ⇒ **every** package writes a `.tsbuildinfo`; **`TS6377`** *"Cannot write file '{0}' because it will overwrite '.tsbuildinfo' file generated by referenced project '{1}'"* — 🔴 the compiler catching, by name and at build time, the exact collision **topic 10 chunk 07** predicted; **`TS6369`** *"Option '--build' must be the first command line argument"* has a real cause (it selects the `buildOpts` parser, so `tsc --noEmit -b` is rejected rather than reinterpreted); and the **`--build` flag set is exactly five** — `--verbose`, `--dry`, `--force`, `--clean`, `--stopBuildOnErrors` — read from `optionsForBuild`. 📌 **`--force` in a CI script is named a defect marker**, not a fix: it means someone hid a staleness problem and now pays full build cost every run; `--clean` once is the actual response. ⚠️ **Chunk 04 answers the syllabus row's other half — *when a monorepo does NOT need references*** — with the three arrangements that need none (one project over all packages when `tsc` only type-checks; workspaces plus a task runner; a bundler-owned build) and the four situations where they earn their place, the strongest being `TS6354` skipping on a deep graph and the most decisive being *"we publish from this repo"*, since declaration emit is then already paid for. **Also repointed all six forward references** to topic 13 across topics 07, 10 and 12. ✅ **14 · Incremental builds WRITTEN** (2026-08-17) — **4 files / 705 lines**, 0 over the cap, **0 broken links**. Topic 13 owned the up-to-date *decision*; this one owns the **file**, read from the compiler's own buildinfo serialiser. 🔴 **Its organising find:** a `.tsbuildinfo` stores **two different hashes per file** — `version` over the file's **text** and `signature` over its **emitted declaration** — and the gap between them is the entire engine of incremental compilation: a function-body edit changes the first and not the second, so the file recompiles and **nothing that depends on it does**. That makes an explicit return type on a module's public surface a **build-time optimisation**, not a style preference, and it is the file-level twin of topic 13's `TS6354` one scale up. 🔴 **Second find — the precise, greppable definition of what invalidates the cache:** `toIncrementalBuildInfoCompilerOptions` walks the options and keeps only those whose record carries **`affectsBuildInfo: true`**, sorted by name — so that one flag decides invalidation for every option, anyone can check any flag with a single grep, and **reordering a `tsconfig.json` invalidates nothing**. 🔴 **Third:** `semanticDiagnosticsPerFile` and `emitDiagnosticsPerFile` are persisted, so **the errors themselves are cached** — an incremental run can report a diagnostic it never recomputed, which is why a stale buildinfo can report errors for code that has already been fixed. **Also settled:** `incremental`'s documented default is literally *"false unless `composite` is set"*; `latestChangedDtsFile` is tracked **only** for composite projects, which is what powers `TS6354`; and `tsBuildInfoFile` carries **both** `affectsEmit` and `affectsBuildInfo`, so moving it is itself a recorded change. 📌 **Chunk 03 answers the syllabus row's third part** — caching in CI — with one rule (cache the buildinfo **and** its outputs together, key on the lockfile and the configs, **never on source content**, because a cache that only hits when nothing changed never helps) and the timestamp trap **in both directions**: a checkout makes sources look newer so everything rebuilds (wasteful), a restore makes outputs look newer so the build **skips work it should have done** (silent, and the dangerous one). ⚠️ **This pass also repointed 27 stale forward references** across lane D's topics 07–10 whose targets now exist — only 15 and 16 remain as plain text, correctly. Next: **15 · `isolatedDeclarations`** (Know). Cursor: `devbible/progress_typescript_part_d.md`. |
| **TypeScript · Part B** — 🔴 **RE-SCOPED 2026-08-15 to phases 10 and 12 only** (`docs/typescript/pages/phase-{10,12}-*/`), plus phase 7's five written Master topics which stay | session `c01e37bb`, 2026-08-17 (took over from `ede9cd9f`) | 2026-08-17 | 🔴 **Active.** Re-scoped on the user's instruction — *"I just need phase 10 and phase 12 apart from rest drop, if they are already written let it be and if not written yet drop those"*. ⛔ **Dropped:** phases **8** (14), **9** (15) and **11** (12) in full — nothing had been written for any of them — plus phase 7's **ten unwritten Understand rows**. ✅ **Kept, and nothing deleted:** phase 7 is **COMPLETE at 5/5**, its five Master topics written this session — **01** `tsconfig.json` for a Node 24 service (6 files, 1,290 lines) · **02** Shipping to production (3, 583) · **03** Typing `process.env` (4, 820) · **04** `catch (e: unknown)` (5, 1,045) · **05** Typed Express handlers (3, 615) — **21 files, 4,353 lines, 0 over the 300-line cap**, build-verified with `[SUCCESS]` and zero warnings or broken links site-wide. 🚧 **Phase 10 · Strictness and correctness at 5/13** — topics 01–04 (the four Master rows) written 2026-08-15, and **05 · `exactOptionalPropertyTypes`** written 2026-08-17 as **4 chunks / 1,178 lines**, 0 over the 300-line cap. That topic is the worked example of the file-size rule as the user restated it: an *Understand* row the earlier cursor had pencilled in as "one file", written out in full first — the ten runtime operations that distinguish `{}` from `{k: undefined}`, the write-not-read asymmetry, the JSON-boundary three-state model, the object-spread soundness hole, and the `Partial<T>` migration cost — **then split on concept boundaries**, never trimmed. Evidence came from the compiler's own tables: the option record (`defaultValueDescription: false`, **no `strictFlag`**) and 🔴 **three distinct diagnostics, not one** — `TS2375` / `TS2379` / `TS2412`, whose singular-vs-plural wording tells you which shape of assignment failed — all confirmed verbatim in **both** the 5.9.3 table and the 7.0.2 native binary. ✅ **06 · The other correctness flags** followed the same day as **5 chunks / 1,204 lines** — a "grouped" row deliberately not written as six paragraphs of even length: `noImplicitOverride` gets a chunk of its own (five diagnostics, and the rename-in-the-base bug nothing else catches), index-signature access another, the control-flow pair a third, the unused-code pair a fourth, and the costed adoption decision a fifth. Two more finds straight out of the option records: 🔴 **`noFallthroughCasesInSwitch` is a *binder* diagnostic** (`affectsBindDiagnostics`, alone in the group) so it is purely syntactic and works on files the checker has given up on; and 🔴 **`allowUnreachableCode`/`allowUnusedLabels` default to `void 0` — `undefined`, a real third "suggestion" state** — so unreachable code is already being reported in every editor today and is invisible to CI, with `allow*` polarity inverted. ✅ **07 · Where TypeScript is unsound by design** (5 chunks / 1,181 lines) paid off all three inherited debts at once — method bivariance, *"narrows the gap rather than closing it"*, and object spread's incorrect result type. It reframes the usual flat list of six holes by sorting them by **what you can do about them**: three you write (`any`, `as`, `!`) and so can count in a diff, two closable by flags this phase already sold you, `Object.keys` returning `string[]` as 🔴 **the honest answer rather than an oversight** (a `keyof T` return would be a *larger* unsoundness), and 🔴 **only two that are neither opt-in nor closable — mutation through an alias and method parameter bivariance**, both demonstrated with no assertion and both with free mitigations nobody applies. ✅ **08 · Suppression directives** (4 chunks / 862 lines): 🔴 **`TS2578` is the only diagnostic in TypeScript that reports a problem which has *stopped* existing**, which makes the usual objection — "it will break the build later" — the feature being purchased; and the directives turn out to be the middle of a **seven-tier ladder ordered by blast radius**, whose tier 6 is 🔴 **`suppressExcessPropertyErrors` / `suppressImplicitAnyIndexErrors`, project-wide suppression that no directive grep will ever find**, filed by the compiler under `Backwards_Compatibility`. ✅ **09 · Excess property checks vs assignability** (4 chunks / 923 lines): freshness as the governing concept, 🔴 **two codes not one** (`TS2561` runs a similarity check and *names the property you meant*), the **seven ways freshness is lost** — the most common being the routine refactor of extracting a literal into a variable — and the **extra / nothing / missing** axis across the three overlapping rules, which together leave one reachable gap: a typo in an optional property, on a target with one required property, passed through a variable, is caught by nothing. Weak-type detection is **cited from phase 1's sandbox-proven measurement**, not re-derived. ✅ **10 · The error codes you will actually meet** (**14 chunks / 3,808 lines**, 0 over the 300-line cap, spread 244–294) — the syllabus asked what nine codes *really* mean, and the answer came from **reading the checker functions that choose between messages**, not from the docs. 🔴 **The organising find: the generic message is always a ladder's LAST rung.** Three independent ladders — `reportNonexistentProperty`, `getCannotFindNameDiagnosticForName` and the element-access branch — each try five or six *specific* diagnoses first, so a bare `TS2339` is a **stronger** statement than a specific one: a static member, a missing `await` (`TS2773`, attached as related information after the compiler hypothetically awaited both sides), a `lib` gap (`TS2550`), a recognisable typo and a missing `dom` lib have all been ruled out — which makes *is this type correct?* the right next question rather than *where did the property go?*. Other finds, all disk-measured: 🔴 **the whole code space counted — 2,073 diagnostics in 13 ranges, of which 242 (90xxx/95xxx) are quick-fix MENU LABELS**, and the `Warning` category is used by nothing, so **TypeScript has no warning level**, which is why the suppression directives carry all that weight; 🔴 **eight 7xxx errors have Suggestion twins at 7043–7050**, so `noImplicitAny: false` *demotes* a finding to grey rather than removing it — the same three-state mechanism topic 06 found on `allowUnreachableCode`, with a third instance in `allowUmdGlobalAccess`; 🔴 **`levenshteinWithMax` is WEIGHTED, not standard Levenshtein** (substitution **2**, case-only **0.1**, insert/delete 1), which puts the suggestion cliff at **five characters** — `obj.nmae` for `name` gets no suggestion at all and `lenght` for `length` does — and yields the corpus's only quantified argument for longer identifiers; 🔴 **`TS7053` is not a type error** but the entire reporting branch sitting inside `if (noImplicitAny)`, and it is a *wrapper* whose inner code (`TS2339` vs `TS7054`) is the fork in the fix; 🔴 **`TS18048`/`TS2532` are one check reported twice**, selected on `isEntityNameExpression(node)` and a name under 100 characters, so the anonymous form *means* the expression has no name and therefore cannot be narrowed in place — which is the mechanism behind topic 04's extract-to-a-`const` advice, and shows where it fails (invocation has no named form); 🔴 **`TS2589`'s limits are depth `100` and count `5,000,000`, reset per EXPRESSION**, so *the project got too big* is never the cause, splitting a chain into named intermediates is a real fix, and *it works in the playground* means a different surrounding expression rather than a different compiler; and the compiler's **hardcoded list of globals it prints actual npm install commands for**, including `Bun`, each existing under two codes because an explicit `types` array is an **allowlist** rather than an addition. 🔴 **Carries a CORRECTION to three pages in this phase** (03, 08, 09), all repointed in the same pass: `suppressExcessPropertyErrors` and `suppressImplicitAnyIndexErrors` are read by **nothing** in the 5.9.3 checker and are **absent from the 7.0.2 binary entirely** — `TS5102` from 5.5, unsilenceable because `canBeSilenced` is only computed when the removal version is still in the future, then `TS5023` in 7.0. ⚠️ **And the methodological limit it exposed: both option records still advertise `affectsSemanticDiagnostics: true` four minor versions after the checker stopped reading them, so the option table is authoritative about defaults and categories but NOT about whether a flag still functions** — grep an option's *consumers*, not its declaration. ✅ **11 · typescript-eslint type-aware rules** (**10 chunks + README = 11 files, 2,509 lines**, 0 over the 300-line cap, spread 202–293) — planned as seven chunks and finished at ten, because two of its items were several arguments each. 🔴 **`strict-boolean-expressions` reduces to one test: truthiness is safe exactly when the non-nullish part of the type holds none of `ToBoolean`'s falsy values** — which explains the whole option matrix, including why `allowNullableObject` is the single default of `true` (objects have no falsy members, so `if (user)` *is* `user != null`). 🔴 **And its defaults draw the line at nullability, not falsiness**: `allowString` and `allowNumber` both default to `true`, so every bug the rule is famous for — the empty username, the zero-valued option, the `0` rendered in JSX — is **permitted out of the box**. The default configuration is not the strict configuration. 🔴 **Four of the eight fixes change runtime behaviour and one makes things worse** — `n !== 0` is `true` for `NaN`, so the mechanical rewrite admits the parse failure truthiness happened to reject; `-0` is not a second case, making `NaN` the only value in the language where the two spellings disagree. 🔴 **The `no-unsafe-*` rules are TEN, not nine, and the prefix is not a family**: five track `any` and are one flow (assignment · member-access · call · argument · return — disabling one relocates the leak rather than removing it), while five are unrelated checks. Two diagnostics were read from the 5.9.3 table to anchor them — **`TS2395` is the only merged-declaration code and it checks *export consistency*, so the class↔interface merge that type-checks and throws has no compiler check at all**, and **`TS2356` lists `'any'` FIRST among the operands arithmetic accepts**, which is exactly the hole `no-unsafe-unary-minus` closes. 📌 Pays topic 03's debt with the mechanism: **the compiler cannot report `any` usage**, because using an `any` is the type's documented meaning — and the one source TypeScript did close (`catch` → `unknown`, 4.4) needed a **dedicated flag**, which is the evidence there was no general mechanism. 🔴 On cost: the two type-checks **cannot be merged**, so the levers are scope and parallelism — and *"lint only the changed files"* does not work, because the build is per **project**, not per file. The topic's closing principle is **flags before rules**: every compiler flag enabled first *reduces* the lint work. ⚠️ **Rule 1 worked in the open here** — chunk 06's draft hit **329 lines and was split, not trimmed**, and the two halves came to **459 together**; the 130 lines the split added were the most useful material on the page. ✅ **12 · Assertion discipline** (**5 chunks + README = 6 files, 1,172 lines**, 0 over the cap). 🔴 **The measured spine: the compiler has NO diagnostic that questions a `!`** — searching the 5.9.3 table for non-null assertions returns one code, `TS8013`, *"Non-null assertions can only be used in TypeScript files"*, which is about **file extensions**. So the three forms rank by oversight — `satisfies` is **verified** (`TS1360`), `as` is **accepted unless the types barely overlap** (`TS2352`), and `!` is **never questioned** — and ⚠️ **that ordering is the reverse of the effort to type them**, which is why this has to be policy rather than preference. 🔴 **`TS2352`'s floor is "sufficient overlap", NOT correctness**, so an accepted `as` means the compiler declined to argue — and **most wrong assertions are between related types, exactly the region it does not police**; the message also **quotes its own escape hatch**, so `x as unknown as T` in a diff means someone was told the types are unrelated and proceeded. 🔴 **`!` cannot be grepped for** (it is also negation, `!=`, `!==`, `!!`), so every "count the assertions" script greps ` as ` and is blind to the larger population — and the two shapes that **contain their own refutation**, `user?.profile!.email` and `x! ?? fallback`, need no judgement to review. **`TS1255`/`TS1263`/`TS1264` are every rule the compiler enforces around `!:` and all three are about FORM, not truth.** 🔴 **`as any` is an exit, not a stronger assertion** — an assertion that cannot be wrong is the *absence* of a type — and the inversion that matters: a bare `as any` is the **loud** spelling the `no-unsafe-*` rules report, while `as any as T` and `as unknown as T` resolve to `T` and are **silent**, so **banning `as any` alone moves the exits to a tier the tooling cannot see**. 📌 Also: **5.5's inferred type predicates are the one place a claim previously taken on trust became one the compiler works out itself** (an argument for *removing* explicit `v is T` from simple guards), **`as const` must be excluded from any metric** since `TS1355` restricts it to literals and it cannot be a lie, and the phase's metric is **assertions added per error fixed**, not the raw count. ⚠️ **A duplication trap was caught on arrival:** phase 2 already owns the *mechanics* in four topics (`08-as-assertions`, `09-assertion-functions`, `10-satisfies`, `13-non-null-assertion`), so the topic README carries an explicit **"what this topic does not repeat"** table — **phase 2 owns the mechanism, phase 10 owns the discipline.** 🏁 **13 · Designing APIs `unknown`-first** (Know, 223 lines) **CLOSES PHASE 10 at 13/13.** Its argument is the answer to topic 12: 🔴 **a parameter type is a promise the compiler cannot keep at a boundary** — inside the program `body: RegisterRequest` is enforced, but at the edge the value's type came from an assertion, so the annotation *documents an assumption* instead of checking one. **Take `unknown` and return the type**, making it an **output of validation rather than an asserted input**; four shapes are costed (parse · result union · predicate · assertion signature), and 🔴 **`f<T>(x: T)` is named as the mistake this gets confused with** — an unconstrained generic is inferred *from the argument*, so it accepts anything and checks nothing, which makes it **more misleading than `unknown`** because it looks typed. The discipline is a **ring**: `unknown` at the boundary, real types inside, and **validating the same value twice in one request is a design smell rather than extra safety**. 🚧 **Phase 12 · Tooling, performance and testing OPEN at 1/15** — directory created, no `_category_.json` on the phase dir. ✅ **01 · Type checking in CI** (Master, **5 chunks + README = 6 files, 1,048 lines**, 0 over the cap). 🔴 **A transpiler cannot type-check, and not because the feature is missing** — it processes one file at a time while checking needs the whole program, so **the speed and the blindness are the same design decision**; replacing `tsc` with a faster tool is not the same check faster but a different, smaller job, and `isolatedModules` is TypeScript agreeing to the transpiler's terms (it makes the fast tool **safe**, not **thorough**). 🔴 **A green `tsc --noEmit` is compatible with an entire directory never having been looked at** — and ⚠️ **`exclude` is not a firewall**: it filters what `include` *finds*, so an excluded file is still compiled if anything reachable imports it. Four introspection flags are quoted from the 5.9.3 option table, led by **`--explainFiles`** (*"print names of files and the reason they are part of the compilation"* — the **reason** is the answer you need). **Test the gate by breaking something on purpose**, since an untested gate is indistinguishable from one that runs nothing. 🔴 **A type error is the classic SEMANTIC merge conflict** — one PR renames a field, another adds a use of the old name in a new file, git merges cleanly, `main` breaks and **neither branch was wrong**; the symptom is `main` breaking weekly with no PR at fault, and it gets misdiagnosed as CI flakiness. Pre-commit is mechanically the wrong place (a whole-program check cannot be scoped to staged files, and `--no-verify` makes it optional for the person it checks), and the type check goes **before** the tests because a type error makes test failures uninterpretable. 🔴 **Speed is a correctness concern** — a slow gate gets narrowed, moved or made advisory, so every *"make it faster by checking less"* change is a **coverage** change arriving in a PR titled *speed up CI*. And 🔴 **advisory is not a halfway house, it is the failure mode**: a non-blocking red is indistinguishable from a green after two weeks and **has no failure indicator**, so the answer to a 400-error codebase is a **required** gate against a baseline, or full strength over the clean part. 🎯 **Remaining: 14 topics**, all in phase 12. |
| **CSS** — all of `docs/css/`               | session `6f020813` | 2026-08-14 | ✅ **COMPLETE — RELEASED, free to pick up.** Syllabus re-scoped to the critical path (119 → 74 topics; SCSS is usage-only, the architecture phase was cut). **All 11 phases written — 74 topics, 81 pages, 17,359 lines, 0 files over 300 lines.** Flexbox and Grid at full Master depth on the user's instruction. Doc-validated under the no-new-sandboxes rule with sources named per page; **no fabricated console blocks.**                                                                                     |
| **Node.js** — all of `docs/nodejs/`        | session `8679dc8c` | 2026-08-14 | ✅ **COMPLETE — audited, free to pick up.** All 13 phases written, **all 248 syllabus topics covered** (231 pages; six phases merge pairs of rows and each says so in a Coverage table). **232 files, 232 carrying `> Verified:`, 0 files over 300 lines, 0 broken links in a clean rebuild.**                                                                                                                                                                                                                       |
| **Express** — all of `docs/expressjs/`     | session `b7f137c4` (continues `ffadd057`) | 2026-08-14 | ✅ **Master-tier depth pass COMPLETE — 28 of 28**, plus **new Phase 5 topic 08 "Every error that arrives"** (the full catalogue: driver, network, library and programmer errors) written on request. Locked to this session by *"Lock it in express js"*; the claim moved rather than a second Express claim being opened. **183 files, 30,731 lines**, 11 phases, **115 of 115 topics**, every page carrying `> Verified:`, a tier badge, Gotchas, a Trade-off and Interview questions; Coverage tables on all 11 phase READMEs; **0 files over the 300-line cap** and **0 broken links** in a clean isolated rebuild. The depth pass fixed a flat tier curve — 28 Master topics had been 63–200 lines and none chunked; they now run 2–5 chunks each, 3,829 → 21,190 lines. **Parked at the user's request (written, complete, not on the reading path): Phase 9/01 trust proxy and Phase 8/07 resource ownership.** Additive and **nothing was run** — new material validated against the Express 5 docs, PostgreSQL/MongoDB error-code references, Node docs, MDN and RFCs 9110/6749/6750/6585/7519/7239, each named in the page's `> Verified:` line. |
| **MongoDB** — all of `docs/mongodb/`       | session `05921047` | 2026-08-14 | 🔴 **CLAIMED and active** — written in worktree `devbible-mongodb`, branch `mongodb-pages`. **Phases 0–5 COMPLETE — 34/82.** Next: Phase 6 · The aggregation pipeline. Syllabus **cut to the critical path: 204 → 82 topics** (Master tier only, capped at 6 per phase). **Phase 0 COMPLETE (5/5)**; phase 1 not started. The session stopped without writing `pages/README.md` or committing two phase-0 pages — repaired by session `632ebd35`, which did **not** take the claim. Next: **Phase 1 · Documents, BSON types and `_id` (6 topics)**. Documentation-validated against the MongoDB Manual under the no-new-sandboxes rule. |
| **Redis · chunk A** — phases **0, 1, 2, 3** (how Redis works + strings) | session `3bb1face` | 2026-08-17 | 🔴 **Chunk A of a THREE-way split (2026-08-17)** — *"we need to split this redis into 3 parts"*. **23 topics.** The execution model, the keyspace and TTL, `redis-cli`, strings/counters/bitmaps. The foundation the other two chunks cite. 🚧 **1 of 23 written** — topic 01 · What Redis is (4 chunks + index, largest 285 lines, 0 over the cap). Next: **Phase 0 · 02 · Single-threaded command execution**. Cursor: `devbible/progress_redis_split_3way.md`. |
| **Redis · chunk B** — phases **4, 5, 6** (collections, streams, the Node client) | — unclaimed | 2026-08-17 | 🔴 **Chunk B of a THREE-way split (2026-08-17).** **21 topics.** Hashes/lists/sets/sorted sets, sorted sets as tools, streams and consumer groups, then `node-redis` vs `ioredis`, injection, reconnection, pipelining vs `MULTI`. Start: **Phase 4 · 01 · Hashes**. Cursor: `devbible/progress_redis_split_3way.md`. |
| **Redis · chunk C** — phases **7, 8, 9, 10** (caching, the patterns, production) | — unclaimed | 2026-08-17 | 🔴 **Chunk C of a THREE-way split (2026-08-17).** **30 topics — the heaviest, and the one the rest of the bible points at.** Cache-aside and invalidation, sessions/rate limits/locks/queues, memory and eviction, replication and cluster. **92 published pages mention Redis** (6 are real links); these phases pay those promises. Start: **Phase 7 · 01 · Cache-aside**. Cursor: `devbible/progress_redis_split_3way.md`. |
| **Frontend toolchain** — 12 **new** folders: `docs/storybook/` `docs/vite/` `docs/webpack/` `docs/babel/` `docs/eslint-oxlint/` `docs/jest-rtl/` `docs/playwright/` `docs/redux-toolkit/` `docs/tanstack-query/` `docs/framer-motion/` `docs/web-vitals-performance/` `docs/frontend-architecture/` | session `0fe4e7e0` | 2026-08-14 | 🔴 **Active — corpus moved in.** **204 pages** brought over from the separate `frontend-bible` repo, in their original section structure, on 2026-08-14. Work happens in worktree `devbible-frontend`, branch **`frontend-merge`**. **This lane touches no existing technology** — React, JavaScript, TypeScript, CSS and Git belong to other live sessions and are out of scope here, as are Next.js and every backup directory in the source repo. **Status: moved, not validated.** The imported pages have **no `> Verified:` line, no tier badge and no Interview section**, and 12 cross-references to non-imported technologies were de-linked during the move. **Storybook is the exception** — phases 0–3 (23 topics, 5,488 lines) were written to full devbible depth and are build-verified. Next step to be agreed with the user. |
| **Git** — all of `docs/git/`               | session `45e775dc` | 2026-08-14 | ✅ **COMPLETE — released, free to pick up.** Was 🔴 locked (*"There was git course yet to complete the explanations can you pick it up ? and lock it in ?"*). Syllabus complete (13 phases, **191 topics**, 4 parts, 55 Master). **Phase 0 COMPLETE — 14 pages**, sandbox-proven from `sandbox/git-p0/ex1`+`ex2`. 🔴 **RE-SCOPED 2026-08-14 to daily-driver Git — 191 topics cut to 52** (*"I just need to know about the git to work daily tasks not more than that"*). In scope: phases **0, 1, 2, 4, 5** — how Git stores things, the everyday loop, branching and merging, remotes, undo and recover. **Parked:** phase 3 (history in depth), phase 6 (team workflow), and Parts 3 and 4 entirely (repo design, hooks/CI, speed, plumbing, history surgery). ✅ **COMPLETE — all 52 in-scope topics written**, 62 files. Phase 0 how Git stores things (14) · 1 the everyday loop (12) · 2 branching and merging (10) · 4 remotes (8) · 5 undo and recover (8). Phases 3, 6 and 7–12 remain deliberately parked. Phase 0 is sandbox-proven from `sandbox/git-p0/`; phases 1–5 are documentation-validated against git 2.55.0's own manuals with no fabricated console output. Topics 03 onward drop the interview-question block on the user's instruction, keeping gotchas and command tables. ⚠️ Phase 1 onward is **documentation-validated, not sandbox-proven** — the no-new-sandboxes rule closed the `ex3` plan the old memory carried; pages reuse the recorded `ex1`/`ex2` output where it genuinely covers a claim and otherwise carry **no console block**. Resume point: `devbible/progress_git_pages.md`. |
| **Docker & Podman · chunk A** — phases **4, 5** (build strategy + image quality and supply chain) | session `e75b3868` (took over from `2e26b051`) | 2026-08-15 | 🔴 **Chunk A of a FOUR-way split (2026-08-15) — claimed and in progress.** 28 topics. 🏁 **PHASE 4 COMPLETE — 16 of 16** — ✅ **01 · How the layer cache decides** (290 lines), ✅ **02 · Instruction ordering** (256), ✅ **03 · The dependency-install pattern** (255), ✅ **04 · Multi-stage builds** (261) — **the Master tier of phase 4 is complete** — ✅ **05 · `RUN --mount=type=secret`** (234), ✅ **06 · `--target`** (212), ✅ **07 · `COPY --from`** (193), ✅ **08 · BuildKit** (208), ✅ **09 · `RUN --mount=type=cache`** (239), ✅ **10 · `RUN --mount=type=bind`** (191), ✅ **11 · `buildx` and platforms** (231), ✅ **12 · Cache import and export** (201), ✅ **13 · Build args vs runtime env** (209). ✅ **14 · docker vs podman vs buildah** (166). ✅ **15 · The build context** (171). ✅ **16 · Reproducible builds** (196). **16 files, 3,592 lines, 0 over the 300-line cap, 139 internal links resolved against the filesystem (not built).** 🏁 **Phase 5 · 12 of 12 COMPLETE** — ✅ **01 · Where size actually goes** (203), ✅ **02 · The classic mistakes** (268). ✅ **03 · Least privilege** (224). ✅ **04 · Measuring** (211). ✅ **05 · Alpine and musl** (205). ✅ **06 · Distroless and scratch** (247). ✅ **07 · Vulnerability scanning** (202). ✅ **08 · Pinning by digest** (199). ✅ **09 · Supply-chain risk** (200). ✅ **10 · Static binaries** (**chunked** — 504 lines across 2 parts + index; the first draft hit 361 and was split on a concept boundary, linking vs the runtimes). ✅ **11 · SBOMs and provenance** (290). ✅ **12 · Signing and verifying** (274). 🏁 **PHASE 5 COMPLETE 12/12 — and with phase 4 already closed, CHUNK A IS FINISHED, 28 of 28 topics, 0 topics left.** Phase 5 is **14 files / 3,100 lines / 0 over the 300-line cap / 0 broken links**, link-checked against the filesystem (**not built** — no build row claimed). Then phase 5 · 01 · Where size actually goes. They pair on purpose: the cache decides how fast the artefact builds, phase 5 decides what is inside it, and multi-stage is the answer in both. Work on `main`, stage explicit paths, **never `git add -A`**. Resume file: `devbible/progress_docker_split_4way.md`. See the [Docker claim notice](docker/pages/README.md) |
| **Docker & Podman · chunk B** — phases **6, 7** (storage + networking) | session `d0c46f84` | 2026-08-15 | 🔴 **Chunk B of a FOUR-way split (2026-08-15) — 🏁 **CHUNK B IS COMPLETE — 26 of 26 topics, both phases closed (2026-08-15).** 🏁 **PHASE 6 · Storage COMPLETE — 12 of 12**, 2026-08-15: ✅ **01 · The container filesystem is disposable** (272 lines) · ✅ **02 · Volumes, bind mounts and tmpfs** (chunked, 3 files / 501 — the *pre-populate vs obscure* contrast the rest of the phase leans on) · ✅ **03 · `-v` short syntax vs `--mount`** (241) · ✅ **04 · Bind mounts in development** (chunked, 4 files / 725 — the loop, the `node_modules` trap and its four fixes, then `develop.watch`) · ✅ **05 · File ownership and UID mismatch** (chunked, 4 files / 723 — the rootless mapping formula, then seven fixes) · ✅ **06 · Volume lifecycle** (263) · ✅ **07 · SELinux `:z` and `:Z`** (241) · ✅ **08 · `--read-only` plus `tmpfs`** (221) · ✅ **09 · `--userns=keep-id`** (201) · ✅ **10 · Backing up and restoring a volume** (241) · ✅ **11 · Volume drivers and network storage** (231) · ✅ **12 · Bind-mount performance on macOS and Windows** (208) — **20 files, 4,270 lines**. 🚧 **PHASE 7 · Networking — 5 of 14:** ✅ **01 · The default bridge vs a user-defined bridge** (225) · ✅ **02 · Service discovery** (250) · ✅ **03 · `localhost` inside a container is the container** (188) — **the Master tier of phase 7 is complete** — ✅ **04 · Publishing ports** (233) · ✅ **05 · Network drivers** (254 — the six drivers, and that `host` and `none` are modes, not drivers you create) · ✅ **06 · `network create` and friends** (278 — `--internal` as the cheap segmentation flag, `connect` on a RUNNING container, and ⚠️ `podman network rm -f` deleting the containers). · ✅ **07 · Reaching the host from inside** (231 — `host-gateway`, the Docker-Engine-vs-Desktop caveat, and the half everyone forgets: the host service bound to `127.0.0.1`). · ✅ **08 · Rootless networking** (217 — the user-mode TCP/IP stack, and the source-IP failure that never shows up in development). · ✅ **09 · Privileged ports rootless** (206 — it is the HOST port that is refused, so `-p 8080:80` is untouched; the four answers ranked). · ✅ **10 · `--network=host`** (183 — the decision page: Podman's *"full access to abstract Unix domain sockets and to TCP/UDP sockets bound to localhost"* warning, and the five cases where the trade is right). · ✅ **11 · Debugging the network** (218 — the five-step ladder that IS the phase gate, and `--network container:<target>` as the way into an image with no shell). · ✅ **12 · netavark and aardvark-dns** (203 — which component owns which error, and the CNI-vs-netavark auto-detection trap). 🔴 **Correction pass: pages 01 and 02 claimed Podman's DEFAULT network resolves container names — it does not** (*"does not support dns resolution because of backwards compatibility with Docker"*); both corrected, plus two stale *(not written yet)* footers. · ✅ **13 · Custom subnets, IPv6 and the VPN clash** (206 — the built-in `default-address-pools` list, why `network create` can succeed and still break the VPN, and IPv6 being off by default) · ✅ **14 · Overlay networks and multi-host** (146 — it requires Swarm, the three ports, and why Kubernetes or a host mesh is usually the better answer). 🏁 **PHASE 7 IS COMPLETE — 14 of 14 at every tier; chunk B is finished at 26 of 26.** Phase 7: **15 files (14 pages + the index), 3,131 lines, 0 over the 300-line cap**; every internal link resolved against the filesystem — **link-checked, NOT built** (no build and no dev server were run at any point). **0 files over the 300-line cap; every internal link resolved against the filesystem — link-checked, NOT built** (no dev server and no build were run at any point, per the session registry). Work on `main`, stage explicit paths, **never `git add -A`**. Resume file: `devbible/progress_docker_split_4way.md` |
| **Docker & Podman · chunk C** — phases **8, 9** (Compose + the MERN/PERN stack) | session `016J3KVb` (took over from `9219957a`) | 2026-08-15 | 🏁 **CHUNK C OF A FOUR-WAY SPLIT (2026-08-15) — COMPLETE, 31 of 31 topics, free to pick up.** 🏁 **Phase 8 · Compose — 17 of 17:** ✅ **01 · What Compose is** (286 lines) · ✅ **02 · `compose.yaml` and the Compose Specification** (chunked, 3 files / 473 lines) · ✅ **03 · `up`, `down` and the lifecycle** (chunked, 3 files / 503 lines — `down -v` is the command that deletes your development database) · ✅ **04 · The `services` block** (chunked, 3 files / 557 lines — what runs · how it is wired) · ✅ **05 · `depends_on` with `condition: service_healthy`** (271 lines) · ✅ **06 · Healthchecks in Compose** (chunked, 3 files / 518 lines — the keys · checks that are true), ✅ **07 · Networks in Compose** (253 lines) · ✅ **08 · Volumes in Compose** (254 lines), all 2026-08-15. ✅ **09 · The project name** (198 lines). 🏁 **The Master tier of phase 8 is COMPLETE — 02, 03, 04, 05, 06, 08.** ✅ **10 · Environment and interpolation** (225 lines) · ✅ **11 · Override files** (237 lines) · ✅ **12 · `profiles`** (189 lines) · ✅ **13 · `develop.watch`** (270 lines — five actions, not three: `restart` and `sync+exec` arrived in Compose v2.32.0) · ✅ **14 · Day-to-day commands** (**chunked**, 3 files / 531 lines — the first draft hit 342 and was **split on a concept boundary, not trimmed**: reading the stack · getting inside). · ✅ **15 · `podman compose` and `podman-compose`** (227 lines — three programs share the name; the wrapper defaults to `docker-compose`). · ✅ **16 · `include` and `extends`** (250 lines — the two features resolve relative paths from **opposite** places, and `extends` does not import referenced resources). · ✅ **17 · `--scale` and the honest limits** (216 lines). 🏁 **PHASE 8 IS COMPLETE — 17 of 17 at every tier** (Master 6/6 · Understand 7/7 · Know 4/4): **28 files, 5,563 lines, largest 286, 0 over the 300-line cap, 0 unresolved internal links** — link-checked against the filesystem, **NOT built** (the build/dev-server row was never claimed). 🏁 **Phase 9 · The MERN/PERN stack — 14 of 14:** ✅ **01 · Containerising a Node/Express API** (Master, **chunked**, 3 files / 575 lines — the build · the runtime). ✅ **02 · Dev image vs prod image** (225 lines). ✅ **03 · PostgreSQL in a container** (Master, **chunked**, 3 files / 531 lines — the data directory · initialisation and connecting; 🔴 the `PGDATA`/`VOLUME` path **moved in Postgres 18**). ✅ **04 · Waiting for the database** (Master, **chunked**, 3 files / 513 lines) · ✅ **05 · Hot reload inside a container** (Master, **chunked**, 3 files / 474 lines). ✅ **06 · Secrets in dev vs prod** (243 lines). ✅ **07 · The whole stack in one file** (Master, **chunked into SEVEN chunks + index, 8 files / 1,842 lines** — the first draft of the walkthrough hit 438 and was **split on concept boundaries, not trimmed**: the file · the anchor · the wiring · the stateful services · the API and the frontend · the proxy · the boot and proving it; largest 297). ✅ **08 · MongoDB in a container** (**chunked**, 3 files / 445 lines — running one · the replica set; the replica-set requirement behind transactions and change streams, and `--keyFile` implying access control). ✅ **09 · Redis in a container** (269 lines — RDB vs AOF quoted from the persistence docs, and `maxmemory` defaulting to zero so the cgroup kills what eviction should have handled). ✅ **10 · Migrations and seeds** (284 lines — the four places it could go and why only the gated one-shot service is right; seeds behind a profile). ✅ **11 · Debugging Node inside a container** (274 lines — the inspector as unauthenticated RCE, `0.0.0.0` inside the namespace vs `127.0.0.1:` on the host, `SIGUSR1` and `--disable-sigusr1`). ✅ **12 · A React/Vite frontend** (253 lines — `VITE_*` statically replaced at build time, so the API URL is a build problem and `/api` is the answer). ✅ **13 · Nginx in front of the API** (258 lines — `try_files` as the SPA fallback, hop-by-hop `Upgrade` headers and the 60-second WebSocket idle close, and the forwarded-header trust problem). ✅ **14 · Connecting from the host** (216 lines — `exec` as the default, and why a published database port is a socket for the life of the stack). 🏁 **PHASE 9 IS COMPLETE — 14 of 14 at every tier** (Master 5/5 · Understand 7/7 · Know 2/2): **32 files, 6,501 lines, largest 297, 0 over the 300-line cap, 281 internal links resolved against the filesystem** — and ✅ **build-verified**: the registry row was claimed and a full site build came back `[SUCCESS]`, 0 warnings, 0 broken links. 🏁 **AND WITH PHASE 8 ALREADY CLOSED, CHUNK C IS FINISHED — 31 of 31 topics, 0 left.** Phase 9 **is** phase 8 applied — its worked `compose.yaml` is the deliverable phase 8 builds up to, so splitting them would duplicate every healthcheck and volume argument. Work on `main`, stage explicit paths, **never `git add -A`**. Resume file: `devbible/progress_docker_split_4way.md` |
| **Docker & Podman · chunk D** — phases **10, 11, 12** (production + Podman in depth + delivery and CI) | session `8e7b6e12` (took over from `75a196a7`) | 2026-08-16 | 🔴 **Chunk D of a FOUR-way split (2026-08-15) — the heaviest at 44 topics**, and the whole of Part 4. 🚧 **Phase 10 under way — 9 of 16:** ✅ **01 · PID 1 is not a normal process** (chunked: the kernel's three rules, then the four ways out — 3 files, 529 lines), ✅ **02 · Graceful shutdown** (chunked: the deadline, then doing it in a real service — 3 files, 570 lines), ✅ **03 · Resource limits** (chunked: memory kills, CPU throttles — 3 files, 559 lines), ✅ **04 · Logs go to stdout and stderr** (chunked: the contract, then logs a machine can read — 3 files, 516 lines), ✅ **05 · Configuration and secrets at run time** (259 lines), ✅ **06 · The production failure catalogue** (chunked: triage + the container died, then still-running-and-useless — 3 files, 554 lines). ✅ **07 · Restart policies as supervision** (214 lines), ✅ **08 · Log drivers and rotation** (237 lines), ✅ **09 · Healthchecks in production** (197 lines), ✅ **10 · Hardening at run time** (**chunked** — the four switches, then enforcing it everywhere; 3 files, 545 lines; the first draft hit 348 and was split on a concept boundary, not trimmed). ✅ **11 · Observing** (230 lines — the CLI subtracts the page cache the kernel kills on; host vs cgroup memory). ✅ **12 · Debugging a container you cannot shell into** (276 lines — five routes: `docker debug`, a namespace-sharing sidecar, `nsenter`, `docker cp`, a debug build target). ✅ **13 · Disk growth** (246 lines — the four pools, `RECLAIMABLE` vs virtual `SIZE`, and why `--volumes` never belongs in a cron job). ✅ **14 · Running containers under systemd** (224 lines — one supervisor only, and `docker run` in a unit supervises the CLI, not the container). ✅ **15 · Time, timezones and locales** (194 lines — `CLOCK_REALTIME` is never virtualised, so skew is a host problem; `TZ` only formats). ✅ **16 · Zero-downtime restarts without an orchestrator** (194 lines — the four-step dance, and why `update_config.order` defaults to `stop-first`). 🏁 **PHASE 10 COMPLETE — 16 of 16, 29 files, 5,645 lines, 0 over the 300-line cap, link-checked against the filesystem (not built).** 🚧 **Phase 11 · Podman in depth — 2 of 16:** ✅ **01 · Daemonless** (Master, **chunked**, 3 files / 564 lines — what runs instead of a daemon · what it changes for restart, logs and `systemctl`; the state is files and the files are per user, `logind`'s `KillUserProcesses=` is why a container dies at logout, and `json-file` is an alias for `k8s-file`). ✅ **02 · Rootless by default** (Master, **chunked**, 3 files / 501 lines — the namespace you are always in · what rootless costs; container UID 0 is *you* and UID *n* is `subuid_start + n − 1`, `nobody` is the overflow ID 65534, and `--privileged` lifts none of the documented shortcomings). ✅ **03 · Pods** (207 lines — the infra container holds the namespaces open; ⚠️ **`pid` is NOT in the default `--share` list** and an unprefixed `--share` REPLACES the list instead of appending; the one place `localhost` means a neighbour). ✅ **04 · Quadlet** (Master, **chunked**, 3 files / 549 lines — what Quadlet is · writing the units; a systemd GENERATOR read only at boot and `daemon-reload`, `foo.container` → `foo.service` → container `systemd-foo`, and referencing `data.volume` generates a real dependency while plain `data` does not). ✅ **05 · Where Podman will bite you** (Master, **chunked**, 3 files / 556 lines — it ran yesterday · it works on Docker; a triage catalogue, and 🔴 **a correction to phase 10 topic 09, which asserted systemd timers drive Podman healthchecks — the reference says only "automatic timer setup" and names nothing**). 🏁 **Phase 11's Master tier is COMPLETE at 4 of 4.** ✅ **06 · `podman unshare`** (235 lines — a shell inside the namespace the containers already run in; `sudo chown` succeeds and makes it worse because it writes a host UID that maps to nothing, so the container sees the overflow ID 65534; ⚠️ **not available with the remote client**, so on macOS/Windows it has to run inside `podman machine`). ✅ **07 · `--userns`: `keep-id`, `nomap`, `auto`** (252 lines — six modes; 🔴 **`--pod` makes `--userns` be ignored entirely**, `nomap` is the security opposite of `keep-id`, and `host` means *no namespace* rootful but *the one you are already in* rootless). ✅ **08 · Pod commands** (220 lines — `--pod new:` creates a pod that can never publish a port, 🔴 **`pod rm` deletes the containers too**, and the pod-vs-user-defined-network decision table: Compose builds the network, never the pod). ✅ **09 · Quadlet vs `podman generate systemd`** (210 lines — the deprecation quoted verbatim, and the real argument: a generated unit is *derived data* that drifts, while `--new` is Quadlet's premise written by hand; the migration's skipped step is disabling the old unit). ✅ **10 · `podman auto-update`** (195 lines — three conditions must ALL hold; 🔴 **rollback only works if the container sends READY via SDNOTIFY**, otherwise a container that starts broken looks healthy; `local` policy never touches the network). ✅ **11 · `podman kube play` / `generate kube`** (196 lines — four subcommands, and 🔴 **`apply` is the only one that leaves the machine**; the unsupported cluster fields are IGNORED not rejected, so local success is a smoke test; `--podman-only` produces YAML Kubernetes cannot use). ✅ **12 · Buildah and Skopeo** (205 lines — the split is about privilege, not taste; every Skopeo argument is a **transport plus a location**, which is how it copies registry→registry with nothing stored locally and inspects *without pulling*; ⚠️ `skopeo delete` only marks for the registry's GC). ✅ **13 · Docker CLI compatibility** (202 lines — three levels; ⚠️ **an alias is invisible to every Makefile and CI script**, so `podman-docker` installing a real `/usr/bin/docker` is the answer, and its stderr notice is quietened per-user at `~/.config/containers/nodocker`; the API is pinned to **Docker v1.40** with Podman-native features on the Libpod layer). ✅ **14 · Podman 6 breaking changes** (199 lines — a *removal* release: cgroups v1, BoltDB, CNI, slirp4netns, iptables, Intel Macs and Windows 10; 🔴 **cgroups v1 is the hard blocker** and the BoltDB→SQLite migration is a **one-way door** that makes a rollback to Podman 5 unreadable). ✅ **15 · `podman machine`** (196 lines — 🔴 **your CLI is a REMOTE CLIENT**, which explains all of it: only `$HOME:$HOME` is shared so paths outside home cannot be mounted, `podman unshare` is refused, and `machine rm` takes the images with it). ✅ **16 · Podman Desktop** (195 lines — a view, not a source of truth; clicking is not reproducible, and the ⚠️ **Docker Desktop licence threshold is about the desktop app only**, not Docker Engine on a server). 🏁 **PHASE 11 IS COMPLETE — 16 of 16 at every tier (Master 4/4 · Understand 8/8 · Know 4/4).** 🚧 **Phase 12 · Delivery, CI and orchestration OPEN — 1 of 12:** ✅ **01 · Tag strategy** (Master, **chunked**, 3 files / 485 lines — a tag is a *mutable pointer*, so 🔴 **immutable tags for machines, moving tags for humans, deploy the digest**; build-time drift and deploy-time ambiguity are different failures with non-interchangeable fixes). ✅ **02 · Building images in CI** (225 lines — a CI runner is *a machine with no memory*; 🔴 **build once per commit and test the artefact you ship**, PR and default-branch builds want different cache scopes, and caching can be a **net loss** when early layers change every commit). ✅ **03 · One image, three environments** (Master, **chunked**, 3 files / 462 lines — 🔴 **a rebuild is a different image and the digest proves it**, so promotion moves a *label* registry-side; the one genuine exception is a **browser bundle**, whose build-time `VITE_*` values are already compiled in, answered by same-origin `/api` or a runtime `config.json`). ✅ **04 · Registry authentication in CI** (212 lines — the ladder from a password on the command line up to **OIDC with nothing stored between runs**; `docker login` writes credentials **base64-encoded, not encrypted**; 🔴 **Podman uses a DIFFERENT file and the compatibility is one-way** — it falls back to reading Docker's, never the reverse). ✅ **05 · Testing with containers** (204 lines — the deciding question is *does this run the same way on a laptop*; wait strategies are the readiness problem solved inside the test framework, and ⚠️ **the Ryuk reaper needs engine access, so a locked-down runner HANGS rather than errors**). ✅ **06 · Deploying without an orchestrator** (198 lines — 🔴 **most applications never need one**: Compose on a VM, Quadlet units, or a PaaS, with what each trades; the production concerns of phase 10 survive all three, and **backups survive even a PaaS**). ✅ **07 · When Compose stops being enough** (194 lines — 🔴 **four conditions, not a feeling**: more than one host, health-gated rolling updates, autoscaling, team isolation; Kubernetes' own docs say it is *not a PaaS*, does *not build applications* and provides *no application-level services*, so adopting it moves work rather than deleting it). ✅ **08 · Kubernetes on-ramp** (180 lines — the translation table; 🔴 **the healthcheck becomes TWO probes and conflating them is the classic restart-loop outage**, `depends_on` has no equivalent *by design*, and with no probe defined the kubelet always considers the result a success). ✅ **09 · Rolling updates and rollback by hand** (193 lines — 🔴 **both versions run at once**, so the schema and API must satisfy both; **expand and contract** over two releases is what keeps every release rollback-safe, and rollback is *asymmetric* because data does not go back). ✅ **10 · `docker context`** (152 lines — useful and dangerous for the same reason: 🔴 **`use` is persistent and invisible**, so put `context show` in the prompt and prefer per-command `--context`; Podman's separate equivalent is `podman system connection`). ✅ **11 · Cost realities** (176 lines — four costs that never appear labelled as *containers*: registry storage, egress **on every pull**, CI minutes, and host disk; ⚠️ **no prices are quoted, deliberately** — the shape of each cost is what is durable). ✅ **12 · Docker Swarm in 2026** (172 lines — it is **not deprecated** and *Classic* Swarm is the thing that is; the real risk is ecosystem gravity, and the page refuses to conflate the two). 🏁 **PHASE 12 IS COMPLETE — 12 of 12 (Master 2/2 · Understand 4/4 · Know 3/3 · When Needed 3/3), and with phases 10 and 11 already closed, CHUNK D IS FINISHED — 44 of 44. THE WHOLE DOCKER & PODMAN TRACK IS COMPLETE AT 192/192.** 🔴 **Phase 10's Master tier is COMPLETE at 5 of 5**; the remaining nine topics are Understand and Know. 0 files over the 300-line cap. Then phase 11 · 01 · Daemonless, then phase 12 · 01 · Tag strategy. Quadlet (11) is how phase 10's "containers under systemd" is actually done, and phase 12's deploy-without-an-orchestrator answer is Quadlet or Compose on a VM. Work on `main`, stage explicit paths, **never `git add -A`**. Resume file: `devbible/progress_docker_split_4way.md` |
| **Docker & Podman** — phases **0–3**, written | session `40090c06` | 2026-08-14 | ✅ **Done and released — 63 of 192 topics (33%), 66 files, 0 over the 300-line cap**, merged to `main` 2026-08-15. ✅ **Syllabus complete: 4 parts · 13 phases · 192 topics** (Master 55 / Understand 85 / Know 44 / When Needed 8 — counted from the badges, not estimated). Both engines are taught together because both turn up in practice; the 5% where they genuinely differ is called out per topic, and Phase 11 collects the Podman-specific depth. Targets **Docker Engine 29.7.2 · Compose v5.4.0 · Podman 6.1.0**; this machine has **Podman 5.8.4 and no Docker**. 🔴 **No sandbox** — every page is documentation-validated against docs.docker.com, docs.podman.io, the OCI specs and the release notes, with the source named, and carries **no console output blocks**. ⚠️ `src/data/progress.js` has **one** docker row all four chunks increment |
| **Nginx** — all of `docs/nginx/`           | session `21fbf27e` | 2026-08-14 | 🔴 **Active — new track.** Work happens in worktree **`devbible-nginx`, branch `nginx`**, not the shared checkout, so a build break here is genuinely this track's fault. ✅ **Syllabus complete — 4 parts, 12 phases, 210 topics** (Master 58 / 28% · Understand 101 · Know 38 · When Needed 13), scoped to what a fullstack MERN/PERN developer needs in front of a Node app; the mail modules, media streaming, body-transform modules and NGINX Plus depth are deliberately out. 🔴 **The version fact the whole track turns on:** since **nginx 1.29.7** upstream `keepalive` is **on by default** (`keepalive 32 local;`), `proxy_http_version` defaults to **1.1**, and `Connection` is no longer sent — the keep-alive incantation in every tutorial is redundant on 1.30+ and still required on 1.28 and older. Target: **1.30.x stable** (1.30.4, 15 July 2026). Documentation-validated against nginx.org under the no-new-sandboxes rule; **there is no nginx sandbox in this repo and none is planned**. ✅ **Phases 0 and 1 COMPLETE — 28/210 topics, 20 files.** Phase 0 the process model (14 topics, 10 files); phase 1 the configuration language (14 topics, 15 files — three topics chunked past the 300-line cap: `include`, `map`, `if is evil`). ✅ **Phase 2 COMPLETE — 18/18 topics, 13 files** (server selection, the default server and host-header safety, the location algorithm, named/`internal` locations, the request stages, internal redirects, `error_page`). **46 of 210 topics written (22%), 41 files, 0 over the 300-line cap.** Next unit: **Phase 3 · Serving static files and SPAs (14 topics)**. |
| **Real World** — all of `docs/real-world/` (new section) | session `08ab5390` | 2026-08-16 | 🔴 **Active — new implementation track on the user's instruction.** One storefront app built across the whole stack: 9 phases, **79 topics** (spec · raw-`pg` database · Node services · Express API · React UI + hooks · JS custom functions · TypeScript · CSS recipes · MongoDB mirror). **Chapters compose existing concept pages and link to them — nothing is re-taught**, per the no-duplication instruction. Syllabus complete (3 parts) and wired into `sidebars.js`, `progress.js`, the homepage and this file. ✅ **Phases 0–4 COMPLETE** (app spec 3 · database 12 · Node services 10 · Express API 12 · React UI 12) — 🏁 the backend spine and the frontend both closed. 🚧 **Phase 5 · JS custom functions at 5/10**: fetch wrapper, TTL cache with stale-while-revalidate, concurrency-limited task queue, event bus, and the form validation engine. **54 of 79 topics, 67 files, 0 over the 300-line cap.** Next is **5·06 · Money and dates with `Intl`**. This session also holds the **Node.js review** (complete — gaps worklist in the store: `devbible/progress_nodejs_improve_review.md`). Doc-validated under the no-new-sandboxes rule; no console blocks. Resume: `devbible/progress_realworld_build.md` |
| **Real World · chunk B** — phase **7 · CSS recipes** | session `36c19088` | 2026-08-17 | 🔴 **Chunk B of the THREE-way Real World split (2026-08-17) — claimed and in progress.** 🔴 **RE-SCOPED to 4 topics on 2026-08-17** — the user dropped **02 · The header and navigation** and **03 · The checkout form** (*"it was css i know how to build those, they are not worth for writing topics"*). Both are standard flex/grid work already covered by CSS 4·06 and CSS 1·09; their syllabus rows are struck through rather than deleted, and reinstating either needs a new instruction. This is the only unwritten CSS work in the repo — the CSS track itself is complete at 74/74. Scope is `docs/real-world/pages/phase-7-css-recipes/` and nothing else. 🚧 **2 of 4:** ✅ **01 · The product grid** (<span className="db-tier t-master">Master</span>, **chunked into 12 + index — 3,025 lines, largest 299, 0 over the 300-line cap**): the track-sizing decision · `auto-fill` vs `auto-fit` and the list reset · the card adapts to its column · container units and what containment does to you · images without layout shift · image delivery and long grids · the text squeeze and clamping · the price row and row alignment · the loading state and announcements · empty, end and error states · the complete stylesheet · tokens, layers and the contract. 🔴 **Three splits happened *because of* the 300-line rule and none lost a word** — the first draft of chunk 01 hit 335, chunk 03 hit 301, chunk 05 hit 302, and each was split on a concept boundary. **Verified by all three checks: 120 internal links resolved against the filesystem, MDX compiles, AST scan finds no render bombs — link-checked, NOT built** (no build or dev server was run; the registry row was never claimed). ✅ **04 · Skeleton loaders and spinners** (<span className="db-tier t-understand">Understand</span>, **5 chunks + index — 1,301 lines, largest 268**): skeleton/spinner/nothing and the three perceptual thresholds · building the skeleton as *the component with its content removed* · the shimmer and why `background-position` janks a grid · the spinner and the busy button · the complete stylesheet. The load-bearing findings: **a CSS-only 400 ms delay (`animation` + `animation-delay` + `fill-mode: forwards`) needs no timer and never flashes on a fast response**; **skeletons stop under `prefers-reduced-motion` but spinners must SLOW, because a stopped spinner reads as hung**; and `disabled` throws focus to the body, which `aria-disabled` plus a handler guard does not. Doc-validated against MDN, the CSS specs and the Nielsen Norman response-time thresholds; **no sandbox, no console blocks**. Next is **7·05 · Dark mode**, then 06 · The overlay layer. Work on `main`, stage explicit paths, **never `git add -A`**. Resume file: `devbible/progress_realworld_chunk_b_css.md` |
| **Unclaimed**                              | —                  | —          | **None — every technology in the brief is now claimed, in progress or complete**  |

🔴 **CONSOLIDATED 2026-08-15 — there are no worktrees and no branches, only `main`.**
Every worktree and branch was merged into `main` and deleted. Any "worktree
`devbible-…`" or "not merged" note in the rows above is **historical**; nothing is
stranded. **A clean rebuild of `main` reports 0 warnings and 0 broken links**, so the
old "those breaks belong to another session" excuse no longer applies — **a break is
yours.** Work in `/run/media/sairam/Storage/Backup/Knowledge/devbible` on `main`.

**Rules for a shared checkout:** never `git add -A` — stage explicit paths only.
`src/data/progress.js` is edited by everyone; touch only your language's rows.
`yarn build` exits 0 with broken links, so grep the log — but a **duplicate doc
id exits 1**, and that failure is often another session mid-write, so wait and
retry before investigating.

:::

Every technology in the bible sits in its own folder, holding two things in this
order:

1. **Syllabus** — the topic inventory. What to learn, in what order, and **how
   hard to work on each item**. No explanations, no code.
2. **Explanations** — the actual pages, one per topic, with runnable code,
   gotchas and interview questions. Written phase by phase once the syllabus is
   approved.

## 🔴 The critical rule — do not violate this

**The 300-line cap is a FILE-SIZE rule, never a content budget.**

It caps how much lives in one *file*. It says **nothing** about how much a
*topic* gets explained. A topic may run 1000+ lines in total — that is normal and
expected for a Master-tier topic.

- **Write the explanation the topic deserves FIRST, then split.** Never size a
  page to fit the cap. Never trim a section, drop a gotcha, or shorten interview
  answers to save lines. Coverage is fixed; file count is the variable.
- At 301 lines, **split on a concept boundary** into a topic directory:
  `NN-topic/` with `_category_.json`, a `README.md` index, and `NN-chunk.md`
  parts. Each chunk repeats the tier badge and `> Verified:` line and carries its
  **own** Gotchas and Interview questions.
- **The tell that you got this wrong:** a run of pages all landing in a narrow
  band just under the cap. Real topic lengths vary widely; clustering at ~200–290
  is evidence of budgeting, not of topics that happened to be that size.

Two rules that pair with it:

- **Never invent output.** Every number, timing and error string comes from a run
  that actually happened. No run means **no console block** — write the
  explanation without it rather than reconstructing one from memory.
- **No new sandboxes.** Validate against the official documentation or the web
  and **name the source** in the `> Verified:` line. Pages backed by an existing
  script are marked **sandbox-measured** so a reader can tell measured evidence
  from cited evidence.

**Link form:** every link ends in `.md` and keeps every numeric prefix —
`../01-inner-join/README.md` for a directory index,
`../01-inner-join/02-fan-out.md` for a file inside it. The directory-slug form
broke 188 links; never bulk-`sed` these.

## Priority tiers

Every topic carries exactly one tier. The tier answers *"how much effort does this
deserve right now?"* — it is about **effort allocation**, not importance.

| Badge                                                    | Tier                | Bar to clear                                                                                       |
| -------------------------------------------------------- | ------------------- | -------------------------------------------------------------------------------------------------- |
| <span className="db-tier t-master">Master</span>         | Must Learn & Master | Use it confidently **without opening documentation**. If you look it up mid-task, you're not done. |
| <span className="db-tier t-understand">Understand</span> | Must Understand     | Know **how it works**, use it correctly. Looking up exact signatures is fine.                      |
| <span className="db-tier t-know">Know</span>             | Should Know         | Know **what it is, why it exists, when it's the right tool**. Details when needed.                 |
| <span className="db-tier t-when">When Needed</span>      | Learn When Needed   | **Don't study upfront.** Learn it the day a project demands it.                                    |

Tiers are assigned **for fullstack application development** — this bible's
purpose. `worker_threads` is <span className="db-tier t-know">Know</span> for a CRUD API and would be <span className="db-tier t-master">Master</span> at a
media-processing company. Where a tier is context-dependent, the syllabus says so.

## Coverage

Ordered by how far the explanations have got, not alphabetically.

| Technology                               | Syllabus             | Explanations                                                                                                                               |
| ---------------------------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| **[Node.js](./nodejs/README.md)**        | 4 parts · 248 topics | **✅ COMPLETE** — [231 pages](./nodejs/pages/README.md) across 13 phases; all 248 topics covered, every page carries a `> Verified:` line   |
| **[PostgreSQL](./postgresql/README.md)** | 4 parts · 233 topics | **✅ COMPLETE** — [298 pages](./postgresql/pages/README.md) across 14 phases; every page carries a `> Verified:` line                       |
| **[JavaScript](./javascript/README.md)** | 5 parts · 337 rows, **316 in scope** | 🏁 **The active queue is empty (2026-08-15)** — **282 of 316 topic rows written / [526 leaf pages](./javascript/pages/README.md)**. The **active queue is 269 topics and every one is written** (phases **0–12** and **16–18**, complete at every in-scope tier); the other 13 are the **Master tier inside the parked phases 13–15**, whose remaining **34 topics are not work to continue**. **Master is 99/99 across phases 0–18.** Counts re-derived from disk 2026-08-15 and matched against `src/data/progress.js`, which now marks 13–15 `parked` so the site reads **100%** rather than showing them as mid-flight |
| **[TypeScript](./typescript/README.md)** | 4 parts · **136 topics in scope** (187 before the 2026-08-15 Part B cut) | 🚧 In progress — **116 of 136 topics**, [376 files](./typescript/pages/README.md) (⚠️ **both recomputed from `progress.js` and `find`, not incremented by hand** — Parts A, B, C and D are all mid-write, so these numbers move under you; re-measure before editing this row); phases 0–3 complete, **phase 4 COMPLETE at 14/14**, **phase 5 open at 12/16**, **phase 6 open at 14/16**, **phase 7 COMPLETE at 5/5 — cut to its Master rows**, 🏁 **phase 10 COMPLETE at 13/13**. ⛔ Part B dropped phases **8, 9 and 11** and phase 7's ten Understand rows; nothing written was deleted. Part B's remaining scope is **phases 10 and 12 (28 topics)** |
| **[CSS](./css/README.md)**               | 4 parts · 74 topics  | **✅ COMPLETE** — [80 leaf pages, 97 files](./css/pages/README.md) across 11 phases; critical-path scope                                                   |
| **[React](./react/README.md)**           | 4 parts · 210 topics | ✅ **COMPLETE** — [277 leaf pages, 333 files](./react/pages/README.md), **phases 0–11 and 14** plus the **Patterns** section; **phases 12 (Data and state) and 13 (Routing) dropped by decision** — confirmed 2026-08-17, no page links to them and `docs/react/` builds with 0 broken links |
| **[Git](./git/README.md)**               | 5 phases · 52 topics (re-scoped) | ✅ **COMPLETE** — [52 topics](./git/pages/README.md) (65 files) — daily-driver scope, phases 3 and 6–12 parked                                                                                       |
| **[Express](./expressjs/README.md)**     | 4 parts · 115 topics | ✅ **Complete, Master tier at full depth** — [183 files](./expressjs/pages/README.md) across 11 phases, 30,731 lines; all 115 topics covered and verified, the 28 Master topics chunked into `NN-topic/` directories; 0 over the cap, 0 broken links |
| **[MongoDB](./mongodb/README.md)**       | 4 parts · 82 topics  | Pending — [34 pages](./mongodb/pages/README.md), **phases 0–5 complete**, phase 6 next                                                         |
| **[Docker & Podman](./docker/README.md)** | 4 parts · 192 topics | ✅ **COMPLETE — [192 of 192 topics](./docker/pages/README.md), all 13 phases** — chunk A closed 4 and 5, chunk B closed 6 and 7, chunk C closed 8 and 9 (all 2026-08-15), and chunk D closed **10, 11 and 12** (2026-08-15/16). **266 files, 50,768 lines, 0 over the 300-line cap, 2,184 internal links all resolving.** Both engines together; **no sandbox**, documentation-validated with no console blocks |
| **[Redis](./redis/README.md)**           | 4 parts · 74 topics  | 🚧 **1 of 74 topics** — [phase 0 open](./redis/pages/phase-0-how-redis-runs/README.md), split three ways across sessions (A 0–3 · B 4–6 · C 7–10) |
| **[Nginx](./nginx/README.md)**           | 4 parts · 210 topics | 🔴 In progress — [42 files](./nginx/pages/README.md), **phases 0–2 complete (46/210 topics, 22%)**; phase 3 next. Target nginx 1.30.x stable |
| **[Real World](./real-world/README.md)** | 3 parts · 77 topics  | 🔴 In progress — [phases 0–5 COMPLETE, phase 6 at 1/8, phase 7 at 2/4, **62/77 topics**](./real-world/pages/README.md) (103 files, 0 over the 300-line cap); one storefront implemented across the whole stack. Split three ways: **A** = phase 6 TypeScript · **B** = phase 7 CSS recipes · **C** = phase 8 MongoDB mirror. ⚠️ **77, not 79** — phase 7's header and checkout-form topics were **dropped 2026-08-17** as standard flex/grid work already covered in the CSS section |

### Imported from the frontend-bible corpus (2026-08-14)

**204 pages moved in across 12 new technologies.** They are complete and readable but were
written to a different standard — see each overview for what is still outstanding.

| Technology | Syllabus | Explanations |
|---|---|---|
| **[Storybook](./storybook/README.md)** | 4 parts · 58 topics | Mixed — [23 topics written and build-verified](./storybook/pages/README.md) (phases 0–3, 5,488 lines) **plus 22 imported pages moved in as-is**. **Phases 0–3 written to full depth (23 topics)** plus the imported corpus (22 pages) — the only imported track with verified pages |
| **[Vite](./vite/README.md)** | Imported corpus | ⚠️ **Moved in as-is** — [16 pages](./vite/pages/README.md); no `> Verified:` line, no tier badges, no interview questions yet. Imported as-is |
| **[Webpack](./webpack/README.md)** | Imported corpus | ⚠️ **Moved in as-is** — [21 pages](./webpack/pages/README.md); no `> Verified:` line, no tier badges, no interview questions yet. Imported as-is |
| **[Babel](./babel/README.md)** | Imported corpus | ⚠️ **Moved in as-is** — [16 pages](./babel/pages/README.md); no `> Verified:` line, no tier badges, no interview questions yet. Imported as-is |
| **[ESLint & Oxlint](./eslint-oxlint/README.md)** | Imported corpus | ⚠️ **Moved in as-is** — [21 pages](./eslint-oxlint/pages/README.md); no `> Verified:` line, no tier badges, no interview questions yet. Imported as-is |
| **[Jest & RTL](./jest-rtl/README.md)** | Imported corpus | ⚠️ **Moved in as-is** — [16 pages](./jest-rtl/pages/README.md); no `> Verified:` line, no tier badges, no interview questions yet. Imported as-is |
| **[Playwright](./playwright/README.md)** | Imported corpus | ⚠️ **Moved in as-is** — [16 pages](./playwright/pages/README.md); no `> Verified:` line, no tier badges, no interview questions yet. Imported as-is |
| **[Redux Toolkit](./redux-toolkit/README.md)** | Imported corpus | ⚠️ **Moved in as-is** — [16 pages](./redux-toolkit/pages/README.md); no `> Verified:` line, no tier badges, no interview questions yet. Imported as-is |
| **[TanStack Query](./tanstack-query/README.md)** | Imported corpus | ⚠️ **Moved in as-is** — [16 pages](./tanstack-query/pages/README.md); no `> Verified:` line, no tier badges, no interview questions yet. Imported as-is |
| **[Framer Motion](./framer-motion/README.md)** | Imported corpus | ⚠️ **Moved in as-is** — [16 pages](./framer-motion/pages/README.md); no `> Verified:` line, no tier badges, no interview questions yet. Imported as-is |
| **[Web Vitals & Performance](./web-vitals-performance/README.md)** | Imported corpus | ⚠️ **Moved in as-is** — [11 pages](./web-vitals-performance/pages/README.md); no `> Verified:` line, no tier badges, no interview questions yet. Imported as-is |
| **[Frontend Architecture](./frontend-architecture/README.md)** | Imported corpus | ⚠️ **Moved in as-is** — [15 pages](./frontend-architecture/pages/README.md); no `> Verified:` line, no tier badges, no interview questions yet. Imported as-is |

Express was previously listed last with the note that its pages "average a third the
depth of the rest". **That was resolved on 2026-08-14**: every topic was brought to
depth, nine syllabus topics that had no page were written, and all 85 pages carry a
`> Verified:` line. Phases 1–10 are documentation-validated with **no sandbox runs**,
which their Verified lines state explicitly.

## What "Verified" means

Every number, timing, error string and console block on a page comes from a script
that was actually executed — never from memory or plausibility. A page that has
been through that carries a `> Verified:` line under its title naming the versions
it was measured on. A page without one has not cleared that bar yet.
