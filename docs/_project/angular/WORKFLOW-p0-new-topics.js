export const meta = {
  name: 'angular-p0-new-topics',
  description: 'Write Angular Phase 0 topics from scratch: research + topic README, then one agent per chunk, then wire the footer chain. Topic slugs come from args.',
  phases: [
    { title: 'Plan', detail: 'Per topic: bank the primary-source research once, then write the topic README and chunk table' },
    { title: 'Write', detail: 'One agent per chunk; each writes ONE file, QCs it, and commits it through the flock helper' },
    { title: 'Wire', detail: 'Per topic: rewrite the footer chain from what is on disk and relink the README chunk table' },
  ],
}

const REPO = "/mnt/Storage/Backup/Knowledge/devbible"
const PDIR = REPO + "/docs/angular/pages/phase-0-how-angular-runs"
const BANK = "/tmp/claude-1000/-mnt-Storage-Backup-Knowledge-devbible/11a770dc-c853-4ea8-b21d-3b1797333cc0/scratchpad/ng-bank.sh"
const MDX  = "/mnt/Storage/my-learning/claude/shared/scripts/mdxcheck.py"

const SHARED = [
  "You are one lane of a parallel run completing **Angular Phase 0** of the devbible reference corpus.",
  "",
  "## Read these FIRST, in full, before writing a single line",
  "",
  "1. `/mnt/Storage/my-learning/claude/devbible/angular/PHASE-0-LANE-BRIEF.md` — the shared brief.",
  "   🔴 It opens with a **CORRECTION 2026-09-06** block that OVERRIDES the version table further down.",
  "   Read the correction block especially carefully; it lists facts the older spec gets wrong.",
  "2. `" + REPO + "/.agents/references/authoring-contract.md` — the depth bar and the 300-line cap.",
  "3. `" + REPO + "/.agents/references/house-style.md` — tier badges, the `> Verified:` line, headings, `★`, footers.",
  "",
  "## The version spine — measured, do NOT re-derive from your training data",
  "",
  "Angular **22.1.5** (`latest`, published 2026-09-03) · `next` 22.2.0-next.5 · CLI / `@angular/build` /",
  "`@angular/ssr` **22.1.7** · `@angular/material` + `@angular/cdk` 22.1.5 · TypeScript peer `>=6.0 <6.1`",
  "(hard pin — Angular 22 does NOT run on TS 5.x) · Node `^22.22.3 || ^24.15.0 || >=26.0.0` · RxJS peer",
  "`^6.5.3 || ^7.4.0` (no RxJS 8) · zone.js peer `~0.15.0 || ~0.16.0` and OPTIONAL · vitest `^4.0.8` is the",
  "`@angular/build` test peer, `karma@^6.4.0` still a peer but end of life. v22 released 3 Jun 2026.",
  "LTS: v21 -> 21.2.22 (to ~May 2027), v20 -> 20.3.30 (ends ~Nov 2026), v19 OUT OF SUPPORT at 19.2.25",
  "(2 Jun 2026). Majors every 6 months (May/June + November); **6 months active + 12 months LTS**.",
  "**v23 is due ~Nov 2026.** `@angular/animations` and `@angular/platform-browser-dynamic` still published.",
  "",
  "🔴 **Four traps that have already caught a lane here:**",
  "- **Zoneless is the DEFAULT in v22**, not merely stable. `internalCreateApplication` prepends",
  "  `provideZonelessChangeDetectionInternal()`; the v22 `ng new` `app.config.ts` has no zoneless call at all.",
  "- **`NullInjectorError: No provider for X!` NO LONGER EXISTS in v20+.** Never teach that string.",
  "  `null_injector.ts` throws ``No provider found for `X`.`` with `error.name = 'ɵNotFound'`, and",
  "  `formatRuntimeError` prefixes `NG0201: `. angular.dev's own DI troubleshooting guide is STALE here —",
  "  docs and source disagree and **the source is right**. Say so on the page when it is relevant.",
  "- **Deprecated in v22, never present as the modern path:** `withFetch` (FetchBackend is the default;",
  "  `withXhr()` is the opt-out) · `withJsonpSupport` · `withIncrementalHydration` (on by default; opt out",
  "  with `withNoIncrementalHydration()`) · `provideAnimationsAsync` · `HttpClientModule` family.",
  "- ⚠️ **`angular.dev/tools/cli/aot-compiler` is STALE** — it still describes the ViewEngine collector,",
  "  `.metadata.json` and `StaticReflector` while sitting under the v22 nav. Its CONSTRAINTS still hold;",
  "  its PHASE DESCRIPTIONS do not describe Ivy. Cite architecture claims, never retired artefacts.",
  "",
  "🔴 **Reading Angular source: `raw.githubusercontent.com` 404s here.** Tags carry a `v` prefix. Use",
  "`gh api repos/angular/angular/contents/<path>?ref=v22.1.5 -q .content | base64 -d`. WebFetch angular.dev freely.",
  "",
  "## The bar — this is a reference corpus, not a docs summary",
  "",
  "The bar is **the page you wish you had had at 2am with the thing broken in production.** A page that",
  "reads like documentation summarised is a FAILED page.",
  "",
  "- **300 lines is a FILE-SIZE cap, never a content budget.** Do not pad toward it and do not trim to fit.",
  "  If your chunk genuinely needs more than 300 lines, write it all and then split on a concept boundary",
  "  into a lettered sibling (`05-x.md` + `05b-y.md`, NEVER renumbering) — and say so in your report.",
  "- **Exhaust the subject.** Every gotcha, pitfall, worked example and interview question the chunk",
  "  genuinely has — never five because five looked like enough. If it fails eleven ways, list eleven.",
  "- **Never write \"the fix is X\" without showing X in code.**",
  "- Code is **runnable and complete** — realistic names, **no `...` elisions**. Non-runnable code is",
  "  labelled `// pseudo-code`.",
  "- 🔴 **No sandbox, ever.** No new scripts, no `yarn build`, no `yarn start`, no `ng` commands, no",
  "  containers, no timings. TypeScript / HTML / JSON / shell **source** is fine; the **output** of running",
  "  it is not. Never invent a terminal transcript, a bundle size, a file tree you did not read in a",
  "  document, or a compiled-output dump.",
  "- ⛔ **Never add a ```console fence.** That is program output and there is nothing here to produce it.",
  "- **A claim the documentation cannot settle is written as explicitly uncertain, or left out.**",
  "  \"The documentation does not state whether X\" is a legitimate sentence. A confident invention is the",
  "  only unacceptable outcome. Report every such claim.",
  "",
  "## Exact page shape — copy this, do not open another page to learn it",
  "",
  "```markdown",
  "---",
  "title: \"A full sentence stating the page's claim, not a label\"",
  "sidebar_label: \"NN · Short label\"",
  "sidebar_position: N",
  "---",
  "",
  "<span className=\"db-tier t-understand\">Understand</span>",
  "",
  "> Verified: 2026-09-06 against angular.dev — [Page title](url), [Page title](url) — and `angular/angular`",
  "> at tag `v22.1.5`: [`path/file.ts`](https://github.com/angular/angular/blob/v22.1.5/path/file.ts).",
  "> Documentation-validated; **no sandbox run**.",
  "> Version spine: **Angular 22.1.5** · CLI / `@angular/build` / `@angular/ssr` **22.1.7** · TypeScript peer `>=6.0 <6.1`.",
  "",
  "**Bold thesis paragraph — three to six sentences saying what is TRUE and why it matters, not what the",
  "page will cover.**",
  "",
  "## <body sections, named after what they argue>",
  "",
  "## Gotchas",
  "",
  "## Interview questions",
  "```",
  "",
  "- The tier badge is the first line after the frontmatter, on its own line, BEFORE `> Verified:`.",
  "  🔴 Use the tier you are told — do not choose one.",
  "- `sidebar_label` separator is a middle dot `·` with spaces — never a hyphen or colon.",
  "- `## Gotchas` and `## Interview questions` are **mandatory and their names are exact**.",
  "- Gotchas are **symptom → cause → fix**, leading with the symptom. Pick ONE of the two house forms and",
  "  stay consistent within your page: either `**★ Symptom: …** Cause: … Fix: …` inline, or the",
  "  `### Name` / `**Symptom.**` / `**Cause.**` / `**Fix.**` block form.",
  "- Interview questions prefer *why* and *what happens if* over *what is*. Mark the frequently-asked ones",
  "  by opening the line with `**★ `.",
  "- Quote load-bearing documentation sentences **verbatim** as `> *\"…\"*` with the source named. A",
  "  paraphrase of a rule is where errors enter.",
  "- ⚠️ Docusaurus `:::note` admonitions are NOT house style. Highlight with **bold lead-ins**, 🔴 and ⚠️.",
  "",
  "## 🔴 MDX hazards — these pass a local read and ABORT the production build",
  "",
  "1. A bare `<!-- … -->` in prose → use `{/* … */}`.",
  "2. A bare `<Something` in prose — `<app-root>`, `Signal<T>`, `Observable<User>`, `<my-app>` →",
  "   **always backtick** them. This is the one that bites hardest in Angular writing.",
  "3. `{` and `}` in prose outside a code fence are JSX delimiters — backtick anything containing them,",
  "   including `{{ interpolation }}` and `@if {}` blocks.",
  "4. An inline code span left OPEN at end of line whose next line starts with `{` → reflow.",
  "",
  "## 🔴 Links — a dangling link breaks the build for every other session in this shared checkout",
  "",
  "Every link ends in `.md` and keeps its numeric prefix. Never link a bare directory slug.",
  "**`ls` a path before you link it.** Anything not yet on disk is **bold text plus *(not written yet)***.",
  "",
  "## Scope — hard boundaries",
  "",
  "- Write **ONLY** the file(s) you are told to write. Touch nothing else — not a sibling chunk, not",
  "  another topic, not the phase README, not `_category_.json`, not `progress.js`, not `sidebars.js`.",
  "- Anything you find wrong outside your file: **report it, do not fix it.**",
  "- Never `git add -A`. Never `git add` by hand. Never `git commit` by hand. Use only the helper below.",
].join("\n")

const QC = [
  "## Before you report — run these, and fix what they find",
  "",
  "```bash",
  "wc -l <your file>                                   # <= 300",
  "grep -c '^\\*\\*★' <your file>                        # the ★ entries are really there",
  "grep -n '<!--' <your file>                          # must be empty",
  "grep -n '```console' <your file>                    # must be empty",
  "head -8 <your file>                                 # frontmatter + tier badge + > Verified:",
  "tail -3 <your file>                                 # the footer is present and is not {/* FOOTER */}",
  "python3 " + MDX + " --no-rawtag <your file>",
  "```",
].join("\n")

const CHUNK_SCHEMA = {
  type: "object",
  additionalProperties: false,
  required: ["file", "lines", "gotchas", "questions", "committed", "unconfirmed", "outsideFindings"],
  properties: {
    file: { type: "string" },
    lines: { type: "integer" },
    gotchas: { type: "integer" },
    questions: { type: "integer" },
    starEntries: { type: "integer" },
    committed: { type: "boolean" },
    splitInto: { type: "array", items: { type: "string" } },
    unconfirmed: { type: "array", items: { type: "string" } },
    outsideFindings: { type: "array", items: { type: "string" } },
  },
}

const PLAN_SCHEMA = {
  type: "object",
  additionalProperties: false,
  required: ["bankPath", "readmeWritten", "committed", "keyFacts", "sources", "contradictions", "planChanges"],
  properties: {
    bankPath: { type: "string" },
    readmeWritten: { type: "boolean" },
    committed: { type: "boolean" },
    keyFacts: { type: "array", items: { type: "string" } },
    sources: { type: "array", items: { type: "string" } },
    contradictions: { type: "array", items: { type: "string" } },
    planChanges: { type: "array", items: { type: "string" }, description: "any chunk whose scope you found wrong, and what you put in the README instead" },
  },
}

const WIRE_SCHEMA = {
  type: "object",
  additionalProperties: false,
  required: ["footersFixed", "readmeUpdated", "committed", "danglingLinks", "notes"],
  properties: {
    footersFixed: { type: "integer" },
    readmeUpdated: { type: "boolean" },
    committed: { type: "boolean" },
    danglingLinks: { type: "array", items: { type: "string" } },
    notes: { type: "array", items: { type: "string" } },
  },
}

// ─────────────────────────────────────────────────────────────────────────────
// The nine topics. `args` selects which of these this run writes.
// ─────────────────────────────────────────────────────────────────────────────

const ALL = {

"04-ng-update-not-npm-install": {
  label: "04 · `ng update`, not `npm install`",
  tier: "understand", tierLabel: "Understand",
  prevTopic: "[The provider array is the wiring](../03-the-provider-array/README.md)",
  nextTopicSlug: "05-the-angular-build",
  nextTopicLabel: "The build: `@angular/build`",
  thesis: "An Angular upgrade is a **code migration**, not a dependency bump. `ng update` runs migration schematics shipped inside the new version that rewrite your source; `npm install @angular/core@latest` does the half that changes numbers and skips the half that changes code.",
  scope: "⚠️ **Full migration mechanics are Phase 15 at Master.** This topic installs the MENTAL MODEL and makes the reader unable to run `npm install @angular/core@latest` by accident. Name Phase 15 rather than teaching it.",
  research: [
    "- What `ng update` actually does that `npm install` cannot: migration schematics shipped inside the new version that rewrite your source (renamed symbols, changed signatures, control-flow migration, removed APIs).",
    "- The full flag surface: `--force`, `--allow-dirty`, `--migrate-only`, `--from`, `--to`, `--next`, `--name`, `--create-commits`/`-C`, `--verbose`. Confirm each against the CLI docs; do not invent one.",
    "- 🔴 One major at a time. `ng update @angular/core@21 @angular/cli@21` then `@22`. Why jumping v20 -> v22 loses v21's migrations FOREVER (the v22 package does not contain v21's schematics).",
    "- Why core and CLI must be updated together.",
    "- Third-party packages with their own migrations in the same command: `@angular/material`, `@ngrx/*`.",
    "- Why a dirty tree is refused, and what `--allow-dirty` costs you.",
    "- update.angular.dev — what it is and what it gives you.",
    "- Node and TypeScript being upgraded BY the Angular upgrade rather than before it; the hard TS `>=6.0 <6.1` pin as the thing that breaks first.",
    "- An out-of-support version (v19 since 2 Jun 2026): the migrations still exist, nobody will fix them.",
  ].join("\n"),
  chunks: [
    { n: "01", file: "01-what-ng-update-does-that-npm-install-cannot.md", label: "01 · What `ng update` does that `npm install` cannot",
      covers: "The core distinction, argued not asserted. `npm install` changes `package.json` and `node_modules`; `ng update` additionally runs the **migration schematics shipped inside the new version** against your source tree. Show a concrete before/after of a migration rewriting real code (a renamed symbol, a changed signature). Explain where the schematics live and how the CLI finds them. Then the consequence: a project bumped with `npm install` compiles against new packages with old source, and the errors it produces do not point at the cause." },
    { n: "02", file: "02-one-major-at-a-time.md", label: "02 · One major at a time",
      covers: "🔴 The expensive mistake this topic exists to prevent. `ng update @angular/core@21 @angular/cli@21`, then `@22` — one major per run, in order. Why: v22's package does not carry v21's migration schematics, so a v20 -> v22 jump loses them permanently and you hand-fix what a tool would have done. Show the correct sequence for a v20 app reaching v22. Why core and CLI move together. The deprecation policy as the reason it hurts (an API deprecated in v(N) is removed no earlier than v(N+2), so two majors is exactly the window where things vanish)." },
    { n: "03", file: "03-reading-the-update-output.md", label: "03 · Reading the update output",
      covers: "What the command reports and what to do with it: the packages it will change, the migrations it will run, and which migrations it names. How to review the resulting diff — this is the step people skip. Committing first so the migration is its own reviewable commit, and `--create-commits` doing that for you. What a migration that reports \"no changes\" usually means. ⛔ Do not fabricate terminal output — describe what is reported and quote the docs, never invent a transcript." },
    { n: "04", file: "04-the-flags.md", label: "04 · The flags, and when each is legitimate",
      covers: "Each flag with the case that justifies it and the case where it is a mistake: `--force` (peer conflicts — and why it is usually the wrong answer), `--allow-dirty` (and what you lose), `--migrate-only` with `--from`/`--to` (re-running migrations on source you already bumped by hand — the rescue path for someone who ran `npm install` first), `--next` (pre-releases), `--name` (one named migration), `--create-commits`, `--verbose`. Confirm every flag against the CLI documentation and say so if one cannot be confirmed." },
    { n: "05", file: "05-third-party-packages-with-migrations.md", label: "05 · Third-party packages with migrations",
      covers: "`@angular/material`, `@angular/cdk`, `@ngrx/*` and any library shipping `ng-update` metadata in its `package.json`. Why they belong in the SAME `ng update` command rather than a separate run. What `ng-update` metadata is and how a library declares its migrations. What to do about a library with no migrations and no v22 release — the honest options, including staying put." },
    { n: "06", file: "06-node-typescript-and-the-peer-pins.md", label: "06 · Node, TypeScript and the peer pins",
      covers: "🔴 The counter-intuitive rule: you do not upgrade TypeScript and then Angular — Angular's upgrade brings TypeScript with it. The hard `>=6.0 <6.1` peer pin is the thing that breaks first, and topic 01 chunk 13 explains WHY the pin is narrow (`ngtsc` is a TypeScript transformer built against its compiler API). Node engines `^22.22.3 || ^24.15.0 || >=26.0.0` and what happens on an older Node. RxJS and zone.js peers. Show the failure for each and the fix in order." },
    { n: "07", file: "07-update-angular-dev-and-out-of-support-versions.md", label: "07 · update.angular.dev and out-of-support versions",
      covers: "What update.angular.dev is for and how to use it before an upgrade rather than during one. Then the honest part: an app on an out-of-support version. v19 has been out of support since 2 Jun 2026 — its migrations still exist and still run, but no one will fix them, and each further major you fall behind adds a hop. The decision a team actually faces, framed as a cost comparison rather than advice." },
    { n: "08", file: "08-when-it-goes-wrong.md", label: "08 · When it goes wrong",
      covers: "The gotcha catalogue, each symptom → cause → fix with the fix in code or in an exact command: a peer-dependency conflict blocking the update; a migration silently skipping a file it could not parse; a monorepo where one app is behind and the update refuses; `npm install` already run so the lockfile is ahead of the source (the `--migrate-only --from` rescue); a migration that rewrote code you had customised; a global `@angular/cli` older than the project; the update succeeding and the build failing on a peer you did not notice. Exhaust it." },
  ],
},

"05-the-angular-build": {
  label: "05 · The build: `@angular/build`",
  tier: "understand", tierLabel: "Understand",
  prevTopic: "[`ng update`, not `npm install`](../04-ng-update-not-npm-install/README.md)",
  nextTopicSlug: "06-angular-json-anatomy",
  nextTopicLabel: "`angular.json` anatomy",
  thesis: "Angular's build is two engines with one config: **esbuild produces the output and Vite serves the dev server**. The Webpack builders still exist, still work, and are legacy — knowing which of the two a project is on is the first question to ask about any Angular repo you did not create.",
  scope: "⚠️ **Phase 14 owns performance, budgets and bundle analysis.** This topic owns what the build system IS and how it is put together. Name the Phase 14 topics rather than teaching them. `angular.json` itself is topic 06.",
  research: [
    "- The package landscape: `@angular/build` (22.1.7) current vs `@angular-devkit/build-angular` (legacy Webpack, still published). The builder strings for each: `@angular/build:application`, `@angular/build:dev-server`, `@angular/build:unit-test`, `@angular/build:extract-i18n` vs `@angular-devkit/build-angular:browser`, `:browser-esbuild`, `:server`, `:karma`. How to tell which one a project is on.",
    "- The two-engine story: esbuild produces the output; **Vite serves the dev server** — a dev-time server with HMR, not the production bundler. Why two tools.",
    "- The `application` builder as the single modern builder replacing `browser` + `server` + `prerender`. The `outputPath` object form; `browser`, `server`, `ssr`, `prerender`, `outputMode`.",
    "- End to end: TypeScript via `ngtsc` -> Angular instructions -> esbuild bundling -> optimization/minification -> the build-time **linker** step for partial-compiled libraries (forward-reference topic 10).",
    "- HMR in v22: template and style hot replacement, what is preserved, the flags to disable it.",
    "- Watch mode and incremental rebuilds; why the first build is slow.",
    "- `ng build` vs `ng serve` vs `ng build --watch`; where `ng serve` legitimately differs from a real build.",
    "- Migrating off the Webpack builders: what `ng update` handles and what it cannot (`@angular-builders/custom-webpack`, `ngx-build-plus`, a hand-written Webpack config).",
    "- Output layout: `dist/<project>/browser`, `/server`, what each file is, what you deploy.",
  ].join("\n"),
  chunks: [
    { n: "01", file: "01-the-package-landscape.md", label: "01 · The package landscape",
      covers: "`@angular/build` vs `@angular-devkit/build-angular`: what each is, which is current, and that the legacy one is still published and still works. The complete builder-string table for both, with what each target does. 🔴 Then the practical skill: given an unfamiliar repo, how to tell in ten seconds which build system it is on (read the `builder` field of the `build` target in `angular.json`, check which package is in `devDependencies`). Why the distinction matters before you change anything else." },
    { n: "02", file: "02-esbuild-and-vite-the-two-engines.md", label: "02 · esbuild and Vite, the two engines",
      covers: "The two-engine story, precisely. esbuild produces the production output. Vite serves the **dev server** — and it is a dev-time server with HMR, not the production bundler; Vite's own Rollup production path is not what ships your app. Why Angular uses two tools rather than one, and what each is good at. 🔴 The consequence that bites: dev and production run different pipelines, which is the structural reason \"works in dev, breaks in prod\" exists at all — chunk 07 gives it full treatment." },
    { n: "03", file: "03-the-application-builder.md", label: "03 · The `application` builder",
      covers: "The single modern builder that replaced `browser` + `server` + `prerender`. Every option that shapes WHAT is produced: the `outputPath` object form and its fields, `browser`, `server`, `ssr`, `prerender`, `outputMode` and its values. Show a complete realistic `build` target for a browser-only app and one for an SSR app, and explain every line. Why one builder rather than three — and what changes for a project migrating from the three." },
    { n: "04", file: "04-what-the-build-does-end-to-end.md", label: "04 · What the build does, end to end",
      covers: "The pipeline as a sequence you can reason about: TypeScript compiled by `ngtsc` (the transformer from topic 01 chunk 13) emitting Angular instructions; esbuild bundling and code-splitting; optimization and minification; and 🔴 the build-time **linker** pass that turns partial-compiled library declarations into instructions at YOUR Angular version — forward-reference topic 10. Say which stage produces which error class, so a reader can locate a failure from its message." },
    { n: "05", file: "05-hmr-and-the-dev-server.md", label: "05 · HMR and the dev server",
      covers: "What hot module replacement replaces in v22 — templates and styles — what state is preserved across a replacement and what is not, and the flags that turn it off. Why a full reload sometimes happens anyway. What the dev server does that a production build does not (on-demand transformation, unbundled dependencies). `proxyConfig` named and handed to topic 06. Gotchas: a change that needs a restart; HMR appearing to work while a stale template is served." },
    { n: "06", file: "06-watch-mode-and-incremental-rebuilds.md", label: "06 · Watch mode and incremental rebuilds",
      covers: "Why the first build is slow and later ones are not: what is cached, what invalidates it, and how locality (topic 01 chunk 12) is what makes an incremental Angular rebuild possible at all. `ng build --watch` and where the cache lives. What forces a full rebuild. The honest limits — a change to a widely-imported file is not incremental in any useful sense." },
    { n: "07", file: "07-ng-build-vs-ng-serve-vs-watch.md", label: "07 · `ng build` vs `ng serve` vs `ng build --watch`",
      covers: "Three things people conflate, separated: what each produces, where it puts it, and which pipeline it runs. 🔴 Then the payoff: every legitimate way `ng serve` differs from a real build — different optimization, different source maps, a different server, no output on disk by default — and therefore the concrete list of bugs that only appear in production. Give \"works in dev, breaks in prod\" full symptom → cause → fix treatment with the reproduce-locally recipe (`ng build` then serve the output statically)." },
    { n: "08", file: "08-migrating-off-the-webpack-builders.md", label: "08 · Migrating off the Webpack builders",
      covers: "What `ng update` handles automatically and what it cannot. The three hard cases, each with the honest answer: a hand-written Webpack config; `@angular-builders/custom-webpack`; `ngx-build-plus`. What you actually lose by staying on the legacy builder, and what you gain by moving. `:browser-esbuild` as the intermediate step. When staying is the correct engineering decision — say so plainly rather than recommending a migration unconditionally." },
    { n: "09", file: "09-what-lands-in-dist-and-build-gotchas.md", label: "09 · What lands in `dist/`, and the gotchas",
      covers: "The output layout: `dist/<project>/browser`, `dist/<project>/server`, what each file is, which of them you actually deploy and which are build artefacts. Hashed filenames and why. Then the gotcha catalogue, each symptom → cause → fix with the fix in code or config: a CommonJS dependency warning; a library that only works under Webpack; `allowedHosts` blocking a request; a polyfill that was implicit under Webpack and is not now; source maps absent in production by default; an asset present in dev and 404 in production; `outputPath` changed and the deploy still serving the old path. Exhaust it." },
  ],
},

"06-angular-json-anatomy": {
  label: "06 · `angular.json` anatomy",
  tier: "understand", tierLabel: "Understand",
  prevTopic: "[The build: `@angular/build`](../05-the-angular-build/README.md)",
  nextTopicSlug: "07-the-typescript-setup",
  nextTopicLabel: "The TypeScript setup Angular requires",
  thesis: "`angular.json` is one hierarchy repeated: **workspace → projects → targets → builders → options → configurations**. Most of the file is scaffolding you set once and never touch; a small, knowable set of fields is where every real decision lives, and knowing which is which is the difference between editing it confidently and editing it by search-and-hope.",
  scope: "⚠️ **The build system itself is topic 05; budgets as a performance practice are Phase 14.** This topic owns the CONFIG FILE — its structure, every field that matters, and changing it safely.",
  research: [
    "- Top-level shape: `$schema`, `version`, `newProjectRoot`, `projects`, `cli`, `schematics`. That the schema is real and editors complete against it.",
    "- The hierarchy: projects -> targets (`build`, `serve`, `test`, `extract-i18n`, `lint`) -> a builder with `options` -> `configurations` as named option overlays merged over the base.",
    "- Every `@angular/build:application` option worth knowing: entry/output (`browser`, `outputPath` object form, `index`, `tsConfig`, `assets`, `styles`, `scripts`, `polyfills`) and behaviour (`server`, `ssr`, `prerender`, `outputMode`, `sourceMap`, `optimization` and its object form, `extractLicenses`, `namedChunks`, `define`, `loader`, `externalDependencies`, `stylePreprocessorOptions`, `fileReplacements`, `budgets`).",
    "- `configurations` and `defaultConfiguration`: how `production`/`development` compose, how a configuration merges, and 🔴 where it REPLACES rather than merges (the array trap).",
    "- `fileReplacements` and the modern alternatives (`define`, a runtime-fetched config, an injected `InjectionToken`).",
    "- `budgets`: entry shape (`type`, `name`, `maximumWarning`, `maximumError`, `baseline`), the types (`initial`, `allScript`, `anyComponentStyle`, `any`, `bundle`, `allComponentStyles`), and the v22 defaults from the generated workspace.",
    "- `assets` in v22: the `public/` convention and glob objects (`glob`, `input`, `output`, `ignore`), copying from `node_modules`.",
    "- `serve` and `test` targets; `proxyConfig`.",
    "- Multi-project workspaces and libraries.",
  ].join("\n"),
  chunks: [
    { n: "01", file: "01-the-top-level-shape.md", label: "01 · The top-level shape",
      covers: "Every top-level key with what it is for and whether you ever change it: `$schema` (and that editor completion is real and worth using), `version`, `newProjectRoot`, `projects`, `cli`, `schematics`. Show a complete minimal `angular.json` for a single-app workspace and annotate every line. Then state the reading strategy for a big one: find the project, find the target, find the builder — everything else is noise until you need it." },
    { n: "02", file: "02-projects-targets-builders-options.md", label: "02 · Projects, targets, builders, options",
      covers: "The hierarchy that the rest of the file is made of, taught once so the reader can navigate any workspace. A workspace has projects; a project has `projectType`, `root`, `sourceRoot`, `prefix` and `architect`/`targets`; a target names a **builder** string and gives it `options`; `configurations` are named overlays. Show the path from `ng build` on the command line to the exact object that configures it. Explain what a builder IS (an executable contract, not a script) and that third-party builders exist." },
    { n: "03", file: "03-application-options-entry-and-output.md", label: "03 · `application` options: entry and output",
      covers: "The options that decide what goes in and what comes out, each with a realistic value and what breaks when it is wrong: `browser`, `index` (and its object form), `outputPath` in both the string and object forms, `tsConfig`, `polyfills`, `assets`, `styles`, `scripts`. For `assets` and `styles`, show the object forms as well as the shorthand. Say which of these you set once at scaffold and which you genuinely revisit." },
    { n: "04", file: "04-application-options-behaviour.md", label: "04 · `application` options: behaviour",
      covers: "The options that change HOW the build runs: `optimization` (boolean and object forms, and what each sub-field does), `sourceMap` (and its object form — why `hidden` exists), `extractLicenses`, `namedChunks`, `define`, `loader`, `externalDependencies`, `stylePreprocessorOptions`, `server`, `ssr`, `prerender`, `outputMode`. Each with what turning it on costs. Cover `define` properly — it is the modern answer to build-time constants and chunk 06 depends on it." },
    { n: "05", file: "05-configurations-and-defaultconfiguration.md", label: "05 · `configurations` and `defaultConfiguration`",
      covers: "How `production` and `development` compose over the base `options`. 🔴 The merge rule, precisely: scalar options are overridden, and an ARRAY option in a configuration **replaces** the base array rather than appending to it — the single most surprising behaviour in this file. Show the trap and the fix. Adding a third environment (`staging`) properly, including its `serve` and `build` sides. `defaultConfiguration` and the gotcha of `ng build` silently being a production build." },
    { n: "06", file: "06-filereplacements-and-the-alternatives.md", label: "06 · `fileReplacements`, and what to use instead",
      covers: "The mechanism, honestly. `environment.ts` -> `environment.prod.ts`, how the swap happens at build time, and why it works. Then the modern take: it is a build-time-only mechanism, it multiplies files per environment, and it cannot express anything decided after the build. Show at least two alternatives PROPERLY in code — `define` for a build-time constant, and a runtime-fetched config or an injected `InjectionToken` for anything else — and say when `fileReplacements` is still the right answer." },
    { n: "07", file: "07-budgets.md", label: "07 · `budgets`",
      covers: "The entry shape (`type`, `name`, `maximumWarning`, `maximumError`, `minimumWarning`, `minimumError`, `baseline`), every `type` value with what it measures (`initial`, `allScript`, `anyScript`, `anyComponentStyle`, `allComponentStyles`, `any`, `bundle`), and the v22 generated defaults. What a failing budget means in CI and why it fails there and not locally. How to set one that is useful rather than decorative. ⚠️ Then hand performance work itself to Phase 14 by name." },
    { n: "08", file: "08-assets-in-v22.md", label: "08 · `assets` in v22",
      covers: "The `public/` convention the v22 scaffold uses and what changed from the older `src/assets` arrangement. The glob object form (`glob`, `input`, `output`, `ignore`) with worked examples, including copying a file out of `node_modules` — the case everyone hits and gets wrong. Why an asset resolves in dev and 404s in production, with the fix. The interaction with `baseHref` and deploying under a sub-path." },
    { n: "09", file: "09-serve-and-test-targets.md", label: "09 · The `serve` and `test` targets",
      covers: "The `serve` target: `buildTarget`, `port`, `host`, `open`, `ssl`, `allowedHosts`, and 🔴 `proxyConfig` — every real app needs it on day one, so show a complete `proxy.conf.json` including a rewrite and a non-localhost target, and explain each field. The `test` target in v22: `@angular/build:unit-test`, the vitest peer at `^4.0.8`, and that `karma@^6.4.0` remains a peer but is end of life. ⚠️ Testing itself is Phase 13 — name it." },
    { n: "10", file: "10-what-you-never-touch-and-multi-project.md", label: "10 · What you never touch, and multi-project workspaces",
      covers: "The fields you set at creation and leave alone: `$schema`, `version`, `newProjectRoot`, `projectType`, `sourceRoot`, `prefix` — and what actually breaks if you change each one after the fact. Then multi-project workspaces: a second application, a library, `ng-packagr`'s target, path mapping between them, and where `angular.json` genuinely gets complicated. `cli.schematicCollections` and `schematics` defaults — setting them once instead of passing flags forever." },
    { n: "11", file: "11-angular-json-gotchas.md", label: "11 · `angular.json` gotchas",
      covers: "The catalogue, each symptom → cause → fix with the fix shown as real JSON: editing it and the dev server not picking the change up (it needs a restart); an array option in a configuration replacing rather than appending; a budget failing only in CI; an asset 404 in production but not dev; `defaultConfiguration` making `ng build` production without your noticing; a `styles` entry ordering problem; a `tsConfig` pointing at the wrong file so the app builds with the spec config; a path alias that works in the editor and not in the build; a second project added by hand with a missing target. Exhaust it." },
  ],
},

"07-the-typescript-setup": {
  label: "07 · The TypeScript setup Angular requires",
  tier: "understand", tierLabel: "Understand",
  prevTopic: "[`angular.json` anatomy](../06-angular-json-anatomy/README.md)",
  nextTopicSlug: "08-what-ng-new-produces",
  nextTopicLabel: "What `ng new` produces in v22",
  thesis: "Angular pins TypeScript to a single minor range — **`>=6.0 <6.1`** — and that one line explains most of what is unusual about an Angular project's TypeScript setup. The compiler is a TypeScript transformer built against TypeScript's own compiler API, so the version is not yours to choose; and because the template is a separate language, half the type checking you get is configured under `angularCompilerOptions` rather than `compilerOptions`.",
  scope: "⚠️ Topic 01 chunk 13 explains WHY `ngtsc` forces the pin and chunk 14 explains what `strictTemplates` checks. This topic owns the CONFIG: the three files, every option, and adopting strictness in a real codebase. Cross-reference, do not duplicate.",
  research: [
    "- The hard peer pin `typescript: >=6.0 <6.1` in `@angular/compiler-cli` 22.1.5. The exact error on a wrong version. Why you cannot upgrade TypeScript ahead of Angular.",
    "- The three-file split: `tsconfig.json` (shared base), `tsconfig.app.json` (`files`/`include` for the app), `tsconfig.spec.json` (test `types`). Why it exists — different file sets and different `types`, so a spec-only import cannot leak into the app build.",
    "- `angularCompilerOptions` in FULL: `strictTemplates` and everything it turns on (`strictInputTypes`, `strictNullInputTypes`, `strictAttributeTypes`, `strictDomLocalRefTypes`, `strictOutputEventTypes`, `strictDomEventTypes`, `strictContextGenerics`, `strictLiteralTypes`, `strictSafeNavigationTypes`), plus `strictInjectionParameters`, `strictStandalone`, `typeCheckHostBindings`, `extendedDiagnostics` (`checks`, `defaultCategory`), `enableI18nLegacyMessageIdFormat`, `disableTypeScriptVersionCheck` if it exists. Verify each against the compiler-options reference.",
    "- The TS `strict` family Angular turns on by default in a generated workspace: `strict`, `noImplicitOverride`, `noPropertyAccessFromIndexSignature`, `noImplicitReturns`, `noFallthroughCasesInSwitch`, `isolatedModules`.",
    "- `\"moduleResolution\": \"bundler\"` and `\"module\": \"preserve\"` in v22 — confirm from the generated tsconfig.",
    "- Path aliases: where they must be declared for the build AND the IDE to agree.",
  ].join("\n"),
  chunks: [
    { n: "01", file: "01-the-hard-peer-pin.md", label: "01 · The hard peer pin",
      covers: "`typescript: >=6.0 <6.1` — quote the peer range from `@angular/compiler-cli` 22.1.5. Why Angular pins a single minor when almost nothing else does: `ngtsc` is a TypeScript transformer built against TypeScript's compiler API, including parts that are not covered by TypeScript's own stability guarantees (cross-reference topic 01 chunk 13). Show the exact failure on a version outside the range. 🔴 The rule that follows: you never upgrade TypeScript ahead of Angular — `ng update` moves it for you. What to do when a different tool in the repo demands a newer TypeScript." },
    { n: "02", file: "02-the-three-file-tsconfig-split.md", label: "02 · The three-file tsconfig split",
      covers: "`tsconfig.json` as the shared base, `tsconfig.app.json` and `tsconfig.spec.json` extending it. Show all three from a v22 workspace and explain every line. 🔴 Why the split exists rather than one file: different `files`/`include` sets and different `types`, so a test-only global (or a spec-only import) cannot leak into the app build. What each build target points at, and the failure when one points at the wrong file. Where to add a compiler option so it applies to both — and the cases where it must go in only one." },
    { n: "03", file: "03-stricttemplates-and-what-it-implies.md", label: "03 · `strictTemplates` and what it implies",
      covers: "The umbrella flag and every individual flag it turns on, one row each with what it catches and a code example of the error it produces: `strictInputTypes`, `strictNullInputTypes`, `strictAttributeTypes`, `strictDomLocalRefTypes`, `strictOutputEventTypes`, `strictDomEventTypes`, `strictContextGenerics`, `strictLiteralTypes`, `strictSafeNavigationTypes`. The interaction with `strictNullChecks` — several of them are inert without it. How to turn the umbrella on and one sub-flag off. ⚠️ The MECHANISM (the Type Check Block) is topic 01 chunk 14 — cross-reference it, do not repeat it." },
    { n: "04", file: "04-the-other-angularcompileroptions.md", label: "04 · The other `angularCompilerOptions`",
      covers: "Everything under `angularCompilerOptions` that is not part of `strictTemplates`, each with what it does and when to change it: `strictInjectionParameters`, `strictStandalone`, `typeCheckHostBindings`, `extendedDiagnostics` with `checks` and `defaultCategory`, and any remaining option the v22 compiler-options reference documents. If an option you expected to find is not in the v22 reference, say so explicitly rather than documenting it from memory." },
    { n: "05", file: "05-the-typescript-strict-family.md", label: "05 · The TypeScript strict family Angular turns on",
      covers: "The options a generated v22 workspace enables that are NOT Angular-specific and that surprise people: `strict` and what it contains, `noImplicitOverride`, `noPropertyAccessFromIndexSignature`, `noImplicitReturns`, `noFallthroughCasesInSwitch`, `isolatedModules`. For each: why the Angular team turned it on, what it forbids, and the idiomatic rewrite. 🔴 `noPropertyAccessFromIndexSignature` gets its own worked example — it is the one that breaks `environment` and config-object access and sends people to Stack Overflow." },
    { n: "06", file: "06-module-resolution-and-path-aliases.md", label: "06 · Module resolution and path aliases",
      covers: "`\"moduleResolution\": \"bundler\"` and `\"module\": \"preserve\"` in a v22 workspace — what each means and why they changed. Then path aliases done properly: `baseUrl`/`paths`, which tsconfig they must live in for the build AND the editor to agree, and the interaction with `@angular/build`. 🔴 The classic failure — an alias that resolves in the IDE and fails in `ng build` — with the fix. Aliases into a library in a multi-project workspace." },
    { n: "07", file: "07-adopting-strictness-in-an-existing-app.md", label: "07 · Adopting strictness in an existing app",
      covers: "The realistic path for a codebase that was not written under these flags. Why turning `strictTemplates` on across a large app in one commit does not work. The staged approach: enable the umbrella with the noisiest sub-flags off, fix by area, tighten. Per-file and per-area escape hatches and their cost. How to make the work visible (a count that goes down). When NOT to do it. Then the same treatment for the TS strict family." },
    { n: "08", file: "08-typescript-setup-gotchas.md", label: "08 · TypeScript setup gotchas",
      covers: "Each symptom → cause → fix with the fix in code: a template error the IDE does not show but the build does (and what the Angular Language Service needs); `noPropertyAccessFromIndexSignature` breaking `environment` access; a path alias working in the editor and failing in `ng build`; a type imported into a spec that then fails the app build; a TypeScript upgrade done ahead of Angular; a `types` entry added to the wrong tsconfig; `isolatedModules` rejecting a re-export of a type; a third-party `.d.ts` that only compiles with `skipLibCheck`. Exhaust it." },
  ],
},

"08-what-ng-new-produces": {
  label: "08 · What `ng new` produces in v22",
  tier: "understand", tierLabel: "Understand",
  prevTopic: "[The TypeScript setup Angular requires](../07-the-typescript-setup/README.md)",
  nextTopicSlug: "09-the-release-train",
  nextTopicLabel: "The release train",
  thesis: "A v22 `ng new` app is about a dozen files, and every one of them is explicable. This is the orientation topic: the generated tree read file by file and line by line, including the v20+ naming change that drops the `.component` suffix — so that the first unfamiliar thing you meet in a real codebase is genuinely unfamiliar, not something the scaffold already answered.",
  scope: "⚠️ `angular.json` is topic 06 and the tsconfigs are topic 07 — name each file here and hand the detail to its own topic. This topic owns the TREE and the four source files.",
  research: [
    "- The `ng new` prompts in v22 and what each choice changes: routing, stylesheet format, SSR/SSG, zoneless, AI config files. Confirm the actual prompt list from the CLI docs rather than assuming.",
    "- The complete generated tree with every file named: `angular.json`, `package.json`, `tsconfig*.json`, `public/`, `src/main.ts`, `src/index.html`, `src/styles.css`, `src/app/app.ts`, `app.html`, `app.css`, `app.spec.ts`, `app.config.ts`, `app.routes.ts`; and with SSR: `app.config.server.ts`, `app.routes.server.ts`, `main.server.ts`, `server.ts`.",
    "- 🔴 The v20+ naming: generated files are `app.ts` / `app.html` / `app.css`, NOT `app.component.ts` — the `.component` suffix was dropped from generated names. Confirm and quote.",
    "- Every line of `main.ts`, `app.config.ts`, `app.routes.ts` and `app.ts`.",
    "- 🔴 The v22 `app.config.ts` contains NO zoneless call — zoneless is the default. Confirm what it DOES contain.",
    "- `ng generate component|service|directive|pipe` in v22 and the flags that matter.",
    "- The `schematics` defaults in `angular.json` and changing them once.",
    "- What `ng new` deliberately does NOT give you: a state library, a UI kit, an HTTP client provider until you add it, a linter until `ng add @angular/eslint`.",
  ].join("\n"),
  chunks: [
    { n: "01", file: "01-the-ng-new-prompts.md", label: "01 · The `ng new` prompts",
      covers: "Each prompt v22 asks, what each answer changes in the generated output, and which are hard to reverse later. Routing, stylesheet format, SSR/SSG, zoneless, and the AI-config-file question. The equivalent flags for a non-interactive run. What a CI or scripted scaffold should pass. If a prompt cannot be confirmed from the v22 CLI documentation, say so rather than listing it from memory." },
    { n: "02", file: "02-the-generated-tree.md", label: "02 · The generated tree, file by file",
      covers: "Every file and directory a v22 `ng new` produces, each with one precise sentence on what it is for and a pointer to the topic that owns it. Root config (`angular.json` -> topic 06, `package.json`, `tsconfig*.json` -> topic 07, `.editorconfig`, `.gitignore`), `public/`, and everything under `src/`. 🔴 Then the v20+ naming change: generated component files are `app.ts` / `app.html` / `app.css`, not `app.component.ts` — quote the source for this and explain what it means when you read an older codebase." },
    { n: "03", file: "03-main-ts-line-by-line.md", label: "03 · `main.ts`, line by line",
      covers: "The whole file, every line explained: the imports, `bootstrapApplication(App, appConfig)`, and the `.catch()`. What the returned `Promise` resolves to and why the catch matters. What `main.ts` is NOT responsible for. Cross-reference topic 02 chunk 01 for `bootstrapApplication` itself rather than repeating it, and say what a real app adds here (usually nothing — and why that is the point)." },
    { n: "04", file: "04-app-config-ts-line-by-line.md", label: "04 · `app.config.ts`, line by line",
      covers: "The generated file in full. 🔴 Lead with what is NOT there: no `provideZonelessChangeDetection()` call, because zoneless is the DEFAULT in v22 — this is the single most likely thing for a reader to get wrong from older material. Then every line that IS there, including `provideBrowserGlobalErrorListeners()` and `provideRouter(routes)` if generated. What a real app adds on day one (`provideHttpClient()` being the first). Cross-reference topic 03 for the provider array itself." },
    { n: "05", file: "05-app-routes-and-app-component.md", label: "05 · `app.routes.ts` and the root component",
      covers: "`app.routes.ts`: the empty `Routes` array, why routes are a value in their own file, and the first route a real app adds. Then `app.ts` / `app.html` / `app.css`: the `@Component` decorator field by field as generated, `signal` in the generated class, the template's `router-outlet`, and what the generated markup is there to demonstrate. What you delete first in a real project." },
    { n: "06", file: "06-the-ssr-files.md", label: "06 · The SSR files, when you choose them",
      covers: "What choosing SSR adds: `app.config.server.ts`, `app.routes.server.ts`, `main.server.ts`, `server.ts` — each explained, plus the `angular.json` changes and the extra `dist/` output. `mergeApplicationConfig` named and handed to topic 03 chunk 17. `RenderMode` and the server route table at the level of \"what this file decides\". ⚠️ Scope: Phase 12 owns SSR — this chunk owns only what the scaffold produces." },
    { n: "07", file: "07-ng-generate-in-v22.md", label: "07 · `ng generate` in v22",
      covers: "`ng generate component|service|directive|pipe|guard|interceptor|resolver` and what each produces in v22, with the naming convention applied. The flags that actually matter (`--skip-tests`, `--inline-template`, `--inline-style`, `--flat`, `--prefix`, `--type`). Where the generated file lands and how that is decided. What `ng generate` will not do for you." },
    { n: "08", file: "08-schematics-defaults.md", label: "08 · Setting the schematics defaults once",
      covers: "The `schematics` block in `angular.json`: setting your team's defaults once instead of passing the same flags forever. Show a realistic block that turns off inline styles, sets a prefix and skips tests for a specific type, and explain the key format (`@schematics/angular:component`). `cli.schematicCollections` for a third-party collection. Where these interact with topic 06's `angular.json` structure." },
    { n: "09", file: "09-what-ng-new-does-not-give-you.md", label: "09 · What `ng new` does not give you",
      covers: "The honest list, each with what to reach for and when: no state library; no UI kit; no HTTP client provider until you add `provideHttpClient()`; no linter until `ng add @angular/eslint`; no formatter config; no environment files by default; no auth. Then the day-one checklist a real project actually runs, in order, with the command for each. Close by naming which later phase owns each addition — this is the reader's map out of Phase 0." },
  ],
},

"09-the-release-train": {
  label: "09 · The release train",
  tier: "understand", tierLabel: "Understand",
  prevTopic: "[What `ng new` produces in v22](../08-what-ng-new-produces/README.md)",
  nextTopicSlug: "10-partial-compilation",
  nextTopicLabel: "Partial compilation",
  thesis: "Angular ships a major every six months and supports each for eighteen — **6 months active plus 12 months LTS**. That single rhythm decides when you upgrade, how far behind you are allowed to fall, and why an API deprecated today is still there two majors from now. A team that upgrades reactively is always out of support; one that upgrades on the cadence is never more than one major behind.",
  scope: "⚠️ Topic 04 owns `ng update` mechanics and Phase 15 owns full migrations. This topic owns the CALENDAR and how to read a changelog.",
  research: [
    "- The cadence: a major every six months (May/June and November), minors roughly monthly, patches weekly. Confirm from the Angular versioning-and-releases doc and quote it.",
    "- **6 months active + 12 months LTS = 18 months per major.** What 'active' entitles you to vs LTS (features and fixes vs critical fixes and security only).",
    "- The concrete current picture: v22 current since 3 Jun 2026, `latest` 22.1.5 (2026-09-03); v21 LTS to ~May 2027 at 21.2.22; v20 LTS ending ~Nov 2026 at 20.3.30; v19 out of support since 2 Jun 2026, final patch 19.2.25; v23 due ~Nov 2026. Say plainly these are as of 2026-09 and name where to re-check.",
    "- 🔴 The deprecation policy: an API deprecated in v(N) is removed no earlier than v(N+2). Quote it. This is exactly why skipping a major hurts.",
    "- How to read a changelog for what matters to YOU: the BREAKING CHANGES section, migration notes, filtering by the packages you use, and the difference between a breaking change with an automated migration and one without.",
    "- Node, TypeScript and RxJS support moving with the train.",
  ].join("\n"),
  chunks: [
    { n: "01", file: "01-the-cadence.md", label: "01 · The cadence",
      covers: "Majors every six months (May/June and November), minors roughly monthly, patches weekly — quoted from the versioning documentation. What Angular's semver actually promises at each level, and what it explicitly does not (the `ɵ`-prefixed private surface, and why a `ɵ` symbol in your code is a time bomb). The release-candidate and `next` tags and who should be on them. The concrete v22 dates as of 2026-09." },
    { n: "02", file: "02-active-support-lts-and-the-current-picture.md", label: "02 · Active support, LTS, and the current picture",
      covers: "**6 months active + 12 months LTS = 18 months.** What each window entitles you to: features and fixes vs critical fixes and security only. Then the concrete table as of 2026-09 — v22 current at 22.1.5, v21 LTS to ~May 2027 at 21.2.22, v20 LTS ending ~Nov 2026 at 20.3.30, v19 out of support since 2 Jun 2026 at 19.2.25, v23 due ~Nov 2026 — with an explicit note that this is a dated snapshot and where to re-check it. 🔴 The arithmetic that matters: an app upgraded once a year is always one major behind; one upgraded every 18 months is out of support on arrival." },
    { n: "03", file: "03-the-deprecation-policy.md", label: "03 · The deprecation policy",
      covers: "🔴 An API deprecated in v(N) is removed no earlier than v(N+2) — quote it. Work through what that means with a real v22 deprecation (`withFetch`, `provideAnimationsAsync`, `withIncrementalHydration`): when it was deprecated, when it can be removed, and what you should do now. Then the consequence that ties this topic to topic 04: two majors is exactly the window in which deprecated things vanish, so a two-major jump lands you in a version where the migration for the removal no longer ships." },
    { n: "04", file: "04-how-to-read-a-changelog.md", label: "04 · How to read a changelog for what matters to you",
      covers: "The practical skill, taught as a procedure. Where the changelog and release notes live. Reading the BREAKING CHANGES section first and filtering it by the packages you actually depend on. Distinguishing a breaking change WITH an automated migration from one without — the second is your work, the first is not. Spotting a deprecation notice for something you use. Reading a minor's notes for a new API worth adopting. What the commit-scope prefixes tell you." },
    { n: "05", file: "05-the-peers-move-with-the-train.md", label: "05 · Node, TypeScript and RxJS move with the train",
      covers: "Each major moves its peer floors, and that is often what actually blocks an upgrade. Node engines `^22.22.3 || ^24.15.0 || >=26.0.0` for v22; the TypeScript `>=6.0 <6.1` pin; the RxJS `^6.5.3 || ^7.4.0` range with no RxJS 8 support; zone.js optional. How to check a major's peers BEFORE you start. The order to upgrade the environment in, and the case where your CI runner's Node is the real blocker." },
    { n: "06", file: "06-planning-upgrades-on-a-cadence.md", label: "06 · Planning upgrades on a cadence",
      covers: "Turning the calendar into a policy. Upgrading every six months versus every twelve versus reactively, with the honest cost of each. Day-one adoption versus waiting for the `.1` — what actually differs, and which teams should do which. Budgeting the work: what a routine one-major upgrade costs versus a rescue. What to automate and what to schedule. Close with the decision rule a team can adopt without further research." },
  ],
},

"10-partial-compilation": {
  label: "10 · Partial compilation",
  tier: "know", tierLabel: "Know",
  prevTopic: "[The release train](../09-the-release-train/README.md)",
  nextTopicSlug: "11-jit-vs-aot",
  nextTopicLabel: "JIT vs AOT",
  thesis: "A library compiled to finished Ivy instructions would be locked to one Angular version's private instruction set, so npm would need one build of every library per Angular major. **Partial compilation** is the answer: a library publishes stable, declarative `ɵɵngDeclare*` calls, and the **linker** turns them into instructions at application build time, at your Angular version.",
  scope: "⚠️ Topic 01 chunk 12 owns locality and chunk 13 owns where the compiler runs. This topic owns the LIBRARY publishing model and the linker.",
  research: [
    "- The problem: instructions are a private, version-specific surface. What would happen without partial compilation.",
    "- The partial output: `ɵɵngDeclareComponent`, `ɵɵngDeclareDirective`, `ɵɵngDeclareInjectable`, `ɵɵngDeclareFactory`, `ɵɵngDeclareNgModule`, `ɵɵngDeclarePipe`, `ɵɵngDeclareClassMetadata` — a stable, declarative, version-independent description.",
    "- The linker: `@angular/compiler-cli/linker` and the babel plugin `@angular/compiler-cli/linker/babel`, run at APPLICATION build time using the application's Angular version.",
    "- `compilationMode: \"partial\"` in a library's `tsconfig.lib.prod.json`, and why `ng-packagr` sets it.",
    "- The Angular Package Format and where partial output sits in it.",
    "- What this means when you debug into `node_modules`.",
    "- Why a library must not be published in full compilation mode, and the symptom when someone does.",
    "- 🔴 The monorepo consequence: a locally-built library is usually FULL-compiled and a published one is not, so behaviour can differ between `ng build my-lib` and the npm tarball.",
  ].join("\n"),
  chunks: [
    { n: "01", file: "01-the-problem-it-solves.md", label: "01 · The problem it solves",
      covers: "Instructions (`ɵɵelementStart`, `ɵɵproperty`, …) are a **private** surface that changes between Angular versions — cross-reference topic 01 chunk 06. So a library compiled to finished instructions is compiled against one Angular version's private API. Work through the consequence properly: npm would need one build of every Angular library per major, or every library would break on every upgrade. State the constraint that any solution had to satisfy, so the next chunk's design reads as inevitable rather than arbitrary." },
    { n: "02", file: "02-what-partial-output-looks-like.md", label: "02 · What partial output looks like",
      covers: "The `ɵɵngDeclare*` family — `ɵɵngDeclareComponent`, `ɵɵngDeclareDirective`, `ɵɵngDeclareInjectable`, `ɵɵngDeclareFactory`, `ɵɵngDeclareNgModule`, `ɵɵngDeclarePipe`, `ɵɵngDeclareClassMetadata` — and what makes them different from instructions: they are DECLARATIVE (a description of what the component is) rather than IMPERATIVE (how to render it), they are versioned, and they are a public-enough contract to be stable across majors. Show the shape of a declaration and say plainly it is illustrative. Explain the `minVersion` / `version` fields and what they are for." },
    { n: "03", file: "03-the-linker.md", label: "03 · The linker",
      covers: "`@angular/compiler-cli/linker` and the babel plugin `@angular/compiler-cli/linker/babel`. When it runs — at APPLICATION build time, as part of the pipeline in topic 05 chunk 04 — and what it does: reads each `ɵɵngDeclare*` call and emits the instructions for the Angular version YOUR app is on. Why that is what makes one published build work across majors. Where it sits relative to esbuild. What it costs (build time on every dependency that ships partial output) and what caching exists." },
    { n: "04", file: "04-how-a-library-is-built-this-way.md", label: "04 · How a library is built this way",
      covers: "`compilationMode: \"partial\"` in `tsconfig.lib.prod.json`, why `ng-packagr` sets it for you, and what the three compilation modes are. The Angular Package Format and where partial output sits within it — the entry points, the `exports` map, the `.d.ts` with its `ɵɵComponentDeclaration` types (cross-reference topic 01 chunk 06). What a library author needs to do, which is mostly nothing, and the one decision that is theirs." },
    { n: "05", file: "05-what-this-means-in-practice.md", label: "05 · What this means in practice",
      covers: "The consequences a reader actually meets, each symptom → cause → fix: unfamiliar `ɵɵngDeclare*` calls when you debug into `node_modules` and what to read them as; a library published in FULL compilation mode and the version-mismatch symptom that follows; 🔴 the monorepo trap — a locally-built library is usually full-compiled while the published tarball is partial, so `ng build my-lib` and the npm install can behave differently, and how to test the real artefact; a library whose `minVersion` is above your Angular; a linker error naming a file you did not write. Exhaust it." },
  ],
},

"11-jit-vs-aot": {
  label: "11 · JIT vs AOT",
  tier: "know", tierLabel: "Know",
  prevTopic: "[Partial compilation](../10-partial-compilation/README.md)",
  nextTopicSlug: "12-dev-mode-only-behaviour",
  nextTopicLabel: "Dev-mode-only behaviour",
  thesis: "**AOT is the only supported deployment mode and has been the default since v9** — `aot: false` is not a production choice, it is a broken one. JIT still exists, but it lives in `TestBed` and a deprecated dynamic-platform package, and the honest framing of this topic in 2026 is that it is mostly an interview question with one real production consequence: Content Security Policy.",
  scope: "⚠️ Topic 01 owns the compiler itself. This topic owns WHEN it runs and why one of the two answers is not a deployment option.",
  research: [
    "- What each means: compile templates in the browser at runtime vs at build time.",
    "- AOT the default since v9 and the only supported deployment mode.",
    "- What shipping JIT would cost: the compiler in your bundle, a runtime compile step before first render, no template type checking, and a CSP problem because JIT needs `eval`-like behaviour.",
    "- Where JIT genuinely still lives: `TestBed` compiling components declared in a spec; `@angular/platform-browser-dynamic` (still published at 22.1.5, deprecated since v20 — confirm the deprecation); dynamic template scenarios.",
    "- Why `TestBed` being JIT explains some test-only behaviour differences.",
    "- 🔴 The CSP angle — the one place this still bites in production. Confirm what a v22 AOT app actually needs in a CSP and what it does not.",
    "- Whether `aot` is still a settable option in v22's `application` builder, or whether it has been removed. Verify; do not assume.",
  ].join("\n"),
  chunks: [
    { n: "01", file: "01-what-each-means.md", label: "01 · What each means",
      covers: "Just-in-time: templates compiled in the browser, at runtime, by a compiler shipped in your bundle. Ahead-of-time: templates compiled at build time by `ngtsc`, and no compiler shipped. Where each sits in the pipeline from topic 05 chunk 04. The history in one paragraph — AOT the default since v9 — labelled as history. 🔴 Then verify and state whether `aot` is still a settable builder option in v22 at all, and say plainly if the documentation does not settle it." },
    { n: "02", file: "02-why-jit-is-not-a-deployment-option.md", label: "02 · Why JIT is not a deployment option",
      covers: "Four costs, each argued: the compiler is in your bundle; there is a runtime compile step before the first render; there is no template type checking, so every error in topic 01 chunk 14 becomes a runtime error; and 🔴 JIT needs `eval`-like behaviour, which a strict Content Security Policy forbids. Then the conclusion stated plainly: `aot: false` is not a performance trade-off you can choose to make." },
    { n: "03", file: "03-where-jit-still-lives.md", label: "03 · Where JIT still lives",
      covers: "`TestBed` compiling components declared in a spec — and why that means your tests run a different compilation path from your app, with the concrete behaviour differences that follow (cross-reference topic 02 chunk 11's `DynamicTestModule`). `@angular/platform-browser-dynamic`, still published at 22.1.5 and deprecated since v20 — say precisely what its status is. Genuinely dynamic template scenarios and what to reach for instead in v22. ⚠️ Testing itself is Phase 13." },
    { n: "04", file: "04-the-csp-angle-and-the-interview-answer.md", label: "04 · The CSP angle, and the interview answer",
      covers: "🔴 The one place this still bites in production: Content Security Policy. What a v22 AOT app actually needs in a CSP and what it does not — verify this and say so where the documentation does not settle it. `unsafe-eval` and why an AOT app should never need it, so needing it is a signal something is compiling at runtime. Styles and nonces named at the level this topic owns. Then the honest close: this is now largely a historical interview question — say so, and give the answer an interviewer is looking for anyway, in prose." },
  ],
},

"12-dev-mode-only-behaviour": {
  label: "12 · Dev-mode-only behaviour",
  tier: "know", tierLabel: "Know",
  prevTopic: "[JIT vs AOT](../11-jit-vs-aot/README.md)",
  nextTopicSlug: null,
  nextTopicLabel: null,
  thesis: "A development build runs checks a production build does not: assertions, `ng-reflect-*` attributes when you ask for them, and the change-detection verification pass that produces `ExpressionChangedAfterItHasBeenCheckedError`. The consequence is the one that costs a day — **a bug that only reproduces in production is often a dev-only check that was hiding it**, and a test that asserts on dev-only output is a test that lies.",
  scope: "⚠️ Phase 5 owns change detection itself. This topic owns WHAT DISAPPEARS between a dev build and a production one, and what to do about it.",
  research: [
    "- `isDevMode()` and what determines it in v22.",
    "- The `ngDevMode` global, the `ngDevMode && assertSomething()` pattern, and how it is tree-shaken out of a production build.",
    "- 🔴 `provideNgReflectAttributes()` — the v22 provider restoring the `ng-reflect-*` DOM attributes that used to appear automatically in dev builds. Confirm which version removed them by default, why, and why you must never assert on them in tests.",
    "- `provideBrowserGlobalErrorListeners()` and where it belongs (cross-reference topic 03 chunk 06).",
    "- The dev-only checks that vanish: the change-detection verification pass, the assertions in signals and change detection, `provideCheckNoChangesConfig` (developer preview).",
    "- `ExpressionChangedAfterItHasBeenCheckedError` — the canonical dev-only error. Its exact string, its causes, and its fixes. 🔴 Confirm whether and how it changes under zoneless, which is the DEFAULT in v22.",
    "- Angular DevTools and what it needs from a build.",
  ].join("\n"),
  chunks: [
    { n: "01", file: "01-isdevmode-and-ngdevmode.md", label: "01 · `isDevMode()` and `ngDevMode`",
      covers: "`isDevMode()`: what it returns, what determines it, and the legitimate uses (and the illegitimate one — branching application behaviour on it). Then `ngDevMode`, which is a different thing: a global the build defines, used in the `ngDevMode && assertSomething()` pattern so that the whole assertion is statically removable. Show the pattern and explain how the production build eliminates it. Why Angular's own source is full of it, and what that means when you read a stack trace from a production bundle." },
    { n: "02", file: "02-provideNgReflectAttributes.md", label: "02 · `provideNgReflectAttributes()`",
      covers: "The `ng-reflect-*` DOM attributes: what they were, why they appeared automatically in dev builds, and 🔴 which version removed them by default and why (confirm from the changelog — do not assume the version). `provideNgReflectAttributes()` as the v22 provider that brings them back for debugging. Then the rule that matters: **never assert on them in a test** — they are a debugging aid, they are absent in production, and a test that depends on them passes for the wrong reason. Show the wrong test and the right one." },
    { n: "03", file: "03-the-checks-that-vanish.md", label: "03 · The checks that vanish",
      covers: "The catalogue of what a production build stops doing: the change-detection verification pass, the assertions inside signals and change detection, template-related dev assertions, and `provideCheckNoChangesConfig` (developer preview — label it). For each, what it was protecting you from and what its absence means. 🔴 Then the load-bearing consequence, stated as a rule: a bug that only reproduces in production is often a dev-only check that was hiding it — with the diagnostic procedure that follows from that." },
    { n: "04", file: "04-expressionchangedafterithasbeenchecked.md", label: "04 · `ExpressionChangedAfterItHasBeenCheckedError`",
      covers: "The canonical dev-only error, given full symptom → cause → fix treatment and enough space to actually settle it. The exact error string. What the verification pass is doing and why it only runs in development. Every common cause with a code example and a working fix: a parent bound to a child value the child mutates; a value changed in a lifecycle hook after the check; an async change resolving synchronously; a shared service mutated during rendering. 🔴 Then confirm and state what changes under zoneless, which is the DEFAULT in v22 — this is the part every older write-up gets wrong. The fixes that are legitimate versus the ones that only silence it." },
    { n: "05", file: "05-error-listeners-and-devtools.md", label: "05 · Error listeners and Angular DevTools",
      covers: "`provideBrowserGlobalErrorListeners()`: what it listens to, what it does with what it catches, and how it relates to `ErrorHandler` — cross-reference topic 03 chunk 06 for the provider itself. Then Angular DevTools: what it needs from a build to be useful, why a production build tells it less, and what it can still show. What to enable when you must debug a production-like build, and what that costs." },
    { n: "06", file: "06-what-to-do-about-it.md", label: "06 · What to do about it",
      covers: "The closing chunk of the phase. The practical procedure for a production-only bug: reproduce against a production build locally (cross-reference topic 05 chunk 07), re-enable what you can, and read what is left. What belongs in a production build permanently (a real `ErrorHandler`, source maps you keep privately) and what must not. The rule for tests: assert on behaviour, never on dev-only output. Close by naming what Phase 1 picks up — this is the last page of Phase 0." },
  ],
},

}

// ─────────────────────────────────────────────────────────────────────────────

const slugs = Array.isArray(args) ? args : Object.keys(ALL)
const TOPICS = slugs.map((s) => {
  const t = ALL[s]
  if (!t) throw new Error("unknown topic slug: " + s)
  return Object.assign({ slug: s }, t)
})

const badge = (t) => "<span className=\"db-tier t-" + t.tier + "\">" + t.tierLabel + "</span>"

phase("Plan")
log("Angular Phase 0 — writing " + TOPICS.length + " topics from scratch: " +
    TOPICS.reduce((n, t) => n + t.chunks.length, 0) + " chunks plus " + TOPICS.length + " topic READMEs.")

const results = await pipeline(
  TOPICS,

  // ── Stage 1: research the topic once, then write its README ─────────────────
  (t) => agent([
    SHARED,
    "",
    "# YOUR JOB — research topic `" + t.slug + "` once, then write its `README.md`",
    "",
    "Topic: **" + t.label + "** — tier **" + t.tierLabel + "**, so the badge on every page of this topic is",
    "`" + badge(t) + "`.",
    "",
    "The directory `" + PDIR + "/" + t.slug + "/` **does not exist yet**. You create it by writing its README.",
    "",
    "## Part 1 — the research bank",
    "",
    "You are the only agent that researches this topic. **" + t.chunks.length + "** writer agents will then write one",
    "chunk each **from your bank**, so the topic is researched ONCE rather than " + t.chunks.length + " times. A thin bank",
    "costs " + t.chunks.length + " agents; an over-full one costs you. Be generous.",
    "",
    "### What to research",
    "",
    t.research,
    "",
    "### How",
    "",
    "- Fetch the PRIMARY sources: angular.dev, the Angular GitHub release notes and `CHANGELOG.md`, the",
    "  published package metadata, and the `angular/angular` source at tag `v22.1.5`. Not blogs.",
    "- For every load-bearing rule, guarantee, default, flag or deprecation, capture the sentence",
    "  **VERBATIM** with its URL. A paraphrase of a rule is where errors enter.",
    "- Where angular.dev and the v22.1.5 source DISAGREE, record both and note that the source wins.",
    "- Where you cannot settle a claim after one honest attempt, record it as UNSETTLED with what you tried.",
    "  Do not guess. The chunks will write \"the documentation does not state whether X\" and that is correct.",
    "- 🔴 Do NOT run any `ng` command, `npm install`, or a build. There is no sandbox. Read documentation.",
    "",
    "Write the bank to **`/mnt/Storage/my-learning/claude/devbible/research_angular_p0_" +
      t.slug.replace(/-/g, "_") + ".md`** with this frontmatter, organised by chunk number, plus a shared",
    "\"facts every chunk needs\" section at the top:",
    "",
    "```markdown",
    "---",
    "name: research-angular-p0-" + t.slug,
    "description: Banked primary-source research for Angular Phase 0 topic " + t.slug + " — verbatim quotes and URLs for every chunk.",
    "metadata:",
    "  type: reference",
    "---",
    "```",
    "",
    "## Part 2 — write `" + PDIR + "/" + t.slug + "/README.md`",
    "",
    "This is the topic index. Exact shape:",
    "",
    "```markdown",
    "---",
    "title: \"<a full sentence stating the topic's claim>\"",
    "sidebar_label: \"Overview\"",
    "sidebar_position: 0",
    "---",
    "",
    badge(t),
    "",
    "> Verified: 2026-09-06 against angular.dev — <the real sources you read, linked> …",
    "> Documentation-validated; **no sandbox run**.",
    "> Version spine: **Angular 22.1.5** · CLI / `@angular/build` / `@angular/ssr` **22.1.7** · TypeScript peer `>=6.0 <6.1`.",
    "",
    "**<bold thesis paragraph>**",
    "",
    "## Chunks",
    "",
    "## The one question this topic exists to answer",
    "",
    "## Phase gate",
    "",
    "## Where this connects",
    "```",
    "",
    "🔴 `sidebar_position: 0` and `sidebar_label: \"Overview\"` are mandatory — a topic README given the",
    "topic's own number sorts BELOW the chunks it indexes.",
    "",
    "**The thesis** — argue from, do not copy verbatim, this: *" + t.thesis + "*",
    "",
    "**Scope, and say it on the page:** " + t.scope,
    "",
    "**The `## Chunks` table** must have one row per chunk below, in order, as `| # | Chunk | Covers |`.",
    "🔴 Every chunk title is **bold text plus *(not written yet)***, NOT a link — none of them exist yet and",
    "a dangling link breaks the build for every other session in this shared checkout. A wire-up agent",
    "relinks them later. Add the line `🚧 **0 of " + t.chunks.length + " chunks written.**` above the table.",
    "",
    "The chunks, with the file each will live in:",
    "",
    t.chunks.map((c) => "- **" + c.n + "** — `" + c.file + "` — **" + c.label + "** — " + c.covers).join("\n\n"),
    "",
    "In `## Where this connects`, link ONLY files that exist right now — `ls` each one. The topics written",
    "so far are `01-compiler-with-a-framework-attached`, `02-standalone-by-default` and",
    "`03-the-provider-array`; check what else has appeared. Everything else is bold text plus",
    "*(not written yet)*.",
    "",
    "**The README footer:**",
    "",
    "```",
    "---",
    "",
    "← Prev: " + t.prevTopic + " · Index: [Phase 0 — How Angular runs](../README.md) · Start → **" + t.chunks[0].label + "** *(not written yet)*",
    "```",
    "",
    "🔴 `ls " + PDIR + "/` first and confirm the Prev target exists. If it does not, make Prev bold text plus",
    "*(not written yet)* instead — a dangling link is worse than a missing one.",
    "",
    QC.replace("<your file>", "README.md"),
    "",
    "Then bank the README (this creates the directory in git):",
    "",
    "```bash",
    "bash " + BANK + " '" + t.slug + "' 'topic " + t.slug.slice(0, 2) + " — topic README and the " + t.chunks.length + "-chunk plan'",
    "```",
    "",
    "Return the structured object. If researching changed your mind about a chunk's scope, put the better",
    "scope in the README's table and report it in `planChanges` — the writers are given the README to read.",
  ].join("\n"), { label: "plan:" + t.slug, phase: "Plan", schema: PLAN_SCHEMA }),

  // ── Stage 2: one writer per chunk ───────────────────────────────────────────
  (plan, t) => parallel(t.chunks.map((c, i) => () => agent([
    SHARED,
    "",
    "# YOUR JOB — write exactly ONE file",
    "",
    "**`" + PDIR + "/" + t.slug + "/" + c.file + "`**",
    "",
    "Topic: **" + t.label + "** — tier **" + t.tierLabel + "**. Your badge line is exactly:",
    "`" + badge(t) + "`",
    "",
    "This is chunk **" + c.n + "** of " + t.chunks.length + ".",
    "",
    "## 🔴 Read the research bank first — the topic was researched once, for you",
    "",
    "**`" + (plan && plan.bankPath ? plan.bankPath : "/mnt/Storage/my-learning/claude/devbible/research_angular_p0_" + t.slug.replace(/-/g, "_") + ".md") + "`**",
    "",
    "It carries verbatim primary-source quotes with URLs, organised by chunk number. Write from it. You may",
    "make **one or two** targeted fetches of your own for detail the bank does not cover — but do not",
    "re-research the topic; that cost has been paid.",
    "",
    "Also read **`" + PDIR + "/" + t.slug + "/README.md`** — the topic thesis, the scope boundary you must",
    "respect, and the agreed chunk table. Do not contradict it and do not repeat another chunk's material.",
    "",
    "## Frontmatter — exactly this",
    "",
    "```",
    "---",
    "title: \"<a full sentence stating this chunk's claim — not a label>\"",
    "sidebar_label: \"" + c.label + "\"",
    "sidebar_position: " + (i + 1),
    "---",
    "```",
    "",
    "## Scope boundary for this topic — state it on the page where it is relevant",
    "",
    t.scope,
    "",
    "## What this chunk must cover",
    "",
    c.covers,
    "",
    "That list is the **floor**, not the ceiling. If the subject has more gotchas, more failure modes or",
    "more interview questions than the list names, write them all. Exhaust it.",
    "",
    "## Your footer — emit exactly this line and nothing else",
    "",
    "```",
    "---",
    "",
    "← Prev: [Topic index](README.md) · Index: [Topic index](README.md) · Next → " +
      (i + 1 < t.chunks.length ? "**" + t.chunks[i + 1].label + "** *(not written yet)*" : "**the next topic** *(not written yet)*"),
    "```",
    "",
    "Your siblings are being written in parallel, so you cannot know what is on disk. A wire-up agent",
    "rewrites the whole chain once every chunk exists. **Do not link a sibling chunk file** — a dangling",
    "link breaks the build for every other session in this checkout. ⛔ Never emit `{/* FOOTER */}`.",
    "",
    QC,
    "",
    "Bank it with:",
    "",
    "```bash",
    "bash " + BANK + " '" + t.slug + "' 'topic " + t.slug.slice(0, 2) + " — chunk " + c.n + " (<your short subject>)'",
    "```",
    "",
    "## Report",
    "",
    "Return the structured object: the path, the final `wc -l`, how many gotchas and how many interview",
    "questions you wrote, whether the commit succeeded, **every claim you could not confirm and what you",
    "wrote instead**, and anything wrong OUTSIDE your file — found, not fixed.",
  ].join("\n"), { label: "write:" + t.slug.slice(0, 2) + "/" + c.n, phase: "Write", schema: CHUNK_SCHEMA }))),

  // ── Stage 3: wire the footer chain and the README ───────────────────────────
  (written, t) => agent([
    SHARED,
    "",
    "# YOUR JOB — wire up topic `" + t.slug + "`, now that every chunk exists",
    "",
    "Directory: **`" + PDIR + "/" + t.slug + "/`**",
    "",
    "Every chunk was written in parallel and carries a PLACEHOLDER footer whose `Next →` is bold text.",
    "Make the topic navigable and internally consistent. **You do not write new prose sections and you do",
    "not rewrite anyone's page body.**",
    "",
    "## 1. `ls` the directory first",
    "",
    "A chunk may have split into a lettered sibling (`05-x.md` + `05b-y.md`) or may be MISSING because a",
    "writer failed. Work from what is **actually on disk**, in `sidebar_position` order — never from a plan.",
    "Read every file's frontmatter for its real `sidebar_position` and `sidebar_label`.",
    "",
    "## 2. Rewrite every chunk's footer as a correct chain",
    "",
    "```",
    "← Prev: [<previous chunk's short label>](<previous file>.md) · Index: [Topic index](README.md) · Next → [<next chunk's short label>](<next file>.md)",
    "```",
    "",
    "- The **first** chunk's Prev is `[Topic index](README.md)`.",
    "- The **last** chunk's Next is the NEXT TOPIC: " +
      (t.nextTopicSlug
        ? "`[" + t.nextTopicLabel + "](../" + t.nextTopicSlug + "/README.md)` — but 🔴 `ls` that path first;" +
          " it is written by a parallel lane and may not exist yet. If it does not, use bold text plus *(not written yet)*."
        : "🔴 there is NO next topic — this is the last topic of Phase 0 and Phase 1 is unwritten. The last" +
          " chunk's footer is `← Prev: [<prev>](<prev>.md) · Index: [Topic index](README.md)` with no Next at all."),
    "- 🔴 `ls` every path before you write it as a link.",
    "",
    "## 3. Update `README.md`",
    "",
    "- Turn every chunk row in the `## Chunks` table from bold text into a **real link** to the file that",
    "  now exists, keeping the \"Covers\" text unless it is now factually wrong.",
    "- Add rows for any lettered siblings a writer created.",
    "- Replace `🚧 **0 of N chunks written.**` with the true state. If every chunk is written, drop the 🚧",
    "  line entirely — it is no longer true.",
    "- Change the footer's `Start →` from bold text to a real link to the first chunk.",
    "- In `## Where this connects`, promote any bold-text entry to a real link **only if that file now",
    "  exists** — `ls` it. Leave the rest.",
    "- Do **not** change the title, the tier badge, the `> Verified:` line, the thesis or the phase gate.",
    "",
    "## 4. QC the whole topic directory",
    "",
    "```bash",
    "cd " + PDIR + "/" + t.slug,
    "wc -l *.md                                          # nothing over 300",
    "grep -c '^\\*\\*★' *.md                               # every chunk has ★ entries",
    "grep -L '^<span className=\"db-tier' *.md            # must print nothing",
    "grep -L '^> Verified:' *.md                         # must print nothing",
    "grep -rn '{/\\* FOOTER \\*/}' .                       # 🔴 must be empty",
    "grep -rn '```console' .                             # must be empty",
    "grep -h '^sidebar_position:' *.md | sort -n | uniq -d   # must print nothing",
    "grep -H '^sidebar_\\(position\\|label\\):' README.md    # must be 0 and \"Overview\"",
    "python3 " + MDX + " --no-rawtag .",
    "```",
    "",
    "Then verify **every relative link in every file of this directory resolves**. Extract them and `ls`",
    "each. Fix the ones inside your own directory by demoting them to bold text plus *(not written yet)*;",
    "report the rest.",
    "",
    "🔴 If a `sidebar_position` is duplicated or a chunk is missing, **report it — do not renumber.**",
    "",
    "## 5. Bank it",
    "",
    "```bash",
    "bash " + BANK + " '" + t.slug + "' 'topic " + t.slug.slice(0, 2) + " — wire the footer chain and relink the README chunk table'",
    "```",
    "",
    "Return the structured object.",
  ].join("\n"), { label: "wire:" + t.slug, phase: "Wire", schema: WIRE_SCHEMA }),
)

return { topics: TOPICS.map((t) => t.slug), wire: results }
