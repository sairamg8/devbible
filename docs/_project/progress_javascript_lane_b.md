---
name: devbible-javascript-lane-b
description: Lane B of the two-way JavaScript split — phases 9-12, 17, 18. Resume point, scope and rules for the second session
metadata:
  type: progress
---

:::danger SUPERSEDED 2026-08-15 — JavaScript is now FOUR chunks, A B C D
The two-lane split this file describes is **closed**, and its letters are **dead**
("lane A = phases 3–8", "lane B = phases 9–12, 17, 18" mean nothing now). The live cursor is
[[devbible-javascript-split-4way]] — **A** phases 5+11 · **B** 6+17 · **C** 7+8 · **D** 12+18.
Read this file for traps, concepts and history only, never for "which phases are mine".
:::

:::danger CONSOLIDATED 2026-08-15 — THE WORKTREE IN THIS FILE NO LONGER EXISTS
Every devbible worktree and branch was **merged into `main` and DELETED** on 2026-08-15
(*"commit every uncommitted branch to main and delete everything"*). **All the content
described below is on `main`** at `/run/media/sairam/Storage/Backup/Knowledge/devbible`
— nothing was lost, every branch was verified at 0 unique commits first. Ignore any
"worktree", "branch", "not merged" or "merge at the phase close" instruction below and
**work on `main`**. `main` builds 0 warnings / 0 broken links, so a break there is yours.
Full record: `progress_worktree_consolidation_20260815.md`.
:::

**Lane B of the two-session JavaScript split**, set 2026-08-14 on the user's instruction
(*"i want to split your javascript work with another session so we can have less to work"*).

Lane A is [[devbible-javascript-build-progress]] — read that file for the shared rules; this
one only says what is different for lane B.

## Held by session `75e511e6` from 2026-08-14 (took over from `b4ffc223`)

## 🔴 Lane B now writes in a WORKTREE (2026-08-14)

On the user's instruction — *"Can you write now onwards all your explanations in complete new
worktree ?"* — every lane B page from **phase 9 topic 14 chunk 03 onward** is written in:

| | |
|---|---|
| **Path** | `/run/media/sairam/Storage/Backup/Knowledge/devbible-js-lane-b` |
| **Branch** | `js-lane-b`, branched from `main` at `d3761a8f` |
| **`node_modules`** | a **symlink** to `../devbible/node_modules` — no `yarn install` needed, and it must be recreated if the worktree is ever moved |
| **Build** | `rm -rf .docusaurus-laneb && DOCUSAURUS_GENERATED_FILES_DIR_NAME=.docusaurus-laneb yarn build --out-dir build-laneb` |

✅ **MERGED INTO `main` on 2026-08-15**, on the user's instruction (*"please merge to main"*).
A **fast-forward** to `60a4fc0c` — `main` was an ancestor, so there were no conflicts at merge time
— then `main` was rebuilt and came out **clean, 0 broken links site-wide**. `git rev-list --count
main..js-lane-b` is **0**: nothing is stranded. The branch and `main` are identical, and the
worktree stays in use for the remaining phases.

🔴 **Merge at every phase boundary from now on**, not at the end. The React lesson is that an
unmerged worktree is worse than none, and a fast-forward is only available while `main` has not
diverged.

**The upside, which is why it was asked for:** the shared checkout constantly carries other
sessions' uncommitted React/TypeScript edits, so a build there always reports breaks that are not
lane B's. The worktree build is clean or it is your fault.

Claimed in the lane table of `docs/javascript/pages/README.md`, in the lane B block below it,
and in the lane B row of `docs/README.md`.

✅ **The loose end from `b4ffc223` is CLOSED.** The clean isolated rebuild ran 2026-08-14 under
session `75e511e6`: **0 broken links under `docs/javascript/`, 0 files over 300 lines.** The 8
breaks in that build are React (4) and TypeScript (4) — other sessions', left alone.

## Your lane — 🔴 CURSOR: phase 11 topic **08 · Aborting and timing out** (Understand)

🎉 ✅ **TWO WHOLE PHASES ARE COMPLETE AT EVERY TIER.**

- **Phase 9 · The DOM — 19/19, 59 files.** Master 01–06 pre-existing; this lane wrote **07–19**
  (13 topics, 46 files).
- **Phase 10 · Events and user input — 14/14, 35 files.** Master 01–04 pre-existing; this lane
  wrote **05–14** (10 topics, 27 files).

Every topic chunked, every file under the 300-line cap, all documentation-validated against MDN
with no fabricated output.

| Phase | Topic | Left |
|---|---|---|
| **9** · The DOM | ✅ **COMPLETE 19/19** | 0 |
| **10** · Events and user input | ✅ **COMPLETE 14/14** | 0 |
| **11** · Network, storage and data transfer | 06–07 ✅ · **resume at 08 · Aborting and timing out** | 14 |
| **12** · The browser platform | 03 onward | 19 |
| **17** · Machine coding | 05 onward | 14 |
| **18** · Building the store front end | **only 11, 12 and 15** | 3 |
| | **total** | **50** |

Work a phase to completion, then the next in this list. Inside a phase:
**Understand → Know → When Needed**.


## Session `75e511e6` — what was done, 2026-08-14 → 15

**Two whole phases finished at every tier, plus two topics of a third.**

| | Topics written | Files |
|---|---|---|
| Phase 9 · The DOM | **07–19** (13) — closes the phase 19/19 | 46 |
| Phase 10 · Events and user input | **05–14** (10) — closes the phase 14/14 | 27 |
| Phase 11 · Network, storage | **06–07** (2) | 5 |
| | **25 topics** | **78 files** |

Every topic chunked on a concept boundary, **every file under the 300-line cap**, every page
documentation-validated against MDN with the sources named in its `> Verified:` line, and **no
console block for a run that did not happen**.

🔴 **MDN facts that memory would have got WRONG** — the reason rule 8's "re-review against the
primary source" earns its place:

- `scrollIntoView()` / `scrollTo()` / `scrollBy()` now **return a Promise** fulfilling
  `{interrupted}` — and MDN warns not every browser does, so `await` silently resolves early.
- `scrollIntoView()` takes a **`container: 'all' | 'nearest'`** option.
- `scrollend` is **Baseline 2025**, not 2023.
- MDN calls throttling `scroll` with `requestAnimationFrame` **"useless"** — frame callbacks fire at
  the same rate; the documented throttle is a `setTimeout` you measure.
- `{passive:true}` on a **`scroll`** listener does nothing; passive applies to `wheel`/`touchstart`.
- **ARIA reflection properties are STRINGS** — `"false"` is truthy.
- `execCommand` is **deprecated AND non-standard**, and its `insertHTML` is a named injection sink.

**Cursor: phase 11 topic 08 · Aborting and timing out.** Then 09–15 (Understand), 16–21 (Know),
then phases 12, 17, 18. **50 topics left.**

## Written in this lane

| Topic | Files | Shape |
|---|---|---|
| **11 · 07 · Reading responses** ✅ 2026-08-15 — **3 files, 425 lines** | `phase-11-network-storage/07-reading-responses/` — README (51) + `01-the-body-readers.md` (175) + `02-streaming-a-body.md` (199) | 01: the six readers and **choose by what the data will BECOME** (a CSV you parse is `text()`, a CSV the user downloads is `blob()`); 🔴 **`json()` rejects on an empty body AND ignores `Content-Type`** — hence `Unexpected token '<'` for an HTML error page — so a wrapper reads `text()` once and parses conditionally, keeping the raw body for the error message; 🔴 **every `createObjectURL` needs `revokeObjectURL`**; `arrayBuffer`/`bytes` for parsing formats vs `blob` staying opaque (and disk-backed); `formData()` is mostly a service-worker tool; 🔴 **`fetch` resolves on HEADERS**, so a connection dropping mid-body fails at the READ, not the fetch — the parse belongs in the same `try`. 02: the `getReader()` `{done,value}` loop where 🔴 **`value` is ALWAYS a `Uint8Array`**; getting a reader **locks** the stream (`releaseLock`), `cancel()` discards; `for await…of` on `res.body` is cleaner but ⚠️ **async iteration of a ReadableStream is not supported everywhere** (MDN); 🔴 **never `TextDecoder().decode()` per chunk** — a multi-byte character straddling a boundary corrupts, intermittently and only for non-ASCII — **`pipeThrough(new TextDecoderStream())`**; the NDJSON buffer where ⚠️ **`lines.pop()` to keep the incomplete last line is not optional**; download progress and its two caveats — **`Content-Length` is often absent** (chunked) and **counts COMPRESSED bytes** so the ratio can exceed 1; 📌 **`fetch` still cannot do UPLOAD progress — that is why XHR survives**; `AbortController` cancels request + stream together; and when NOT to stream |
| **11 · 06 · `Request`, `Response` and `Headers`** ✅ 2026-08-15 — **2 files, 253 lines**. First Understand topic of phase 11 | `phase-11-network-storage/06-request-response-headers/` — README (47) + `01-the-three-objects.md` (206) | `fetch` takes a **`Request` object**, and `new Request(request, {…})` is the copy-with-changes idiom behind interceptors/retries; the `Response` property table incl. **`type: opaque`** (a `no-cors` response — status 0, unreadable, not an error) and 🔴 **`ok` = 200–299, the check `fetch` does NOT do for you**; the static constructors `Response.json()` / `redirect()` / `error()` — what a **service worker** and a **test double** return; the six body readers, all **mutually exclusive**; 🔴 **the body is a STREAM so it reads ONCE** (`bodyUsed`), `clone()` must happen **before** the first read and ⚠️ **buffers the body in memory**; 🔴 **`res.json()` THROWS on an empty body** (204 / empty error) — read `text()` and parse when non-empty, which also gives you one read for logging; `Headers` — 🔴 **names are case-insensitive** (never build your own Map), **`append` adds vs `set` replaces** (the comma-joined-value surprise), `getSetCookie()` because `Set-Cookie` must not be joined; ⚠️ cross-origin exposes only **CORS-safelisted** headers unless `Access-Control-Expose-Headers`, and **forbidden request headers are ignored SILENTLY**; ⚠️ a **`Request` body is single-use too** — a retry must clone or rebuild |
| **10 · 14 · Debugging events** ✅ 2026-08-15 — **2 files, 234 lines**. **CLOSES PHASE 10 (14/14)** | `phase-10-events/14-debugging-events/` — README (51) + `01-finding-what-stole-your-click.md` (183) | The **seven suspects in elimination order** — nothing listening (element replaced) · `stopPropagation` · `preventDefault` · something on top · `pointer-events:none` · passive listener · wrong element/shadow retargeting; ⚠️ **`getEventListeners`/`monitorEvents`/`$0` are DevTools CONSOLE UTILITIES, not web APIs** (flagged in the Verified line too) — 🔴 `getEventListeners($0)` settles suspect 1 in one line; **Event Listener Breakpoints** for library code you cannot grep; 🔴 **the capture-vs-bubble logger on `document`** as the in-page diagnosis — capture ✅ + bubble ❌ ⇒ `stopPropagation()`, neither ⇒ the event never reached the tree; `composedPath()[0]`, `eventPhase` 1/2/3, `defaultPrevented`, **`isTrusted`**; `elementsFromPoint(x,y)` for the leftover overlay; ⚠️ **a synthesised event is not a real one** — `isTrusted:false` carries no user activation, so a passing `dispatchEvent` test can still fail for a user |
| **10 · 13 · Touch and gestures** ✅ 2026-08-15 — **2 files, 238 lines** | `phase-10-events/13-touch-and-gestures/` — README (49) + `01-touch-events.md` (189) | 🔴 **MDN's recommendation quoted verbatim — use pointer events instead**; the four events; 🔴 the **three lists** — `touches` (all) vs `targetTouches` (started here) vs **`changedTouches` (what changed — the one you want, because on `touchend` the lifted finger is NOT in `touches`)**; `Touch.identifier` = `pointerId`; the honest comparison table; the **300 ms tap delay** framed as HISTORY not a measurement — its cause was **double-tap-zoom detection**, and the cure is a responsive viewport + **`touch-action: manipulation`**, ⚠️ with FastClick-style `touchend` shims to be REMOVED when found; ⚠️ **touch support is not a mobile detector** (touchscreen laptops) — `(pointer: coarse)`; the platform has **no pinch/rotate/swipe event** (CSS scroll-snap first, else two `pointerId`s and `Math.hypot`); **every gesture needs a non-gesture route** |
| **10 · 12 · `EventTarget` as a base class** ✅ 2026-08-15 — **2 files, 232 lines** | `phase-10-events/12-eventtarget-base-class/` — README (49) + `01-building-on-the-platform.md` (183) | 🔴 **`EventTarget` is CONSTRUCTIBLE** — `class Store extends EventTarget {}` is a complete emitter with no dependency; the free-features table, where 🔴 **`{signal}` is the actual reason to prefer it** (one `abort()` removes every listener, no reference matching — most hand-written emitters have no cleanup story); the honest **vs Node `EventEmitter`** table — one event object not arguments, **no chaining**, ⚠️ **NO INTROSPECTION** (cannot count/list listeners or remove a whole type), `error` is not special; when NOT to extend — there is an element (dispatch there so delegation works), exactly one consumer (callback), you need a **return value**, ordering matters; 🔴 **composition beats extension when dispatch should stay private** — hold a `#bus` and forward the three methods, since extending makes `dispatchEvent` public and anyone can fake your events; the TypeScript typing caveat as the main honest argument for a custom emitter; `bubbles` is meaningless off-DOM |
| **10 · 11 · Default actions you should not block** ✅ 2026-08-15 — **2 files, 249 lines**. **Closes phase 10's Understand tier (05–11)** | `phase-10-events/11-default-actions/` — README (53) + `01-what-preventdefault-costs.md` (196) | 🔴 **`preventDefault()` on a non-cancelable event is a SILENT no-op** (at most a console warning) — `scroll`, `resize`, `focus`/`blur`, and any custom event without `cancelable: true`; the **passive default** on `wheel`/`mousewheel`/`touchstart`/`touchmove` for window/document/body means a cancel there is ignored unless you pass **`{passive:false}`** — and doing so reintroduces exactly the jank the default prevents, so **`touch-action` is the better tool**; the **do-not-block table** — 🔴 Tab/Shift+Tab, zoom, scrolling, text selection, context menu, middle-click and ⌘-click, browser gestures, Enter/Space on controls; 🔴 **the router link-interception guard set** (`defaultPrevented`, `button !== 0`, all four modifier keys, `target`, cross-origin, `download`) — each guard is a default action a bare `preventDefault()` destroys; where cancelling IS correct — `submit`, `beforeinput`, **`dragover` (the inversion: its default is to REJECT a drop, so cancelling is how you accept)**, a scoped `keydown`, a cancelable custom event; and cancel **narrowly** — smallest element, last statement inside the handled branch, check `defaultPrevented`; 📌 `preventDefault()` ≠ `stopPropagation()` and reaching for both "to be safe" silently breaks delegated listeners upstream |
| **10 · 10 · Page lifecycle** ✅ 2026-08-15 — **3 files, 409 lines** | `phase-10-events/10-page-lifecycle/` — README (50) + `01-startup.md` (170) + `02-shutdown.md` (189) | 01: 🔴 **default to `DOMContentLoaded`; `load` waits for the slowest image** (the "app takes ages to become interactive" bug); the **`readyState` guard** for a script that loads after the event already fired — `loading`/`interactive`/`complete`, six lines and it is all `$(document).ready` ever was; `defer` (parallel, **document order**, just before `DOMContentLoaded` — makes ready handlers redundant) vs `async` (**no order guarantee**) vs `type=module` (deferred); ⚠️ do not measure at `DOMContentLoaded` (fonts/styles unsettled); do not make startup one long task; 🔴 **`DOMContentLoaded`/`load` DO NOT fire on a bfcache restore** — `pageshow` + `event.persisted`. 02: 🔴 **save on `visibilitychange` → `hidden`, which MDN recommends directly** — a page can be discarded with **no unload event at all** (mobile app-switch, memory pressure); the reliability ranking `visibilitychange` > `pagehide` > `beforeunload` > ⛔ `unload`; what `beforeunload` actually costs — **no custom message**, **sticky activation required** (so it will not fire in a naive test), unreliable on mobile, and 🔴 **it DISQUALIFIES the page from the bfcache** (MDN quoted), so register it **only while dirty and remove it after**; `pagehide`/`pageshow` with `event.persisted` and ⚠️ **the bfcache restores spinners, disabled buttons and stale prices** — reset transient UI on restore; **`sendBeacon()` / `fetch(keepalive:true)`** for the last request (a plain `fetch` at unload is cancelled), fired from `hidden`; and `saveState()` must be **idempotent and cheap** |
| **10 · 09 · Scroll, resize and visibility** ✅ 2026-08-15 — **3 files, 435 lines** | `phase-10-events/09-scroll-resize-visibility/` — README (53) + `01-the-high-frequency-events.md` (211) + `02-visibility-and-lifecycle.md` (171) | ⚠️ **Written as the DECISION, not a second copy** — the scroll-throttling depth lives in phase 9 topic 14 chunk 04 and this page links to it (the overlap rule). 01: the pick-your-signal table where **only "exactly where is the scroll position" needs a listener**; `resize` (viewport only, **after paint**) vs **`ResizeObserver`** (any element, 🔴 **before paint**, so the correction ships in the SAME frame); the entry boxes — **`contentRect` is the CONTENT box**, so comparing it with `getBoundingClientRect()` is apples-to-oranges, use `borderBoxSize`; `device-pixel-content-box` for canvas; 🔴 **"ResizeObserver loop completed with undelivered notifications" = the callback resized what it observes**, MDN's two fixes (defer into rAF, or track expected sizes in a `WeakMap`) plus the better one — **do not size from JS at all**; 🔴 **breakpoints belong to `matchMedia`** (`innerWidth` includes the scrollbar, media queries use `clientWidth`, so they disagree exactly at the boundary); observers have **no `signal` option** — `disconnect()` is explicit. 02: `visibilityState` `visible`/`hidden`, where **hidden includes a screen that turned off**; ⚠️ it is **NOT "on screen"** — that is `IntersectionObserver`; the documented throttling — 🔴 **rAF callbacks STOP ENTIRELY in a background tab** (so frame-counted animation drifts; drive from timestamps) and timers are budget-throttled after ~30s (10s Chrome), with **audio / WebSocket / WebRTC / IndexedDB exempt**; **save state on `hidden`**, because a mobile app-switch may never produce another event; and the combined `onScreen && !document.hidden` gate; visibility ≠ focus |
| **10 · 08 · Custom events** ✅ 2026-08-15 — **3 files, 462 lines** | `phase-10-events/08-custom-events/` — README (52) + `01-dispatching-and-listening.md` (217) + `02-decoupling-components.md` (193) | 01: 🔴 **EVERY option defaults to `false`** — `bubbles`, `cancelable`, `composed` — and `detail` to `null`, the opposite of built-in events and the cause of almost every "my custom event does not fire"; `CustomEvent` exists **only for `detail`** (else `new Event()`), with **subclassing `Event`** as MDN's third option for named properties; `initCustomEvent`/`createEvent` deprecated; 🔴 **`dispatchEvent()` is SYNCHRONOUS** — listeners run to completion before it returns, a throwing listener does not stop the others, re-entrancy recurses; the **veto protocol** — `cancelable: true` + `preventDefault()` + **`dispatchEvent()` returns `false`**; naming (namespace it, never reuse a built-in name, past tense for facts / `before-` for cancelable intents); the dispatch-target table (element+bubbles ≫ document bus, which loses instance identity); `composed: true` for shadow roots. 02: **emit up, command down** — events are notifications, methods/attributes are commands; **`EventTarget` is constructible and extensible** so a store/router gets the whole listener API with no dependency (and `bubbles` is meaningless off-tree); 🔴 **one `AbortController` per component, aborted in teardown** — kills the identity trap and the bus-outlives-listener leak, ⚠️ but `disconnectedCallback` can fire on a mere move so a re-connect needs a fresh controller; the **wrong-tool table** — shared state (store), needing a **return value** (method call), listener **ordering** (explicit calls), **cross-tab** (`BroadcastChannel`; DOM events never leave the document); 🔴 the real cost is **traceability** — with a document-level bus, "what happens when I click this" is only answerable by grepping a string; and the event-contract checklist (**`detail` is public API** — adding fields is safe, renaming breaks consumers; never emit in a loop, dispatch is synchronous) |
| **10 · 07 · Pointer events** ✅ 2026-08-15 — **3 files, 459 lines** | `phase-10-events/07-pointer-events/` — README (53) + `01-the-unified-model.md` (181) + `02-a-drag-that-does-not-leak.md` (225) | 01: one model for mouse/pen/touch, events mirroring the mouse set one-for-one; the properties mouse events never had — **`pointerId`** (stable per pointer ⇒ multi-touch is a `Map`), `pointerType`, `isPrimary`, `pressure`, **`width`/`height`** (contact patch ⇒ MDN's "size targets for the largest contact surface"), pen tilt; `PointerEvent` **extends `MouseEvent`**; 🔴 **`pointercancel` has no mouse equivalent — the browser TAKES the pointer and NO `pointerup` follows**, the #1 pointer bug and it never reproduces on desktop; 🔴 **`touch-action` is the fix for "drag works on desktop, scrolls on mobile"** — `preventDefault()` cannot work because the browser decides before your handler runs and those listeners are passive by default; **compatibility mouse events** fire alongside (so handling both runs logic twice), and MDN is specific that `mouseover/out/enter/leave` and hovering pointers can **never** be prevented; 📌 **keep `click` for buttons** — it covers every pointer type AND the keyboard. 02: 🔴 **`setPointerCapture()` is released IMPLICITLY on `pointerup`/`pointercancel`** — which is exactly why the pattern cannot leak, versus the old document-listener shape (identity trap, throw-between-down-and-up leaks forever, `mouseup` lost outside the window, two drags sharing listeners); while captured **`pointerover/enter/leave/out` do not fire** (documented, not a bug); `setPointerCapture` **after** a DOM move; touchscreens may capture **implicitly**; the full drag — **4 px threshold**, **`transform` not `left`/`top`**, state keyed by `pointerId`, and 🔴 **`pointercancel` REVERTS while `pointerup` COMMITS** (different outcomes, not one cleanup); 📌 `pointermove` already coalesces per frame so rAF adds latency without saving work; and the accessibility half — keyboard equivalents, the value in the DOM (`role="slider"`/`aria-valuenow` or a native `<input type=range>`), a non-drag route for reordering |
| **10 · 06 · Keyboard events** ✅ 2026-08-15 — **3 files, 439 lines** | `phase-10-events/06-keyboard-events/` — README (52) + `01-key-code-and-keycode.md` (184) + `02-building-a-shortcut.md` (203) | 01: the three-property table — 🔴 **`key` = the character (layout- and modifier-aware), `code` = the physical US-layout position**, so **`key` for text-meaning shortcuts, `code` for positional controls (WASD)**; 📌 **space is `' '` in `key` and `'Space'` in `code`** — the most common typo; `'Dead'` and `'Unidentified'`; the sequence `keydown` (repeating, `e.repeat`) → `beforeinput`/`input` → `keyup`, with **`keypress` deprecated**; 🔴 **`isComposing`** — without the guard, Enter-to-confirm fires mid-IME-candidate (and the legacy spelling of that check was **`keyCode === 229`**, the one reason to recognise the deprecated property); `getModifierState('CapsLock')` for the caps warning without logging keys. 02: the handler shape with **`{signal}`/`AbortController`** teardown; ⌘-vs-Ctrl with an explicit **reject-the-other-modifier** test, ⚠️ `navigator.platform` deprecated but still the most reliable hint; 🔴 **compare ALL FOUR modifier flags** or Ctrl+S also catches Ctrl+Shift+S; **lower-case `e.key`** because Shift makes it `'K'`; stand-down cases (typing, modal open, component owns its keys, `defaultPrevented`); the **do-not-steal table** — 🔴 **Tab/Shift+Tab above all**, plus zoom; **`preventDefault()` only inside the handled branch**; discoverability, rebinding, never the only route; `accesskey` collides with AT; sequence buffers, and ⚠️ unmodified single-letter shortcuts collide with screen-reader browse mode |
| **10 · 05 · Form and input events** ✅ 2026-08-15 — **3 files, 453 lines**. First topic of phase 10's Understand tier | `phase-10-events/05-form-and-input-events/` — README (50) + `01-the-event-set.md` (196) + `02-a-controlled-input.md` (207) | 01: the six-event table with **bubbling** noted; 🔴 **`change` waits for BLUR on text fields** but fires immediately for checkbox/radio/select/file (per-control table, `range` fires both); 🔴 **setting `.value` from code fires NOTHING** — dispatch `new Event('input',{bubbles:true})` (the autofill-invisible bug); `beforeinput` as the cancelable one with `inputType`, covering paste/drop/IME in one place; `e.submitter` + `new FormData(form, submitter)`; 🔴 **`form.submit()` skips validation AND the submit handler — `requestSubmit()` is the one**; a control named `submit` **shadows the method**; disabling the form removes fields from the payload (disable the **button**); keyboard shortcuts must bail on `input, textarea, select, [contenteditable]`; `focusout` (not `input`) is the accessible validation moment. 02: controlled vs uncontrolled and **what controlled costs**; 🔴 **assigning `input.value` RESETS THE CARET even to an identical string** — the `if (input.value !== state)` guard is the whole trick behind every "cursor jumps to the end"; caret preservation through a formatter by counting **stable units (digits before the caret)** and re-walking, never the raw offset; 🔴 **the IME `composing` flag** — a formatter without it makes the field unusable in CJK, three lines; rejecting edits with `beforeinput` beats `keydown` (paste/drop) and beats stripping in `input` (caret already moved), ⚠️ but rejection is a poor primary validation strategy; debounce must **capture `e.target.value` synchronously**, not read it in the timeout |
| **9 · 19 · Selection, `Range`, `contenteditable`** ✅ 2026-08-15 — **3 files, 468 lines**. **CLOSES PHASE 9 (19/19)**. Only When-Needed-tier topic in the lane so far | `19-selection-range-contenteditable/` — README (55) + `01-selection-and-range.md` (217) + `02-contenteditable.md` (196) | 01: a boundary point is **container + offset**, and 🔴 **offset counts CHARACTERS in a text node but CHILD NODES in an element** — the source of most Range confusion; the build/mutate method tables; **`surroundContents()` throws when the range partially contains a non-text node** (so highlighters wrap per text node); `createContextualFragment()` is an **HTML sink** like `innerHTML`; a Range is **live and fragile** — recreate after edits, never hold one across a re-render; ranges have **geometry** (`getBoundingClientRect`, one rect per line) which is how a selection toolbar positions itself; 🔴 **anchor/focus ≠ start/end** (anchor is where the drag began, so backwards selections invert them); MDN: multiple ranges were a Netscape thing, **spec requires one**; ⚠️ **programmatic selection MOVES FOCUS in Safari/Chrome but not Firefox** (MDN documents it); 🔴 **the Selection API does not reach inside `<input>`/`<textarea>`** — those use `selectionStart`/`setSelectionRange`; `getComposedRanges()` for shadow boundaries. 02: `contenteditable` is **enumerated, not boolean** — `true`/`""`, `false`, 🔴 **`plaintext-only`** (the value most projects want and few know), and it **inherits** (so `false` carves out non-editable islands); nested editables are **not tab stops**; 🔴 **`execCommand` is BOTH deprecated AND non-standard** — MDN's quoted wording, different markup per browser, **may not fire `beforeinput`/`input`**, `insertHTML` is a named **injection sink**, and its return value **cannot be used for feature detection**; `beforeinput` as the control point (`inputType`, `data`, `dataTransfer`, **`getTargetRanges()`**), with ⚠️ **MDN's own caveat that it does not fire for every modification and can be non-cancelable** (autocomplete, spell-check, autofill, IME) so it is never the source of truth; the target is the **editing host**; and the list of why editors are libraries — document model, caret across re-renders, **IME composition** (the CJK-breaking one), paste sanitisation, undo once you `preventDefault`, accessibility |
| **9 · 18 · Shadow DOM and custom elements** ✅ 2026-08-14 — **4 files, 680 lines**. **Closes phase 9's KNOW tier (16–18)** | `18-shadow-dom-and-custom-elements/` — README (58) + `01-custom-elements.md` (242) + `02-shadow-dom.md` (205) + `03-living-with-the-boundary.md` (175). 03: 🔴 **event retargeting** — outside the boundary `event.target` is the **host**, and `composedPath()[0]` is the real node; 🔴 **`bubbles: true` is NOT enough for a custom event to escape — it needs `composed: true`** (the #1 "my component's events don't work"); `document.querySelector` does not descend, `closest()` stops at the root, `getRootNode().host` climbs out, `getRootNode({composed:true})` reaches the document; `activeElement` reports the host and **`delegatesFocus`** makes the host behave like a control; 🔴 **controls in a shadow root are NOT successful controls** of the outer form (`ElementInternals` + `setFormValue`, not a hidden input) and **`<label for>` cannot cross**; **ARIA ids cannot cross either** — pass the label in or use internals; and the framing the topic is built on — **custom elements and shadow DOM are separate features**, skip the shadow root when you own the page and keep the lifecycle | 01: hyphenated name is a **parser requirement**, not style; autonomous vs customized built-in and ⚠️ **Safari does not support customized built-ins** (so you reimplement `<button>`); 🔴 the **constructor is severely restricted** — MDN forbids inspecting attributes/children there, because an **upgraded** element runs its constructor after it already has both; `observedAttributes` is **opt-in with no wildcard** (a forgotten attribute fails silently) and fires for initial values during upgrade; 🔴 **`connectedCallback` can run MORE THAN ONCE** — `appendChild` disconnects and reconnects, so setup must be idempotent; `Element.moveBefore()` moves **without** disconnecting (`connectedMoveCallback`); upgrade + `:defined` + `whenDefined()` for the unstyled flash; 🔴 **a property assigned before upgrade SHADOWS the class accessor** — the documented delete-and-reassign cure; `ElementInternals` = `formAssociated` + `setFormValue`, custom states styled with **`:state()`**, and implicit ARIA role. 02: `open` vs `closed` and ⚠️ **MDN: `closed` is NOT a security mechanism** (extensions evade it) — use `open`; the **crossing table** — page selectors and shadow styles do NOT cross, but **inherited properties, custom properties and `dir`/`lang` DO**; `::part()` (opt-in, needs `part=`) and `::slotted()` (one level deep) as the two escape hatches, with **custom properties as the real theming contract**; `:host` / `:host()` / `:host-context()` and ⚠️ **an unknown tag is `display: inline`** so `:host{display:block}` is the usual first line; `adoptedStyleSheets` parsed **once and shared** vs a per-instance `<style>`; 🔴 **slotted content STAYS IN THE LIGHT DOM** — page CSS matches it, `document.querySelector` finds it, only its render position moves; `assignedElements()` + `slotchange`; **declarative shadow DOM** (`<template shadowrootmode>`, consumed during parsing, no template left in the tree) is what makes SSR of components possible |
| **9 · 17 · `MutationObserver`** ✅ 2026-08-14 — **3 files, 442 lines** | `17-mutationobserver/` — README (56) + `01-the-api.md` (225) + `02-when-to-use-it.md` (161) | 01: 🔴 **`observe()` throws `TypeError`** in three documented ways — none of `childList`/`attributes`/`characterData` true, or `*OldValue`/`attributeFilter` combined with an explicit `false`; the **implications** go the other way (`attributeOldValue` ⇒ `attributes`, `attributeFilter` ⇒ `attributes`, `characterDataOldValue` ⇒ `characterData`), so the short form is legal; 🔴 **a record carries `oldValue`, never a new value** (read it off `record.target`), and `oldValue` is `null` unless requested; `addedNodes` are **nodes not elements** (filter `nodeType`); delivery is **batched at microtask time** — ten appends, one callback, ten records, and **you cannot veto a mutation**; 🔴 **`disconnect()` DISCARDS the queue** so `takeRecords()` first, which is also the cure for the self-triggering loop; no `{signal}` option, so `AbortController` needs an explicit abort listener; a connected observer keeps its target alive (the detached-node leak). 02: the **decision table** — every row above `MutationObserver` has a cheaper answer (own the change · `connectedCallback`/`attributeChangedCallback` · `IntersectionObserver`/`ResizeObserver` · delegation), and it is Know-tier because reaching for it in your own code is a design smell; the genuine cases (third-party widget, `contenteditable`, enforcing an invariant on content you do not own, userscripts); the **one-shot pattern** (disconnect inside the callback); ⚠️ **observers report CHANGES, not state** — an element already present fires nothing, so check once first |
| **9 · 16 · `<dialog>`, popover, `inert`** ✅ 2026-08-14 — **4 files, 615 lines**. **First KNOW-tier topic of the lane** | `16-dialog-popover-inert/` — README (57) + `01-the-dialog-element.md` (208) + `02-the-popover-api.md` (169) + `03-inert-and-the-top-layer.md` (181). 03: `inert`'s exact block-list; the **three-way table** `inert` (subtree, blocks interaction, out of a11y tree) vs `aria-hidden` (**does NOT block interaction — the bug**) vs `disabled` (one control); ⚠️ **no visual effect, MDN says so** — dim it yourself; uses (off-screen drawer still in the tab order, carousel slides, group-disable) and 🔴 **`inert` does NOT remove fields from the form payload** (`disabled` does); a modal dialog **escapes inertness**, nothing else can; the **top layer** (modal dialog, popover, fullscreen) escapes `z-index`, ancestor `overflow: hidden` **and** an ancestor `transform` — which is why portalling to `<body>` is obsolete; entering/leaving it is **discrete**, so exit animation needs `transition-behavior: allow-discrete` on `display`/`overlay` plus **`@starting-style`** for entry | 01: `show()` vs `showModal()` as a **table of five differences** — only the modal one inerts the page ("as if the `inert` attribute is specified"), reaches the **top layer** with `::backdrop`, closes on **Escape** (firing `cancel`), sets `aria-modal="true"`, and is limited to one at a time; `showModal()` **throws `InvalidStateError`** on an already-non-modally-open dialog; ⚠️ MDN says **do not render via the `open` attribute**; focus goes to the **first focusable element** (usually Close — hence `autofocus` on the primary action) and ⚠️ **`tabindex` "must not be used" on `<dialog>`**; `close(value)` / `returnValue` / **`<form method="dialog">`** (collects data, submits nothing, sets `returnValue`); `returnValue` defaults to `''` so test by value not truthiness; what it still does NOT do — accessible name, `overscroll-behavior` for its own scrolling, exit animation (needs `allow-discrete` + `@starting-style`), a trigger that still exists. 02: Popover API is **Baseline 2025**; the zero-JS `popovertarget` form; `auto` (light dismiss + mutual exclusion) vs **`manual`** (toasts) vs `hint`; `showPopover`/`hidePopover`/`togglePopover`; **`beforetoggle` (cancellable, lazy-load) vs `toggle`**, both with `newState`; 🔴 **a popover is NOT a modal** — page stays interactive, focus is not trapped, so never build a confirmation on it; what you still owe it (role, `aria-expanded` on custom triggers, roving-tabindex arrows, initial focus, **positioning — it is viewport-centred, not anchored**) |
| **9 · 15 · Focus and accessibility** ✅ 2026-08-14 — **5 files, 993 lines**, all on `js-lane-b`. **Closes phase 9's Understand tier (07–15)** | `15-focus-and-accessibility/` — README (59) + `01-what-can-hold-focus.md` (258) + `02-managing-focus.md` (254) + `03-aria-from-javascript.md` (210) + `04-live-regions.md` (212). 03: **first rule of ARIA**; state lives on the **trigger** not the panel (`aria-expanded` is the classic misplacement); 🔴 the reflection properties (`el.ariaExpanded`, Baseline Oct 2023) are **STRINGS** — `"false"` is truthy, compare `=== 'true'`; `aria-disabled` ≠ `disabled` (still focusable, still fires `click`); naming order visible text → `aria-labelledby` → `aria-label`, and **`aria-label` silently overrides visible text** (breaks voice control); 🔴 **`aria-hidden` on focusable content is the worst state** — reachable by Tab, announced as nothing; `display:none` also removes from the a11y tree, so SR-only text needs the clip pattern; keep state in the attribute, not a parallel boolean. 04: 🔴 **the container must exist in the DOM BEFORE the content changes** — injecting region+content together is silent; `polite`/`assertive`/**`off` = "only when focus is inside"**; the implicit-live roles table; MDN advises a **redundant** `aria-live` on `status`/`log`, but ⚠️ `role="alert"` + `aria-live="assertive"` **double-speaks on iOS VoiceOver**; `aria-atomic` (the 17:33→"34" clock example) and `aria-relevant` (**removals are silent by default**); identical text announces **nothing** — clear then set in a later task; `aria-busy` to batch; and when a live region is the WRONG answer (move focus instead; per-field errors belong on `aria-describedby`) | 01: the implicit-`tabindex="0"` element list; **only `0` and `-1`** (MDN's own wording quoted — a positive value reorders the whole document); 🔴 a focusable `<div>` is **not in the accessibility tree as a control**; `activeElement` falls back to **`<body>`** (the removed-focused-node bug), `focus()` scrolls unless `preventScroll`, `focus()` on a non-focusable element **fails silently**, `blur()` is almost always wrong; shadow roots have their **own** `activeElement` (recursive `deepActiveElement`); 🔴 **`focus`/`blur` do NOT bubble — `focusin`/`focusout` do**, and `relatedTarget` is the "did focus leave the component" test, **`null`** when it goes to browser chrome; MDN notes the **spec's focus-event order differs from what browsers implement** — do not depend on interleaving; `:focus-visible` (matches for keyboard **and scripted** focus) vs `:focus` vs `:focus-within`, WCAG **3:1** indicator contrast. 02: the **three obligations** (send focus in, keep it in, give it back); record `activeElement` **before** anything moves; 🔴 **`inert` beats a hand-written trap** — MDN's full list (no clicks, no focus, no selection, no find-in-page, out of the a11y tree), **Baseline since April 2023**, ⚠️ **no visual indication** so you must dim it yourself, and the modal must be a **sibling** of the inerted root; `inert` (regions) vs `aria-hidden` (**leaves content tabbable — the worst state**) vs `disabled` (controls); `showModal()` does the lot and a modal dialog **escapes inertness**; if you must trap, requery the focusable list **on every Tab**; restore with an **`isConnected`** guard; roving tabindex (one `0`, arrows move it, `preventDefault` so the page does not scroll) vs `aria-activedescendant` |
| **9 · 14 · Scrolling** ✅ 2026-08-14 — **5 files, 1,066 lines** (01–02 on `main`, 03–04 + README on `js-lane-b`) | `14-scrolling/` — README (59) + `01-moving-the-scroll-position.md` (231) + `02-landing-on-an-element.md` (224) + `03-scroll-containers-and-sticky.md` (286) + `04-watching-and-restoring.md` (266) | 🔴 **Two MDN facts that memory would have got WRONG, both verified by fetch:** `scrollIntoView`/`scrollTo`/`scrollBy` now **return a Promise** fulfilling `{interrupted}` (and MDN warns not every browser does — `await undefined` resolves *immediately*, a silent gap), and `scrollIntoView` takes a **`container: 'all' \| 'nearest'`** option (default `'all'` — scrolls *every* scrollable ancestor). `scrollend` is **Baseline 2025**, not 2023. 01: the three ways to move a scroll position; `scroll`/`scrollTo` are the same method; coordinate form needs **both** args while the options form leaves an omitted axis alone; `scrollTop` is **fractional**, **clamps** on overflow, can be **negative** in `column-reverse`, and returns 0 (silently) on a non-scroller; 🔴 the setter is `scroll({behavior:'auto'})` and **`auto` means "use the CSS `scroll-behavior`"**, so a *restore* animates under `html{scroll-behavior:smooth}` — pass **`'instant'`**; `document.body.scrollTop` is 0 in standards mode (`document.scrollingElement`); `scroll-behavior` on `body` does **not** propagate to the viewport (MDN's own wording); `prefers-reduced-motion` read **per call**. 02: the **asymmetric defaults** — `block:'start'` but `inline:'nearest'` — so 🔴 `{block:'nearest'}` is the keyboard-list answer; the legacy boolean is an **alignment** switch, not on/off; `scroll-margin-*` (on the target) vs `scroll-padding-*` (on the container) beat hand-subtracting a header height because they also fix `:target` navigation and snapping, and force no layout; 🔴 **no `scrollend` fires if the position did not change** — a wait-for-scrollend promise hangs when the element was already in view, so it needs a timeout; scrolling ≠ focus (`tabindex="-1"` + `focus()`, then `preventScroll:true` to stop the double jump); `scrollIntoViewIfNeeded` is **non-standard** |
| **9 · 13 · Measuring elements** ✅ 2026-08-14 ⚠️ **build not re-run** | `13-measuring-elements/` — README + `01-the-four-families.md` (214) + `02-viewports-and-device-pixels.md` (203) | 01: four families, four answers — `getBoundingClientRect` (**fractional**, **viewport**-relative, **reflects transforms**, border box) vs `offset*` (**rounded integer**, untransformed layout box, `offsetTop` is relative to **`offsetParent`**) vs `client*` (padding box, **no border, no scrollbar**) vs `scroll*` (full content + the writable `scrollTop`). Margin is in **none** of them. **Zeros** for `display:none` and for a node still in a fragment, while **`visibility:hidden` still measures**. 🔴 the **~1 px tolerance** an at-bottom check needs (fractional `scrollTop` vs integer heights) — the infinite-scroll bug that only reproduces elsewhere. `getClientRects()` = one rect **per line box** for a wrapped inline; `ResizeObserver` `contentRect` is the **content** box (use `borderBoxSize` to compare with a rect). 02: the **two coordinate systems** and which CSS `position` needs which; the three viewport widths — `innerWidth` (**with** scrollbar) vs `documentElement.clientWidth` (**without**, and **what media queries use**) vs `visualViewport.width`; 🔴 **never reimplement breakpoints — `matchMedia`**; the modal scroll-jump and `scrollbar-gutter: stable`; `visualViewport` for pinch-zoom and the mobile keyboard; `devicePixelRatio` for canvas (buffer × dpr, CSS size for display, `ctx.scale`, ⚠️ **assigning `canvas.width` resets the whole 2D context state**) and 🔴 **DPR is not constant** — the self-re-arming `matchMedia('(resolution: Ndppx)')` listener, since there is no change event |
| **9 · 12 · Layout thrashing** ✅ 2026-08-14 | `12-layout-thrashing/` — README + `01-the-forced-reflow.md` (197) + `02-fixing-it.md` (217) | 01: writes mark layout **dirty** and the engine defers to once-before-paint — a geometry read while dirty **forces synchronous layout**; the loop makes it `n` passes. Trigger list by family: offset / client / scroll, `getBoundingClientRect`/`getClientRects`, `getComputedStyle`, `window.scrollY`/`innerHeight`, `document.elementFromPoint`, the scroll methods, and the two surprises 🔴 **`innerText`** (rendered text ⇒ needs layout, unlike `textContent`) and **`focus()`**. Style vs layout invalidation, so a colour-only class is not the culprit. DevTools: **sawtooth** of script/layout inside one task, forced-reflow warning **with a stack trace** — the read is often a call away. The deliberate `void el.offsetWidth` animation-restart hack and its modern `getAnimations()` alternative. 02: **read phase then write phase**, and 🔴 it fails when a **callee** reads; `rAF` runs before that frame's layout and is **not a barrier** — hence the **double-`rAF`** measure-after-write idiom; `ResizeObserver`/`IntersectionObserver` deliver **after** layout so reading their entries forces nothing (RO **loop** error = the callback resizes what it watches); animate **`transform`/`opacity`** (compositor) not `left`/`width`; `will-change` **sparingly, last resort** (MDN's own wording); CSS **`contain: layout paint`** and `content-visibility: auto` as the no-JS fix; then a 5-step order to try |
| **9 · 11 · Batching DOM work** ✅ 2026-08-14 | `11-batching-dom-work/` — README + `01-build-off-document.md` (204) + `02-not-freezing-the-page.md` (180) | Split the row into its **two** real problems. 01: `DocumentFragment` is **one-use** (insertion moves its children out and empties it), has **no layout** (`isConnected` false, `getBoundingClientRect()` zeros — cannot measure before insertion); `<template>` content is **inert** (no image fetches, no scripts) and is a **live fragment**, so 🔴 always `cloneNode(true)` or only the first row appears; templates also sidestep the HTML parser's **table-context** rules; `append`/`replaceChildren` are variadic and take strings as text; **`innerHTML +=` re-parses the container**, destroying and recreating every existing node (listeners, focus, identity). ⚠️ Deliberately does **not** sell fragments with a speed number — engines coalesce; the honest claim is "the shape that cannot go wrong". 02: **long task = >50 ms** (Long Tasks API); 🔴 **batching does not fix the JS cost**; answer order is (1) do not render what nobody sees — virtualise, `content-visibility: auto` + `contain-intrinsic-size`, `IntersectionObserver` sentinel; (2) chunk and yield, with 🔴 **`await Promise.resolve()` is a MICROTASK and not a yield**; (3) coalesce scroll/resize into one rAF with a **queued flag** + `passive`. Workers do computation, never the DOM. Tools named for measuring instead of numbers |
| **9 · 10 · Removing and replacing** ✅ 2026-08-14 | `10-removing-and-replacing/` — README + `01-the-api.md` (187) + `02-cleanup.md` (237) | 01: `remove()` is a **no-op with no parent** where `removeChild` throws `NotFoundError`; `replaceWith`/`replaceChildren` take **strings as text**, so they are not HTML sinks; **appending MOVES** (one parent), so `cloneNode(true)` is the copy — and 🔴 **clone does not copy `addEventListener` listeners** (it does copy inline `on*` attributes, which hides the gap) nor the current input value (attribute = default); replace the **smallest subtree**, because a full rebuild throws away focus, selection, scroll and running transitions; `querySelectorAll` is static so safe to iterate while removing, `children` is not. 02: the **detached-node leak** — out of the document ≠ collectable, and the referrer is usually a `Map`, a closure on a **surviving** listener, an observer or a timer; 🔴 **`AbortController` + `{signal}` removes every listener in one `abort()`** (and cancels a `fetch`), sidestepping the `removeEventListener` **identity trap**; observers/intervals/rAF need explicit stops (`isConnected` as a rAF stop condition); **delegation removes the problem structurally**; `WeakMap` not `Map` for per-node data; and **removing the focused element drops focus to `<body>`** — capture with `contains` (which counts the node itself) and restore |
| **9 · 09 · Forms** ✅ 2026-08-14 | `09-forms/` — README + `01-formdata.md` (213) + `02-constraint-validation.md` (212) + `03-form-events.md` (226) | Three chunks. 01: **successful controls** (a `name` is required — an `id` is not; disabled out, `readonly` in; unchecked boxes contribute **no key at all**, and a valueless checked box submits `"on"`), `new FormData(form, e.submitter)` for multi-action forms, 🔴 the **`Object.fromEntries` multimap collapse** that silently drops checkbox groups / `<select multiple>` (`getAll` is the fix), `form.elements` matching **name OR id** (so a field can look present and not submit), and **never hand-set `Content-Type` for a FormData POST** (the boundary). 02: `checkValidity` (silent) vs `reportValidity` (bubble + focus), the `ValidityState` flags with **`badInput` reading as `''` while text is visible**, `setCustomValidity` and the **never-cleared** permanent-invalid bug (assign unconditionally), the **`invalid` event does not bubble**, `novalidate` disabling the UI but **not** the API, `:user-invalid` over `:invalid`, and client validation is **not** a security control. 03: `input` (every change) vs `change` (**blur** for text, immediate for checkbox/radio/select), **programmatic `.value` fires nothing**, `beforeinput` as the cancelable one covering paste/IME, `submit` on the form with `e.submitter`, 🔴 **`form.submit()` skips validation AND the submit handler — `requestSubmit()` is the one**, a control named `submit` shadowing the method, `reset()` restoring **attribute defaults**, and disabling the **button not the form** (disabled controls leave the payload) |
| **9 · 08 · Classes and styles** ✅ 2026-08-14 | `08-classes-and-styles/` — README + `01-classlist.md` (193) + `02-styles-and-custom-properties.md` (244) | Chunked from the start. Spine: **JS decides state, CSS decides what state looks like** — so JS writes classes and custom properties, not declarations. Chunk 01: the `DOMTokenList` API, **`toggle(name, bool)`'s forcing second argument** as the cure for class/state drift, `className` wiping siblings and being an **`SVGAnimatedString` on SVG**, the two throws (`SyntaxError` empty / `InvalidCharacterError` whitespace), class vs `data-*` for **mutually exclusive** states, ARIA alongside. Chunk 02: `element.style` is the **inline attribute only**, camelCase mapping with **custom properties exempt** (`setProperty` required), CSS's **silent** rejection of unitless numbers and bad keywords, `cssText` wiping the block, `'initial'` ≠ remove, `!important` via the third argument, `getComputedStyle` returning resolved values and needing **`.trim()`** on custom properties, the **custom-property bridge**, and `adoptedStyleSheets` for styling many at once |
| **9 · 07 · Traversal** ✅ 2026-08-14 | `07-traversal/` — README + `01-the-two-families.md` (194) + `02-closest-matches-and-scope.md` (227) | Drafted flat at **325 lines**, over the cap, so split on the concept boundary **properties vs selector API**. Chunk 01: the `Node`/`Element` twin families, whitespace text nodes, `parentNode` vs `parentElement` at the document boundary, live `children` while removing, `replaceChildren`, when a `TreeWalker` is right. Chunk 02: `closest` (starts at self, full selector, `null` on miss, `SyntaxError`), `matches`, the `:scope` trap where a descendant selector matches against the whole document, `contains` (includes the node itself) vs the `compareDocumentPosition` bitmask, shadow roots ending the up-walk, `getRootNode().host`, `isConnected` |

## Session state — `b4ffc223`, 2026-08-14

**7 topics written, 23 files, all committed.** Phase 9 Understand is **07–13 done, 14–15 left**,
then Know 16–18 and When Needed 19.

⚠️ **One loose end:** the clean isolated rebuild for **topic 13** was interrupted before it ran.
Every earlier topic built clean (0 broken links under `docs/javascript`, 0 files over 300). Run
this first thing on resume:

```bash
rm -rf .docusaurus-laneb node_modules/.cache && \
DOCUSAURUS_GENERATED_FILES_DIR_NAME=.docusaurus-laneb yarn build --out-dir build-laneb 2>&1 \
  | grep "source page path" | sed 's#.*/devbible/docs/##' | cut -d/ -f1 | sort | uniq -c
find docs/javascript -name '*.md' -exec wc -l {} + | awk '$1>300 && $2!="total"'
```

Topic 13 links only to pages that already exist (`12-layout-thrashing/README.md`,
`11-batching-dom-work/README.md`) plus bold-plain-text forward references, so a break is unlikely
— but it is unverified, and a green build is not assumed.

## ✅ The build blocker is RESOLVED — and the lesson that came with it

**2026-08-14 → resolved 2026-08-15.** A full `yarn build` in the worktree failed MDX compilation on
`docs/react/pages/phase-11-ssr-hydration/09-partial-prerendering/` (README + `03-the-caveats.md`) —
*"Unexpected closing slash `/` in tag"* — inherited at the branch point. **React fixed it on `main`
in `0d90db70` ("strip a stray `</content>` line from 5 pages"), and `git merge main` into
`js-lane-b` picked it up.** The build now completes: **0 broken links under `docs/javascript/`, 0
files over 300**; the 3 remaining breaks are React (2) and TypeScript (1), theirs.

🔴 **The lesson, and it is a new disguise of rule 4.** Two reports in this session said "clean
build" and should not have. The command was `yarn build … | tail -N`, so the pipeline's exit status
came from **`tail`**, and `grep -c "broken"` returned 0 only because **the build died before it ever
checked links**. A build that never ran looks *identical* to a clean one when you grep for warnings.

```bash
# always confirm the artifact exists — this is the check that catches it
ls build-laneb/index.html || echo "BUILD DID NOT COMPLETE"
```

⚠️ **Merging `main` into the lane branch is now a known-good move** when another language's page
breaks the shared build — it is their fix to make, and yours to pick up. Expect conflicts in
`docs/README.md` and `docs/javascript/pages/README.md`; resolve by **keeping your lane B row and
taking `main`'s for every other row**.

**The cheap stand-in**, still useful between builds (link + cap check):

```bash
python3 - <<'EOF'
import os, re
ROOT='docs/javascript/pages'; LINK=re.compile(r'\]\(([^)\s#]+?\.md)(#[^)]*)?\)'); bad=[]; n=0
for d,_,fs in os.walk(ROOT):
  for f in fs:
    if not f.endswith('.md'): continue
    p=os.path.join(d,f)
    for m in LINK.finditer(open(p,encoding='utf-8').read()):
      t=m.group(1)
      if t.startswith('/') or '://' in t: continue
      n+=1
      if not os.path.exists(os.path.normpath(os.path.join(d,t))): bad.append((p,t))
print(n,'links,',len(bad),'broken'); [print('BROKEN:',*b) for b in bad]
EOF
find docs/javascript -name '*.md' -exec wc -l {} + | awk '$1>300 && $2!="total"'
```

## Traps found

- **The 325-line draft is the standing warning**: an Understand DOM topic does **not** fit one
  file if it is written properly. Expect to chunk rather than trim — the cap is a file-size rule.
- **Phase 3 broken link and two over-cap files are LANE A's**, live in another session as of
  2026-08-14. Tally by language before reacting; a `docs/javascript/` break is not automatically
  yours.
- Build in isolation so the other sessions do not collide:
  `DOCUSAURUS_GENERATED_FILES_DIR_NAME=.docusaurus-laneb yarn build --out-dir build-laneb`.
  React (5) and TypeScript (3) breaks are permanent background noise from their own sessions.
- **Phase 10 topics 01–04 exist**, so `../../phase-10-events/04-event-delegation/README.md` is a
  safe cross-link from phase 9 — the delegation pairing is worth making on every DOM page.

⛔ **Never write in phases 3, 4, 5, 6, 7 or 8 — those are lane A's.** Not to fix a link, not to
correct a count.

## Everything else is the same as lane A

Read these from `~/.claude/CLAUDE.md` §11b and §11b-scope — they apply to both lanes:

- **Tier lock**: Understand and Know only. **Master is closed at 99/99 and is never reopened
  for depth.**
- **The scope cut**: phase 18 keeps only topics 11, 12, 15. Phases 13, 14, 15 are parked and
  16 is dropped — none of them are in either lane. **Phase 17 IS in scope** (it implements
  JavaScript's own library functions) and it is yours.
- **300 lines per file is a file-size rule, never a content budget.** Write the explanation the
  topic deserves, then split on a concept boundary.
- **No sandbox, no timings, no invented output.** Documentation-validated against MDN, named in
  the `> Verified:` line. Where a measured fact exists on an already-written page, **link to it
  rather than restating the output**.
- **Per-file cadence**: write a file → boards → clean build → commit → memory.

## Shared files — the collision rules

| File | Rule |
|---|---|
| `src/data/progress.js` | only your phases' rows |
| `docs/README.md` | only the **lane B** row |
| `docs/javascript/pages/README.md` | the lane table, and your own lane block |
| phase `README.md` | only your phases |

**Never `git add -A`.** Expect lane A's rows in your `git diff` and leave them. A build failure
under another language is not yours — tally first:

```bash
yarn build 2>&1 | grep "source page path" | sed 's#.*/devbible/docs/##' | cut -d/ -f1 | sort | uniq -c
```

## The page shape to match

Look at `docs/javascript/pages/phase-3-functions/12-composition.md` through `16-*.md` — the four
most recent lane-A pages. Understand-tier topics land at **~220–280 lines, single file**:
front matter, tier badge, `> Verified:` line, prose sections, **Gotchas** written
symptom → cause → fix, **Interview questions** with ★ on the ones that get asked, then a
prev/index/next footer.

Related: [[devbible-javascript-build-progress]] · [[devbible-javascript-syllabus]] ·
[[devbible-parallel-sessions]] · [[devbible-never-compress-to-fit-cap]]
