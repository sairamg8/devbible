---
name: research-python-p03-t07-heapq-and-bisect
description: Banked primary sources for Python phase 3 topic 07 (heapq and bisect — heap invariant, push/pop/replace/pushpop, nlargest/nsmallest, the 3.14 max-heap functions, priority queues with removal, heapq.merge, bisect_left/right, insort, key= asymmetry, range queries, sortedcontainers) — verbatim doc quotes with URLs, CPython v3.14.7 source facts with file:line, and a could-not-settle list. DO NOT RE-DERIVE.
metadata:
  type: reference
---

# research — python phase 3, topic 07 · `heapq` and `bisect`

🔴 **DO NOT RE-DERIVE.** Banked 2026-09-10 in one pass. Write every chunk from this file.

**How it was read.** Every file below was fetched from the **`v3.14.7` tag**
(`https://raw.githubusercontent.com/python/cpython/v3.14.7/<path>`) on 2026-09-10, so line
numbers are v3.14.7 line numbers. The rendered pages
<https://docs.python.org/3.14/library/heapq.html> and
<https://docs.python.org/3.14/library/bisect.html> were fetched once each the same day: both
show **3.14.7**, "last updated Sep 10, 2026", and carry the same text as the tag's `.rst`
(bisect thread-safety note present; heapq page has **no** thread-safety note). No
`library_heapq*`/`library_bisect*` dump existed in `05-slicing/_scratch/`.

Files: `Doc/library/heapq.rst` (425 lines), `Doc/library/bisect.rst` (260),
`Lib/heapq.py` (611), `Modules/_heapqmodule.c` (811), `Modules/clinic/_heapqmodule.c.h`,
`Lib/bisect.py` (118), `Modules/_bisectmodule.c` (484), `Modules/clinic/_bisectmodule.c.h`,
`Lib/test/test_heapq.py`, `Lib/test/test_bisect.py`, `Doc/whatsnew/3.14.rst`,
`Doc/library/queue.rst`, `Lib/queue.py`, `Lib/sched.py`, `Lib/asyncio/base_events.py`,
`Lib/asyncio/events.py`, `Doc/library/asyncio-eventloop.rst`, `Doc/library/asyncio-queue.rst`,
`Doc/howto/sorting.rst`, `Doc/library/threadsafety.rst`. Also **v3.13.0** `Lib/heapq.py` and
`Modules/_heapqmodule.c` (to settle what was private before 3.14).

Anchors (verified on the live page): `#heapq.heapify_max`, `#heapq.merge`, `#heapq.nlargest`,
`#priority-queue-implementation-notes`, `#bisect.bisect_left`, `#bisect.bisect_right`,
`#bisect.bisect`, `#bisect.insort_left`, `#bisect.insort_right`, `#bisect.insort`.
Also used: `#performance-notes`, `#searching-sorted-lists`, `#examples` (bisect),
`#basic-examples`, `#other-applications`, `#theory` (heapq) — standard Sphinx section slugs,
not individually verified.

---

## heapq — docs, verbatim (heapq.rst v3.14.7)

- L19–21 *"Min-heaps are binary trees for which every parent node has a value less than or equal to any of its children. We refer to this condition as the heap invariant."*
- L23–27 *"For min-heaps, this implementation uses lists for which ``heap[k] <= heap[2*k+1]`` and ``heap[k] <= heap[2*k+2]`` for all *k* for which the compared elements exist.  Elements are counted from zero.  The interesting property of a min-heap is that its smallest element is always the root, ``heap[0]``."*
- L29–34 *"Max-heaps satisfy the reverse invariant: every parent node has a value *greater* than any of its children.  These are implemented as lists for which ``maxheap[2*k+1] <= maxheap[k]`` and ``maxheap[2*k+2] <= maxheap[k]`` … The root, ``maxheap[0]``, contains the *largest* element; ``heap.sort(reverse=True)`` maintains the max-heap invariant."* ⚠️ prose says "greater", formula says `<=` — the formula is what the code does.
- L43–45 *"These two aspects make it possible to view the heap as a regular Python list without surprises: ``heap[0]`` is the smallest item, and ``heap.sort()`` maintains the heap invariant!"*
- L47–48 *"Like :meth:`list.sort`, this implementation uses only the ``<`` operator for comparisons, for both min-heaps and max-heaps."*
- L52 *"The API for max-heaps is named using a ``_max``  suffix."*
- L54–56 *"To create a heap, use a list initialized as ``[]``, or transform an existing list into a min-heap or max-heap using the :func:`heapify` or :func:`heapify_max` functions, respectively."*
- heapify L63 *"Transform list *x* into a min-heap, in-place, in linear time."*
- heappush L68 *"Push the value *item* onto the *heap*, maintaining the min-heap invariant."*
- heappop L73–75 *"Pop and return the smallest item from the *heap*, maintaining the min-heap invariant.  If the heap is empty, :exc:`IndexError` is raised.  To access the smallest item without popping it, use ``heap[0]``."*
- heappushpop L80–82 *"Push *item* on the heap, then pop and return the smallest item from the *heap*.  The combined action runs more efficiently than :func:`heappush` followed by a separate call to :func:`heappop`."*
- heapreplace L87–98 *"Pop and return the smallest item from the *heap*, and also push the new *item*. The heap size doesn't change. If the heap is empty, :exc:`IndexError` is raised."* / *"This one step operation is more efficient than a :func:`heappop` followed by :func:`heappush` and can be more appropriate when using a fixed-size heap. The pop/push combination always returns an element from the heap and replaces it with *item*."* / *"The value returned may be larger than the *item* added.  If that isn't desired, consider using :func:`heappushpop` instead.  Its push/pop combination returns the smaller of the two values, leaving the larger value on the heap."*
- Max functions L104–148, each `.. versionadded:: 3.14`: `heapify_max(x)` *"Transform list *x* into a max-heap, in-place, in linear time."*; `heappush_max(heap, item)`; `heappop_max(heap)` *"Pop and return the largest item from the max-heap *heap*, maintaining the max-heap invariant.  If the max-heap is empty, :exc:`IndexError` is raised.  To access the largest item without popping it, use ``maxheap[0]``."*; `heappushpop_max`; `heapreplace_max` *"The value returned may be smaller than the *item* added."*
- merge L154–176 *"Merge multiple sorted inputs into a single sorted output (for example, merge timestamped entries from multiple log files).  Returns an :term:`iterator` over the sorted values."* / *"Similar to ``sorted(itertools.chain(*iterables))`` but returns an iterable, does not pull the data into memory all at once, and assumes that each of the input streams is already sorted (smallest to largest)."* / *"Has two optional arguments which must be specified as keyword arguments."* / *"*reverse* is a boolean value.  If set to ``True``, then the input elements are merged as if each comparison were reversed. To achieve behavior similar to ``sorted(itertools.chain(*iterables), reverse=True)``, all iterables must be sorted from largest to smallest."* / versionchanged 3.5 added key and reverse.
- nlargest L181–185 *"Return a list with the *n* largest elements from the dataset defined by *iterable*. … Equivalent to:  ``sorted(iterable, key=key, reverse=True)[:n]``."*; nsmallest L190–193 *"Equivalent to:  ``sorted(iterable, key=key)[:n]``."*
- L196–200 *"The latter two functions perform best for smaller values of *n*.  For larger values, it is more efficient to use the :func:`sorted` function.  Also, when ``n==1``, it is more efficient to use the built-in :func:`min` and :func:`max` functions.  If repeated usage of these functions is required, consider turning the iterable into an actual heap."*
- heapsort example L210–220 then *"This is similar to ``sorted(iterable)``, but unlike :func:`sorted`, this implementation is not stable."*
- L222–223 *"Heap elements can be tuples.  This is useful for assigning comparison values (such as task priorities) alongside the main record being tracked"*
- Running median L244–262 (two heaps, `heappush_max(lo, heappushpop(hi, x))` / `heappush(hi, heappushpop_max(lo, x))`) — *"A running median can be efficiently implemented by balancing two heaps, a max-heap for values at or below the midpoint and a min-heap for values above the midpoint."*
- Priority queue notes L273–339: the four challenges (stability; *"Tuple comparison breaks for (priority, task) pairs if the priorities are equal and the tasks do not have a default comparison order."*; changed priority; delete). L288–292 *"A solution to the first two challenges is to store entries as 3-element list including the priority, an entry count, and the task.  The entry count serves as a tie-breaker so that two tasks with the same priority are returned in the order they were added. And since no two entry counts are the same, the tuple comparison will never attempt to directly compare two tasks."* L294–295 wrapper class *"that ignores the task item and only compares the priority field"* (`@dataclass(order=True) class PrioritizedItem: priority: int; item: Any=field(compare=False)`). L305–311 *"Finding a task can be done with a dictionary pointing to an entry in the queue."* / *"Removing the entry or changing its priority is more difficult because it would break the heap structure invariants.  So, a possible solution is to mark the entry as removed and add a new entry with the revised priority"*. Recipe L313–339: `pq`, `entry_finder`, `REMOVED = '<removed-task>'`, `counter = itertools.count()`, `add_task` (removes existing then pushes `[priority, count, task]`), `remove_task` (`entry = entry_finder.pop(task); entry[-1] = REMOVED`, KeyError if absent), `pop_task` (loop heappop, skip REMOVED, `del entry_finder[task]`, raise `KeyError('pop from an empty priority queue')`).
- Theory L345–348 *"For the sake of comparison, non-existing elements are considered to be infinite."*; L372–377 percolate *"This is clearly logarithmic on the total number of items in the tree. By iterating over all items, you get an *O*\ (*n* log *n*) sort."*; L379–386 *"you can efficiently insert new items while the sort is going on, provided that the inserted items are not "better" than the last 0'th element you extracted.  This is especially useful in simulation contexts"* … *"a heap is a good structure for implementing schedulers"*; L388–392 *"the speed is almost constant, and the worst case is not much different than the average case."*; L394–402 disk sorts / runs *"twice the size of the memory for random input"*.
- **No thread-safety note on the heapq page** (confirmed on live page).

## What's New 3.14 (whatsnew/3.14.rst L1402–1412)
*"The :mod:`!heapq` module has improved support for working with max-heaps, via the following new functions:"* heapify_max, heappush_max, heappop_max, heapreplace_max, heappushpop_max. URL: https://docs.python.org/3.14/whatsnew/3.14.html (heapq section).

## heapq — source facts (Lib/heapq.py v3.14.7)
- L129–131 `__all__` = heappush, heappop, heapify, heapreplace, heappushpop, heappush_max, heappop_max, heapify_max, heapreplace_max, heappushpop_max, nlargest, nsmallest, merge.
- L133–136 heappush = `heap.append(item)` + `_siftdown(heap, 0, len(heap)-1)`.
- L138–146 heappop: `lastelt = heap.pop()` (*"raises appropriate IndexError if heap is empty"*), then `returnitem = heap[0]; heap[0] = lastelt; _siftup(heap, 0)`.
- L148–162 heapreplace docstring: *"Note that the value returned may be larger than item!  That constrains reasonable uses of this routine unless written as part of a conditional replacement: if item > heap[0]: item = heapreplace(heap, item)"*.
- L164–169 heappushpop: `if heap and heap[0] < item: item, heap[0] = heap[0], item; _siftup(heap, 0)`; `return item` → **on a tie (`heap[0] == item`) it returns the new item and leaves the heap untouched.**
- L171–180 heapify: `for i in reversed(range(n//2)): _siftup(x, i)` — docstring *"in O(len(x)) time"*.
- L182–215 max versions; L206 heappushpop_max `if heap and item < heap[0]`.
- L235–272 comment: bottom-up siftup (*"We *could* break out of the loop as soon as we find a pos where newitem <= both its children, but turns out that's not a good idea"*, Knuth vol. 3). L248–252 *"Cutting the # of comparisons is important, since these routines have no way to extract "the priority" from an array element, so that intelligence is likely to be hiding in custom comparison methods, or in array elements storing (priority, record) tuples.  Comparisons are thus potentially expensive."* L265–267 *"Building the heap by using heappush() 1000 times instead required 2198, 2148, and 2219 compares:  heapify() is more efficient, when you can use it."* L269–272 *"list.sort() is (unsurprisingly!) more efficient for sorting."* (these compare counts are the SOURCE's own measurements — quotable as such, never as ours).
- L330–408 merge: builds `h` of `[value, order * direction, next]` (key: `[key(value), order*direction, value, next]`), `_heapify(h)`, loop yields `h[0]` value, advances that iterator, `_heapreplace(h, s)`; on StopIteration `_heappop(h)`; when one iterator left: `yield from next.__self__`. `reverse=True` swaps in heapify_max/heappop_max/heapreplace_max and `direction = -1`. Docstring: *"returns a generator"*. **order index breaks ties → earlier iterable wins → stable** (test_heapq L350–363 `test_merge_stability`). Key called **once per element** (L390, L400). Holds **one pending element per non-exhausted input**.
- L411–475 algorithm notes for nlargest/nsmallest: *"Make a single pass over the data while keeping the k most extreme values in a heap.  Memory consumption is limited to keeping k values in a list."* — table of comparisons (source's numbers). *"Other algorithms were not used because they: 1) Took much more auxiliary memory, 2) Made multiple passes over the data. 3) Made more comparisons in common cases (small k, large n, semi-random input)."*
- nsmallest L477–535: n==1 → `min(it, default=sentinel, key=key)` → `[]` or `[result]`; `try: size = len(iterable)` → if `n >= size: return sorted(iterable, key=key)[:n]`; key None: `result = [(elem, i) for i, elem in zip(range(n), it)]` (*"put the range(n) first so that zip() doesn't consume one too many elements from the iterator"*), `heapify_max(result)`, loop `if elem < top: _heapreplace(result, (elem, order))` (strict `<` → earlier equal wins), `result.sort()`. Key path: `(key(elem), i, elem)` → **the element itself is never compared when a key is given** (unique i). n <= 0 → `zip(range(n), …)` empty → `[]`.
- nlargest L537–593: mirror with `range(0, -n, -1)` orders, min-heap `heapify`, `if top < elem`, `result.sort(reverse=True)`.
- L595–599 `from _heapq import *` overrides the Python functions; nlargest/nsmallest/merge stay Python and call the C push/pop via module globals.
- L601–606 *"For backwards compatibility"* aliases `_heappop_max`, `_heapreplace_max`, `_heappush_max`, `_heappushpop_max`, `_heapify_max`.
- **v3.13.0** `Lib/heapq.py`: private `_heappop_max` (L181), `_heapreplace_max` (L191), `_heapify_max` (L198) only; `_heapq` C in 3.13.0 exported `_heappop_max`, `_heapify_max`, `_heapreplace_max`. No push-max / pushpop-max existed before 3.14.

## heapq — C (Modules/_heapqmodule.c v3.14.7)
- L25–68 siftdown; L53–57 raises `RuntimeError("list changed size during iteration")` if a comparison changed the list size; L34–37 / L80–83 `IndexError("index out of range")`.
- L70–118 siftup (bottom-up, `childpos += ((unsigned)cmp ^ 1)` picks right child when `not left < right`).
- L120–150 heappush: clinic `@critical_section heap`, `heap: object(subclass_of='&PyList_Type')`, positional-only (`/`). L140–144 comment *"In a free-threaded build, the heap is locked at this point."* Appends first (`_PyList_AppendTakeRef`), then siftdown; **if siftdown fails (comparison raised) the item is already in the list** (L142–148).
- L152–183 heappop_internal: empty → `IndexError("index out of range")` (L160–162) — note: C message differs from Python's `list.pop` message. Removes last, stores it at slot 0, siftup; **on failure `Py_DECREF(returnitem)` — the popped top is dropped: gone from the heap and not returned** (L175–181). Same shape in heapreplace_internal L202–220 (old top dropped, new item in heap).
- L263–297 heappushpop: empty → return item; `cmp = top < item`; `cmp == 0 → return item` (heap untouched).
- L311–362 cache_friendly_heapify; L369–375 used when `n > 2500`; comment L325–328 *"both ways do the exact same number of comparisons and produce exactly the same heap."*
- L407–635 `_max` variants; L637–651 method table (10 functions); L791 `Py_mod_gil, Py_MOD_GIL_NOT_USED`.
- clinic `Modules/clinic/_heapqmodule.c.h` L30–33: `if (!PyList_Check(args[0])) { _PyArg_BadArgument("heappush", "argument 1", "list", args[0]);` → deque/UserList/array → TypeError; list **subclasses** accepted. L36 `Py_BEGIN_CRITICAL_SECTION(heap);` on every function.
- test_heapq L383–403 `test_comparison_operator`: `__lt__` alone suffices; `__le__` alone raises TypeError (*"For python 3.0, __le__ alone is not enough"*). L567–629 comparisons that mutate the heap → IndexError/RuntimeError.

## bisect — docs, verbatim (bisect.rst v3.14.7)
- L14–17 *"This module provides support for maintaining a list in sorted order without having to sort the list after each insertion.  For long lists of items with expensive comparison operations, this can be an improvement over linear searches or frequent resorting."*
- L19–25 *"Unlike other bisection tools that search for a specific value, the functions in this module are designed to locate an insertion point. Accordingly, the functions never call an :meth:`~object.__eq__` method to determine whether a value has been found.  Instead, the functions only call the :meth:`~object.__lt__` method and will return an insertion point between values in an array."*
- L27–35 note: *"The functions in this module are not thread-safe. If multiple threads concurrently use :mod:`!bisect` functions on the same sequence, this may result in undefined behaviour. Likewise, if the provided sequence is mutated by a different thread while a :mod:`!bisect` function is operating on it, the result is undefined. For example, using :py:func:`~bisect.insort_left` on the same list from multiple threads may result in the list becoming unsorted."*
- bisect_left L42–64 *"Locate the insertion point for *x* in *a* to maintain sorted order. The parameters *lo* and *hi* may be used to specify a subset of the list which should be considered; by default the entire list is used.  If *x* is already present in *a*, the insertion point will be before (to the left of) any existing entries.  The return value is suitable for use as the first parameter to ``list.insert()`` assuming that *a* is already sorted."* / *"The returned insertion point *ip* partitions the array *a* into two slices such that ``all(elem < x for elem in a[lo : ip])`` is true for the left slice and ``all(elem >= x for elem in a[ip : hi])`` is true for the right slice."* / *"*key* specifies a :term:`key function` of one argument that is used to extract a comparison key from each element in the array.  To support searching complex records, the key function is not applied to the *x* value."* / *"If *key* is ``None``, the elements are compared directly and no key function is called."* / versionchanged 3.10 key.
- bisect_right / bisect L67–78 *"Similar to :py:func:`~bisect.bisect_left`, but returns an insertion point which comes after (to the right of) any existing entries of *x* in *a*."* / partition `all(elem <= x …)` / `all(elem > x …)`.
- insort_left L81–96 *"This function first runs :py:func:`~bisect.bisect_left` to locate an insertion point. Next, it runs the :meth:`~sequence.insert` method on *a* to insert *x* at the appropriate position to maintain sort order."* / *"To support inserting records in a table, the *key* function (if any) is applied to *x* for the search step but not for the insertion step."* / *"Keep in mind that the *O*\ (log *n*) search is dominated by the slow *O*\ (*n*) insertion step."*
- insort_right / insort L99–116 *"Similar to :py:func:`~bisect.insort_left`, but inserting *x* in *a* after any existing entries of *x*."*
- Performance Notes L122–137: *"Bisection is effective for searching ranges of values. For locating specific values, dictionaries are more performant."* / *"The *insort()* functions are *O*\ (*n*) because the logarithmic search step is dominated by the linear time insertion step."* / *"The search functions are stateless and discard key function results after they are used.  Consequently, if the search functions are used in a loop, the key function may be called again and again on the same array elements. If the key function isn't fast, consider wrapping it with :py:deco:`functools.cache` to avoid duplicate computations.  Alternatively, consider searching an array of precomputed keys to locate the insertion point (as shown in the examples section below)."*
- seealso L141–149: Sorted Collections (https://grantjenks.com/docs/sortedcollections/) *"is a high performance module that uses *bisect* to managed sorted collections of data."*; SortedCollection recipe (https://code.activestate.com/recipes/577197-sortedcollection/) *"The keys are precomputed to save unnecessary calls to the key function during searches."*
- Searching Sorted Lists L155–193: *"The above `bisect functions`_ are useful for finding insertion points but can be tricky or awkward to use for common searching tasks."* — `index`, `find_lt`, `find_le`, `find_gt`, `find_ge` (all raise ValueError on miss).
- Examples L201–211 grade: `i = bisect([60, 70, 80, 90], score); return "FDCBA"[i]` — *"90 and up is an 'A', 80 to 89 is a 'B', and so on"*. L213–245 Movie namedtuple + `attrgetter('released')`, `movies[bisect(movies, 1960, key=by_year)]`, `insort(movies, romance, key=by_year)`. L247–260 precomputed keys `keys = [r[1] for r in data]; data[bisect_left(keys, 0)]`.

## bisect — source facts
- `Lib/bisect.py`: insort_right L4–18 (`lo = bisect_right(a, key(x), lo, hi, key=key)`; `a.insert(lo, x)`); bisect_right L21–54 (`if lo < 0: raise ValueError('lo must be non-negative')` L34–35; `if hi is None: hi = len(a)`; comment L38–39 *"the comparison uses "<" to match the __lt__() logic in list.sort() and in heapq."*; `if x < a[mid]: hi = mid else: lo = mid + 1`); insort_left L57–72; bisect_left L74–107 (`if a[mid] < x: lo = mid + 1 else: hi = mid`); L110–114 `from _bisect import *`; L116–118 `bisect = bisect_right`, `insort = insort_right`.
- `Modules/_bisectmodule.c`: get_sq_item L32–50 — any type with `sq_item` works (list, tuple, str, range, array, deque, Python classes with `__getitem__`); mapping → `"%.200s is not a sequence"` (L43); else `"'%.200s' object does not support indexing"` (L46). internal_bisect_right L52–149: lo<0 ValueError L60–63; `hi == -1` → `PySequence_Size(list)` L64–68; key called per probe L90–96; `mid = ((size_t)lo + hi) / 2` (issue 13496); same-type fast path; NotImplemented fallback. insort_right L201–234: key applied to x once (L212); `PyList_CheckExact(a)` → `PyList_Insert`, else calls `a.insert(index, x)` via method (L221–231) → list subclasses' overridden insert and deque.insert are honoured. internal_bisect_left L236–333; insort_left L387–419. L465 `Py_MOD_GIL_NOT_USED`. **No critical section in `_bisect`** (contrast heapq).
- clinic `_bisectmodule.c.h`: `hi: Py_ssize_t(c_default='-1', accept={int, NoneType}) = None`; L70 `Py_ssize_t hi = -1;`; L102 `_Py_convert_optional_to_ssize_t`. ⇒ **implementation quirk: an explicit `hi=-1` is indistinguishable from `None` in C (searches to len(a)), while pure-Python `bisect.py` treats `hi=-1` as an empty range and returns `lo`.** Undocumented; do not rely on either.
- test_bisect L121–127 negative lo → ValueError; L129–154 `range(sys.maxsize-1)` works (virtual sequence); L260–264 `test_insort_keynotNone` (key="b" → TypeError); L266–301 `__lt__` returning non-bool / NotImplemented handled; L318–327 `test_listDerived` insort calls subclass `insert`; L358–371 non-sequence / len-only / getitem-only → TypeError.

## Neighbouring stdlib, verbatim
- queue.rst L11–14 *"The :mod:`!queue` module implements multi-producer, multi-consumer queues. It is especially useful in threaded programming when information must be exchanged safely between multiple threads.  The :class:`Queue` class in this module implements all the required locking semantics."*; L20–22 *"With a priority queue, the entries are kept sorted (using the :mod:`heapq` module) and the lowest valued entry is retrieved first."*; PriorityQueue L59–64 *"The lowest valued entries are retrieved first (the lowest valued entry is the one that would be returned by ``min(entries)``).  A typical pattern for entries is a tuple in the form: ``(priority_number, data)``."* / *"If the *data* elements are not comparable, the data can be wrapped in a class that ignores the data item and only compares the priority number"*. `Lib/queue.py` L280–296: `_put` = heappush, `_get` = heappop, `self.queue = []`.
- asyncio-queue.rst L14–16 *"asyncio queues are designed to be similar to classes of the :mod:`queue` module.  Although asyncio queues are not thread-safe, they are designed to be used specifically in async/await code."*; PriorityQueue L160–166 *"retrieves entries in priority order (lowest first)."*
- asyncio-eventloop.rst L293–295 (call_later) *"*callback* will be called exactly once.  If two callbacks are scheduled for exactly the same time, the order in which they are called is undefined."* — the heap-without-counter consequence, documented.
- `Lib/asyncio/base_events.py`: L55–62 `_MIN_SCHEDULED_TIMER_HANDLES = 100`, `_MIN_CANCELLED_TIMER_HANDLES_FRACTION = 0.5`; L811–815 `heapq.heappush(self._scheduled, timer)`; L1972–1975 cancel only counts; L1984–2006 `_run_once`: if >100 scheduled and >50% cancelled → rebuild list without cancelled + `heapq.heapify(new_scheduled)`; else pop cancelled from head. L2027–2032 pops due timers. `Lib/asyncio/events.py` L162–165 `TimerHandle.__lt__` compares `_when` only (no sequence).
- `Lib/sched.py` L35 `Event = namedtuple('Event', 'time, priority, sequence, action, argument, kwargs')`; L40–41 sequence doc *"A continually increasing sequence number that separates events if time and priority are equal."*; L62–75 enterabs (under `self._lock`, heappush); L87–96 cancel = `self._queue.remove(event)` + `heapq.heapify(self._queue)` (O(n)); L155–167 `queue` property copies and pops with heappop *"With heapq, two events scheduled at the same time will show in the actual order they would be retrieved."*
- functions.rst (max) *"If multiple items are maximal, the function returns the first one encountered.  This is consistent with other sort-stability preserving tools such as ``sorted(iterable, key=keyfunc, reverse=True)[0]`` and ``heapq.nlargest(1, iterable, key=keyfunc)``."* (min mirror with nsmallest).
- howto/sorting.rst "Partial Sorts" L392–411: *"heapq.nsmallest and heapq.nlargest return the n smallest and largest values, respectively.  These functions make a single pass over the data keeping only n elements in memory at a time.  For values of n that are small relative to the number of inputs, these functions make far fewer comparisons than a full sort."*
- time-complexity.html#list: Insert `l.insert(k, x)` **O(n - k)**, Pop `l.pop(k)` O(n - k), footnote [2] *"inserting at index *k* shifts the elements from *k* onwards one slot to the right, moving *n* - *k* elements."*; Sort O(n log n) worst, footnote [4] *"Sorting is adaptive and input that is already sorted or reverse-sorted takes only *O*\ (*n*) comparisons."*
- collections.rst deque *"Indexed access is *O*\ (1) at both ends but slows to *O*\ (*n*) in the middle.  For fast random access, use lists instead."*

## sortedcontainers (third-party, NOT pinned in src/data/pins.js)
- PyPI latest **2.4.0**, uploaded 2021-05-16 (pypi.org/pypi/sortedcontainers/json, 2026-09-10).
- https://grantjenks.com/docs/sortedcontainers/sortedlist.html — *"Sorted list is a sorted mutable sequence."* / *"Sorted list values must be comparable. The total ordering of values must not change while they are stored in the sorted list."* add/remove/bisect/getitem listed as O(log(n)) "approximate" (fetched via summariser — treat exact complexity wording as unconfirmed; quote only the two sentences above). `irange(minimum=None, maximum=None, inclusive=(True, True), reverse=False)`. SortedKeyList: *"maintains values in comparison order based on the result of a key function applied to every value."*
- Pin rule: a library taught in depth needs a pin in `src/data/pins.js`. This topic only **names** it as the alternative — report, do not pin (outside dir).

## Corpus pages already covering part of this (link, don't repeat)
- `01-list-internals/03d-partial-sorts-and-counting.md` — re-sort-per-insert, repeated max, insort O(n) quote, nlargest intro.
- `01-list-internals/07b-merging-and-galloping.md` — heapq.merge for external sort; `07c-galloping.md` — gallop_left/right ≈ bisect_left/right, insort_left vs insort_right with equal keys.
- `01-list-internals/08-stability-and-multi-key-sorts.md`, `08b-ties-and-determinism.md` — min/max/nlargest stability.
- `01-list-internals/11c-thread-safety-under-free-threading.md`, `11d-sharing-a-list-between-threads.md`.
- `02-tuple/09-comparison-and-ordering.md`, `02-tuple/09b-sort-keys-and-priority-queues.md` — tuple comparison, `(priority, count, job)` basics, `bisect_left(keys, (tenant,))` prefix search.
- `03-dict/09c-dict-equality.md` — dict `<` TypeError in heaps; `03-dict/01b-what-o1-does-not-promise.md` — range query → bisect.
- `06-collections-module/03b-counter-top-n.md` — most_common(n) = nlargest.
- `../phase-1-language-core/06-comparisons/08c-min-max-heapq-bisect-groupby.md` — key= across tools, bisect key not applied to x.
- `../../../../postgresql/pages/phase-10-indexes/02-btree.md`; `../../../../dsa/pages/phase-2-recursion-maths-bits/02i-binary-search-as-degenerate-divide-and-conquer.md`.

## Could NOT settle (write as uncertain or leave out)
1. Whether heapq calls are **documented** thread-safe/atomic under free-threading — docs silent; source shows a per-call critical section on the list (implementation detail only). A comparison that runs Python code can in principle let another thread in (critical sections can be suspended) — not confirmed from docs; state as "do not rely on it; lock or use queue.PriorityQueue".
2. Any timing/crossover `n` at which nlargest stops beating sorted — docs say only "smaller values of n"; the source's comparison-count table is the only number available, and it counts comparisons, not time.
3. Exact sortedcontainers complexity wording (fetched through a summariser).
4. Whether the docs' "greater" vs `<=` wording for max-heaps is an intended distinction — treat the formula as authoritative.
5. Exact rendered error strings: derived from source format strings (`"index out of range"`, `"lo must be non-negative"`, `"%.200s is not a sequence"`, `"'%.200s' object does not support indexing"`, `_PyArg_BadArgument(..., "list", ...)` → "must be list"). Quote the source strings, not a transcript.

## Added during writing (2026-09-10)
- `Modules/itertoolsmodule.c` v3.14.7 lines 3650–3678 `count_next`: default build increments `lz->cnt++` in C; free-threaded build uses `_Py_atomic_compare_exchange_ssize` (slow mode under a critical section) ⇒ `next(itertools.count())` returns unique values across threads (source only; docs silent). The `05-slicing/_scratch` copy of this file was diffed identical to the tag.
- reference/datamodel.rst (`object.__hash__` note): *"By default, the `__hash__()` values of str and bytes objects are "salted" with an unpredictable random value.  Although they remain constant within an individual Python process, they are not predictable between repeated invocations of Python."* — https://docs.python.org/3.14/reference/datamodel.html#object.__hash__
- Prometheus https://prometheus.io/docs/concepts/metric_types/ — *"every bucket counts all observations less than or equal to the upper boundary provided as a label"*; series *"`<basename>_bucket{le="<upper inclusive bound>"}`"*.
- asyncio-eventloop.rst call_later: *"If two callbacks are scheduled for exactly the same time, the order in which they are called is undefined."*

## Chunk map (topic CLOSED 2026-09-10, 18 chunks + README, positions 1–18)
01 invariant · 01b building/sifting · 02 push/pop/replace/pushpop · 02b comparison failures (source-derived) · 03 nlargest mechanism/cost · 03b ties/keys/top-N/DB · 04 max-heaps 3.14 · 05 priority queues/counter · 05b removal recipe · 05c stale entries/compaction/Dijkstra · 05d threads/asyncio/processes · 06 merge · 07 bisect left/right · 07b key= asymmetry/binary search on answer · 08 insort cost · 09 range queries · 09b breakpoint tables/rings · 10 when not heapq/bisect.
