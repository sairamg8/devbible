---
title: "Java — Explanations"
sidebar_label: "Overview"
sidebar_position: 0
---

Status board for the Java explanation pages — one page per syllabus topic, with
code, gotchas and interview questions.

**Writing in progress** — approved 2026-08-17, written phase by phase in
reading order. The [syllabus](../README.md) is complete: 232 topics, 17 phases.

import Progress from '@site/src/components/Progress';

<Progress lang="java" />

| Phase | Topics | State |
|---|---|---|
| 0 · The platform and the JVM | 13 | ✅ 13/13 |
| 1 · Language core | 16 | ✅ 16/16 |
| 2 · Classes and objects | 15 | ✅ 15/15 |
| 3 · Generics and collections | 16 | ✅ 16/16 |
| 4 · Lambdas, streams and `Optional` | 13 | ✅ 13/13 |
| 5 · Exceptions and failure design | 8 | ✅ 8/8 |
| 6 · Concurrency | 17 | ✅ 17/17 |
| 7 · I/O, time and the everyday stdlib | 13 | ✅ 13/13 |
| 8 · The build: Maven, Gradle, dependencies | 12 | ✅ 12/12 |
| 9 · Spring Boot and the web | 16 | ✅ 16/16 — complete |
| 10 · Data access | 14 | ✅ **CLOSED 14/14** — topics 01–08 complete (**335 chunks, ~88,000 lines**): JDBC · connection pooling · JDBC transactions · Spring `@Transactional` · SQL-first access · the JPA/Hibernate model · relationships and fetch types · the N+1 problem. **09 · Spring Data JPA (49 chunks), 13 · jOOQ (33) and 14 · Spring Data Mongo/Redis (26) closed 2026-08-27** with their indexes. **The last three closed 2026-08-27**: 12 · Caching (35 chunks), 10 · Lazy-loading pitfalls (35) and 11 · Migrations with Flyway (44), each with its index. The phase is **569 files / ~141,700 lines**, **0 over the 300-line cap, 0 dangling links, 0 MDX hazards**, all 14 topics indexed. Two chunks that committed pages linked to by name but nobody had written — Flyway's `10c` and caching's `07d` — were found by resolving every link target against the filesystem and have been authored |
| 11 · Testing | 12 | 🚧 **IN PROGRESS — 11/12 closed** (session `a3339484`, 2026-09-01). ✅ **12 · Real-world testing scenarios CLOSED — 50 chunks + index, 11,818 lines, 598 ★**, contiguous 1–50, built by three forks on disjoint bands under a hard 3-agent ceiling. *(Earlier the same day: 10/12 closed.)* **389 chunks, ~93,800 lines on disk.** ✅ **10 · Property-based testing CLOSED 2026-09-01 — 40 chunks + index, 9,469 lines, 389 ★**, contiguous 1–40, two proven splits, and two jqwik documentation inconsistencies stated in-page as unresolved rather than guessed. *(Previously: 9/12 closed, session `01cb3b13`, 2026-08-31.)* **349 chunks, ~84,300 lines on disk.** ✅ **09 · Coverage with JaCoCo CLOSED 2026-08-31 — 22 chunks + index, 5,306 lines, 304 ★**, positions 0–22, 0 over the cap, 0 dangling links, one proven split (324/16 → 407/23). Its argument rests on JaCoCo's own counter definitions: *"exception handling is not considered as branches"* and *"try/catch blocks will also not increase complexity"*, so the counter you are told to gate on cannot see error handling at all. 🔴 Three widely-repeated claims were falsified against primary sources: `jakarta.annotation.Generated` is `@Retention(SOURCE)` and therefore **not** filtered; Lombok does **not** add `@lombok.Generated` unless `lombok.addLombokGeneratedAnnotation` is set; and an unspecified `check` limit is `BUNDLE`/`INSTRUCTION`/`COVEREDRATIO`, not line coverage. 🔴 **The remaining three topics (10 jqwik, 11 PIT, 12 real-world scenarios) are being written concurrently by four `devbible-author` forks** on the user's instruction to finish the phase — one topic per fork, except topic 12 which is split across two forks on disjoint `sidebar_position` bands (1–29 / 30–59) for the coordinator to renumber at close. ✅ **08 · Test data patterns CLOSED 2026-08-31 — 34 chunks + index, ~8,999 lines, 450 ★**, built by the coordinator and two `devbible-author` forks inside the one topic on disjoint `sidebar_position` bands, renumbered contiguously 1–34 at close. 🔴 **Eleven splits, every one proven** by recording `wc -l` and `grep -c '^**★'` before and demanding both after — 457→836 lines (17→40 ★), 348→551 (17→26), 337→480 (15→23), 369→562 (15→26), 321→486 (15→24), 319→578 (16→28), 302→435 (16→21) and four more. 🔴 **Three documented facts contradicted the coordinator's briefs and the authors who verified were right every time**: `@AutoConfigureTestDatabase.replace` defaults to **`NON_TEST`**, not `ANY`, so the ubiquitous "`replace = Replace.NONE` beside a container" advice is stale on Boot 4.1; **Boot 4 moved the slice annotations** out of `org.springframework.boot.test.autoconfigure.*` — `TestEntityManager` now lives in `org.springframework.boot.jpa.test.autoconfigure`, *not* `data.jpa`; and **Lombok's `@Builder` ignores field initializers** unless they carry `@Builder.Default`, which inverts the one property a test-data builder exists for. 🔴 **A new topic 12 · Real-world testing scenarios was added on the user's instruction** — task-shaped (mock a class, mock an API response, test a controller, an async job, a consumer), with a JS/React → Java mapping chunk; `_plan.md` written, no chunks yet. 🚧 **IN PROGRESS — 7/11 closed** (session `3c755994`, 2026-08-31 10:17). **267 chunks, ~70,000 lines on disk.** ✅ **07 · Testcontainers CLOSED 2026-08-31 — 50 chunks + index, ~11,900 lines, 657 ★**, the phase's third-largest topic, built by **four agents inside the one topic** on disjoint `sidebar_position` bands after the user raised the ceiling (*"along with you deploy 3 more agents"*). It began at 3 chunks and 3 `draft: true` stubs; **all three stubs are gone**. 🔴 **Sixteen splits, every one proven** by recording `wc -l` and `grep -c '^**★'` before and demanding both after — 1,277→2,221 lines (41→128 ★), 490→1,339 (22→77), 650→892 (30→46), 565→835 (21→46), 487→701 (21→40) and eleven more; **no gotcha or question was lost anywhere**. 🔴 **Four claims in the coordinator's own briefs and in the banked research were contradicted by the sources, and every one was caught by an author told to verify rather than comply**: dynamic property *values* are not in the context cache key (`DynamicPropertiesContextCustomizer` keys on `Set<Method>`, so subclasses sharing an inherited `@DynamicPropertySource` share a context); Ryuk is **0.14.0**, not the research file's 0.13.0; H2 uses SQLState **23505** exactly as PostgreSQL does, so the brief's example was backwards (the real divergences are `23513` vs `23514` and H2's `40001` where PostgreSQL splits `40001`/`40P01`); and **the already-committed `01b` wrongly listed `SKIP LOCKED` and `DISTINCT ON` as constructs H2 cannot parse** — H2 2.4.240 parses both, verified against its own grammar, and the correction made the page's argument stronger because a construct H2 *accepts and approximates* costs a green build where one it rejects costs a red one. ✅ **06 · MockMvc CLOSED 2026-08-30 — 34 chunks + index, ~8,000 lines, 406 ★**, written by two `devbible-author` forks and the coordinator working **inside the one topic** on disjoint files, per the standing order to finish a topic before opening another. A 15-line security stub became **nine** chunks and a 14-line exception-handler stub became **six** — both split under a before/after line-and-★ audit that only ever went up (465→2,360 lines / 22→134 ★, and 475→1,490 / 20→78). 🔴 Closing it also cleared a **314-line cap violation** that had survived a commit in `06-validation-errors.md`, split on the mechanism/practice seam to 204+143 with all 16 ★ preserved and 0 source lines lost. Both forks contradicted the coordinator's dispatch brief and were right to: `ResponseStatusException` **extends** `ErrorResponseException` (so it *is* caught by any `ResponseEntityExceptionHandler`), `@ResponseStatus` shares `@Valid`'s `sendError` empty-body problem rather than differing from it, and the "your `SecurityConfig` is not in the slice" finding is **documented outright** in Boot's how-to rather than being the inference the brief called it — that hedge in `02-webmvctest.md` was upgraded to a citation. ✅ **04 · Mockito CLOSED — 57 chunks + index, ~14,100 lines, 758 interview questions.** Its two longest-outstanding arcs were written this session by a `devbible-author` fork — spies (08/08b/08c/08d/08e) and `@InjectMocks` (09/09b/09c/09d/09e) — plus the contract-testing arc 12c/12d/12e that 12b had promised twice in prose. 🔴 **Each was split under a before/after line-and-★ audit and every one expanded rather than lost content**: 394→1,383 lines (19→76 ★), 335→1,236 (19→68), 328→645 (10→34). That check is the only thing that distinguishes a split from a deletion, and this phase had already produced three deletions. Positions were then renumbered contiguous 1–57 **without renaming a single file** — the diff is 17 lines, all of them `sidebar_position` — because two forks had shared the directory with reserved bands and inbound links point at exact filenames. ✅ **05 · The test pyramid CLOSED — 22 chunks + index, ~4,950 lines, 300 interview questions**, written by the coordinator while a `devbible-author` fork worked topic 04. It owns slice mechanics, the ten-component context cache key and the bean-override family, and it carries three findings that invalidate most published Spring-testing material: `@MockBean`/`@SpyBean` were **removed** in Boot 4 with their replacements moved into Spring **Framework**; `@AutoConfigureTestDatabase`'s `replace` now defaults to `NON_TEST`, so `@DataJpaTest` no longer swaps a Testcontainers `@ServiceConnection` datasource for H2 (and the habitual `replace = NONE` is the *less* safe option, failing open); and a `REPLACE`-strategy override registers a bare manual singleton with **no AOP advice at all**, so a test asserting that `@Retryable` retries has nothing left to assert on and passes. The topic's uncertainty is stated rather than guessed: no Spring, Boot or JUnit reference defines the pyramid or endorses any ratio. ✅ **01 · JUnit 5 CLOSED — 62 chunks + index, ~14,500 lines**, the largest topic in the Java corpus. Closing it repaired **four live 404s inside the already-closed topics 02 and 03**, which had been shipping links to an index nobody had written; a fifth dangling link (to the unwritten topic 08) was converted to plain text rather than left pointing at nothing. ✅ **02 · AssertJ CLOSED — 25 chunks + index**, sourced mostly from the `assertj-core` 3.27.7 **class javadocs** at tag `assertj-build-3.27.7`, because the doc site truncates before soft assertions, custom assertions, `Optional`, temporal assertions and descriptions. ✅ **03 · Parameterized tests CLOSED — 38 chunks + index**. 🔴 **04 · Mockito (44 chunks) is written but NOT closed** — it still owes `08-spies`, `09-injectmocks`, `12c-contract-testing-a-fake`, a contiguous renumber (positions run 1–30 then jump to 50–63) and its `README.md` index. 05 · The test pyramid, 06 · MockMvc and 07 · Testcontainers opened at 2 chunks each before their forks were wound down at a 3-agent ceiling; their documentation research is banked in the store (`research_java_p11_t0{4,5,6,7}_*.md`) so it is not re-derived. 🔴 The recurring defect in this phase is a fork answering "split this file" by **deleting** content instead: it happened three times, was caught each time only by comparing line and ★ counts before and after, and one file (`04/06e`) had to be restored from a fork's context because git had never seen it. A trim passes the cap, MDX and link checks and looks identical to a split in a file listing |
| 12 · The JVM in production | 15 | Planned |
| 13 · OAuth2, OIDC and service security | 14 | 🚧 3 of 14 |
| 14 · Microservice architecture | 12 | Planned |
| 15 · Messaging and event-driven | 14 | Planned |
| 16 · Resilience and operating the fleet | 13 | Planned |
