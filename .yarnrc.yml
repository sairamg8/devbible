# Pinned explicitly: locally this comes from ~/.yarnrc.yml, which CI has no
# copy of, and Yarn 4 would otherwise fall back to PnP on the runner.
nodeLinker: node-modules

approvedGitRepositories:
  - "**"

enableScripts: true

npmMinimalAgeGate: 0
