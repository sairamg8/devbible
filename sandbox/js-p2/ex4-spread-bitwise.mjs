console.log('--- spread copies are shallow ---');
const orig = { a: 1, nested: { b: 2 } };
const copy = { ...orig }; copy.nested.b = 99;
console.log('  orig.nested.b after copy mutation =', orig.nested.b);
console.log('  object spread order matters:', JSON.stringify({ ...{a:1,b:2}, b:3 }), 'vs', JSON.stringify({ b:3, ...{a:1,b:2} }));
console.log('  spread SKIPS undefined? ', JSON.stringify({ ...{a:undefined} }));
console.log('  array spread of string:', [...'hi']);
console.log('  spread non-iterable {}:', (()=>{ try { return [...{}]; } catch(e){ return e.constructor.name+': '+e.message; } })());
console.log('  object spread of array:', JSON.stringify({ ...['x','y'] }));
console.log('\n--- rest ---');
const f = (first, ...rest) => ({ first, rest, len: f.length });
console.log('  (first,...rest) length property =', ((a,...r)=>0).length, '<- rest not counted');
const { a, ...others } = { a:1, b:2, c:3 };
console.log('  object rest:', a, JSON.stringify(others));
console.log('\n--- bitwise coerces to 32-bit signed ---');
console.log('  2**31 | 0        =', 2**31 | 0);
console.log('  4294967296 | 0   =', 4294967296 | 0);
console.log('  ~~3.9            =', ~~3.9, '| ~~-3.9 =', ~~-3.9, '| Math.trunc(-3.9) =', Math.trunc(-3.9));
console.log('  ~~1e10           =', ~~1e10, '<- WRONG, exceeds 32 bits; Math.trunc =', Math.trunc(1e10));
console.log('  5 >> 1 =', 5>>1, '| -5 >> 1 =', -5>>1, '| -5 >>> 1 =', -5>>>1);
console.log('  flags: READ|WRITE =', (1|2), '| has WRITE?', ((1|2) & 2) !== 0);
