Scratch output for the pg-api scripts. Kept **inside the project** deliberately —
sandbox artifacts must not land in the host's `/tmp` (user instruction, 2026-08-13).

Everything here is regenerable; scripts recreate what they need on each run.
