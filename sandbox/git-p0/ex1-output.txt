
=== 1. version ===
git version 2.55.0
man-page date: 

=== 2. porcelain surface ===
main commands (git help -a, first section): 46
total commands on PATH:                    172

=== 3. commands that may or may not exist on this build ===
  switch           builtin
  restore          builtin
  sparse-checkout  builtin
  worktree         builtin
  maintenance      builtin
  replay           builtin
  backfill         builtin
  last-modified    builtin
  bundle           builtin
  range-diff       builtin
  filter-branch    builtin
  filter-repo      ABSENT
  lfs              ABSENT
  scalar           git version 2.55.0

=== 4. git init with no init.defaultBranch set ===
  hint: Using 'master' as the name for the initial branch. This default branch name
  hint: will change to "main" in Git 3.0. To configure the initial branch name
  hint: to use in all of your new repositories, which will suppress this warning,
  hint: call:
  hint:
  hint: 	git config --global init.defaultBranch <name>
  hint:
  hint: Names commonly chosen instead of 'master' are 'main', 'trunk' and
  hint: 'development'. The just-created branch can be renamed via this command:
  hint:
  hint: 	git branch -m <name>
  hint:
  hint: Disable this message with "git config set advice.defaultBranchName false"
  Initialized empty Git repository in /tmp/git-p0-AvIHiY/defaultbranch/.git/
HEAD points at: refs/heads/master

=== 5. ref storage backend ===
default ref format:  files
reftable supported:  yes → reftable
reftable on disk:    0x000000000001-0x000000000001-5d9a0b8b.ref tables.list 
loose-ref layout:    heads tags 

=== 6. object format ===
default object hash: sha1
sha256 supported:    yes → sha256

=== 7. defaults that changed in recent versions (unset = git's built-in default) ===
  init.defaultBranch       (unset)
  pull.rebase              (unset)
  push.default             (unset)
  merge.conflictstyle      (unset)
  core.autocrlf            (unset)
  feature.experimental     (unset)
  fetch.prune              (unset)
  diff.algorithm           (unset)
  maintenance.auto         (unset)
  gc.auto                  (unset)
  rebase.updateRefs        (unset)

=== 8. what a bare 'git pull' does on diverged branches with pull.rebase unset ===
  From /tmp/git-p0-AvIHiY/defaultbranch/.
     d3d4fdd..68ee6ce  master     -> origin/master
  hint: You have divergent branches and need to specify how to reconcile them.
  hint: You can do so by running one of the following commands sometime before
  hint: your next pull:
  hint:
  hint:   git config pull.rebase false  # merge
  hint:   git config pull.rebase true   # rebase
  hint:   git config pull.ff only       # fast-forward only
  hint:
  hint: You can replace "git config" with "git config --global" to set a default
  hint: preference for all repositories. You can also pass --rebase, --no-rebase,
  hint: or --ff-only on the command line to override the configured default per
  hint: invocation.
  fatal: Need to specify how to reconcile divergent branches.

=== 9. is 'git switch' still advertised as experimental? ===
  usage: git switch [<options>] [<branch>]
  
      -c, --[no-]create <branch>
  usage: git restore [<options>] [--source=<branch>] <file>...
  
      -s, --[no-]source <tree-ish>

=== 10. the empty-identity failure (why every setup guide starts with user.email) ===
  Author identity unknown
  
  *** Please tell me who you are.
  
  Run
  
    git config --global user.email "you@example.com"
    git config --global user.name "Your Name"
  
  to set your account's default identity.
  Omit --global to set the identity only in this repository.
  

=== 11. hash of an empty file, and what an object actually is ===
hash-object of empty blob: e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
raw object header:         blob
sha1 of "blob 6\0hello\n": ce013625030ba8dba906f756967f9e9ca394464a
git says:                  ce013625030ba8dba906f756967f9e9ca394464a
